# Execution progress log

**Purpose: interruption recovery.** Update as items complete and always at stage
boundaries. A fresh session must be able to continue from this file + `git log`
alone. Keep the "Resume here" line current; record any deviation from PLAN.md and
any execution-time micro-ruling in the notes column (with the commit that took it).

Commit convention: small per-item commits on this branch
(`worktree-py-ext-feedback-plan`), messages prefixed `pyext-<item>:`.

**Resume here:** EXECUTING (started 2026-08-10 on user signal; branch rebased onto
main at d52fe4c — dev-docs cleanup landed). Stages 1, 2, 3, and 4 COMPLETE (all
gates green; Stage 4 = documentation batch, items 4.1–4.15, docs-only — no code
behavior changed); next: Stage 5 (api-baseline; rationale entries — ARCHITECTURE/
DESIGN_RATIONALE now editable only if the cleanup agent is done; courtesy notes;
delete this folder).

| Item | Status | Notes |
|---|---|---|
| 1.1 LineIndex multi-byte resume fix | **done** | boundary-advance on resume point (unclamped — offset==len queries rely on past-the-end computed_end); 3 regression tests |
| 1.2 ChildRegion::staged() pub + Panics docs | **done** | staged() pub with full doc; three accessors' Panics sections point at is_resolved()/staged() guards; test covers fresh (Some) vs finished-tree (None) |
| 1.3 Include-chain message: written reference | **done** | hand-written Display (derive can't call error.message()); micro-ruling: typographic quotes ‘…’ matching sibling NoSourceResolver, tail = ResolveError::message() not full Display (would repeat the prefix); rewriting-resolver test added |
| 1.4 Oracle falsifiability test | **done** | drops the middle \emph node via restage (Emit(vec![])), reemits "one  three" from "one \emph{two} three" — span-copying would resurrect the dropped bytes |
| 1.5 format_position_with message | **done** | fallback now "@ char pos N (no line info)" — no cause named; assertion updated; no other test pinned the old wording |
| 1.6 scan_specials bounds → Err | **done** | content.get(pos..) guard in Package::scan_specials; spelling = Custom+ImplementationError (the token reader's shipped match-end-validation precedent — no new TokenErrorKind variant needed); micro-rulings: guard runs before the visibility gate (caller bugs not maskable by mode), error span clamped to content.len() (stays liftable to SourceSpan); pos contract documented on trait method + impl; only in-crate impl with the slice (ScopeStack folds, defaults ignore pos; test-only impls out of scope); 3 tests |
| 1.7 drop criterion + README bench line | **done** | criterion removed from dev-deps; README's whole Performance section removed (it contained only the cargo bench instructions — an empty header would remain otherwise); Cargo.lock untracked, nothing else |
| **Stage 1 gate** (build/test/docs) | **green** | cargo build clean; cargo test all suites 0 failed (lib 783, integration 30+22+13+8, doc-tests 68 passed/3 ignored); cargo docs from fresh target/doc, zero warnings |
| 2.1 + Any on four traits | **done** | +Any on SpecsProvider/ArgumentParser/EnvironmentBehavior/SourceResolver; CallableSpec rationale generalized, short pointer note on each trait; stale structure.rs no-Any comment removed; micro-ruling: SourceResolver's borrowed/boxed forwarding impls take the `'static` the Any supertrait forces (`&'static R`, `Box<R: 'static>`) — the plan's accepted non-'static break; 4 downcast round-trip tests |
| 2.2 Language::new Into<Arc> | **done** | `initial_state: impl Into<Arc<ParsingState<L>>>`; every by-value call site compiles unchanged; doc sentence on identity preservation; Arc::ptr_eq test via a parsed node's state |
| 2.3 NodeTree::slice(range) validated | **done** | O(1) parent-table check (parent(start)'s children block must contain range.end); root only as 0..1; in-bounds empty ranges answer Some (incl. start==node_count); NodeSlice::new stays pub(crate); no-TreeTag caveat documented like nodes_in; round-trip/cross-parent/root/empty/out-of-bounds tests over the hand-built example tree |
| 2.4 MacroSpec::with_after_effect | **done** | builder + private `after_effect` field; make_invocation_parser wraps StdInvocationParser, returns boxed delta (post-descent shape); both AfterEffectSpec test copies replaced by the public path — engine's finding #1–#3 tests ported from the private MacroLang scaffolding to Latexlike (MacroLang/MacroDriver deleted; finding #3 seeds via lang_initial_with_packages().derived() to keep the single-`]` premise); micro-ruling: the private field ends external MacroSpec struct-literal construction (new/default/builder cover it); tests: sibling effect + scoping, two-effect merge, persist_state on/off (existing, now public-path) |
| 2.5 TreeViolationKind/TokenKind as_str | **done** | both `pub const fn`, bare variant name (data-carrying variants answer the name only); exhaustive-match (no `_` arm) test per enum; micro-ruling: written as `const fn` although the NodeKind::as_str pattern is plain `fn` (plan text named const; strictly more general, no cost) |
| 2.6 NodeTree::tree_tag() pub | **done** | visibility flip + doc sentence naming the NodeId pre-check use before the always-on `node()` assert |
| 2.7 TreeViolation::new + MalformedBegin | **done** | TreeViolation::new(node, kind) (struct stays #[non_exhaustive]); no_constructor dropped from MalformedBegin (the only shipped condition carrying it — test-only conditions keep theirs); construct-and-match + Display test; MalformedBegin::new() doc-test |
| 2.8 DiagnosticInfo::identifier() | **done** | defaulted method answering Self::IDENTIFIER; blanket DiagnosticData forwards to the method; docs scope the override to binding/embedding adapter types per ruling (const stays required, remains the type's own identity); sealing comment softened; adapter round-trip + shipped-conditions-unaffected tests |
| **Stage 2 gate** (build/test/docs) | **green** | cargo build clean; cargo test all suites 0 failed (lib 796, integration 30+8+13+22, doc-tests 69 passed/3 ignored); cargo docs from fresh workspace target/doc, zero warnings |
| Stage 1 review fixes | **done** | 12 review findings applied in one commit (`pyext-stage1-review:`): argument-interior drop case in the oracle; pos-contract doc rescope + cross-refs (Lang/ScopeStack); ChildRegion companions in the crate panic register + should_panic pin; format_position fallback reworded (provider `None`, not just huge sources); line-index resume-point comment corrected + test strengthened against LineIndexCache incl. descending order; 4-byte-first-char display case with pinned root line; Package::scan_specials error names bounds vs boundary; ResolveError Display ‘…’ quotes + reworded distinguishable from the condition; Stage 5 PLAN line for the panic-policy companions clause; gates re-run green |
| Stage 2 review fixes | **done** | review findings applied in one commit (`pyext-stage2-review:`): E0034 collision sentence on DiagnosticInfo::identifier + Stage 5 PLAN api-baseline line (with the Language::new `L`-inference note); Diagnostic/ParseError identifier docs aligned to the method-not-const wording; MacroSpec construction-path sentence (fields stay as they are); slice/covering_slice reciprocal cross-refs + NodeTree::slice named in NodeSlice type/module docs; three downcast tests strengthened (marker "*" state via Debug; SourceResolver routed through ParseDriver::source_resolver on a StdParseDriver; EnvironmentBehavior routed through EnvironmentSpec::behavior with a state check — micro-ruling, the review's second named fix pointed at the resolver test which lives in source/resolver.rs); Box<R> forwarding impl drops redundant `'static` + comment names only the `&'static R` narrowing; NodeKind::as_str now `pub const fn` (the three as_str agree); discarded-after-effect comment in AfterEffectInvocationParser; structure.rs trailing blank line dropped; Stage 4 PLAN item 15 (guide coverage for Stage 2 additions); L8 bare-indexing nit skipped per ruling; fmt note: repo has no rustfmt.toml and HEAD carries ~1417 default-rustfmt divergences — gate read as "no new divergence on touched files" (verified per-file, one divergence removed); gates re-run green |
| 3.0 HookFailed condition (with cause field) | **done** | lives in src/error.rs (the error facade — one canonical path `techy::error::HookFailed`); identifier `core.error.hook-failed` (area = the module the condition lives in, matching the register convention); cause field per ResolveError (`Option<Arc<dyn Error + Send + Sync>>`, Clone by refcount, no PartialEq — the UnresolvableSourceReference precedent); micro-rulings: added `with_cause` by-value sugar mirroring ResolveError (the derive's `new()` takes the cause positionally as an `Arc`; a concrete error won't unsize through `impl Into<Option<Arc<dyn …>>>`); added `ToDiagnosticValue for Arc<dyn Error + Send + Sync>` in error.rs (renders the cause chain outermost-first, the ResolveError projection idiom) so the derive serializes the field; type docs state the three-way split (HookFailed / ImplementationError / document conditions); 3 tests (construction+Display, chain access via ParseError downcast + Arc-shared clone, serializable_data with and without cause); **[corrected by Stage 3 review]** identifier is now `core.hooks.hook-failed` — the register's areas are topical, not module names (cf. `core.specs.scope-op-failed` living in constructs/) |
| 3.1 Tier A signatures (+ lang_initial ripple) | **done** | two commits: (a) `initial_state_data -> Result<StateData, FinalizeError>` + the lang_initial family fallible — 169 call sites rippled (src/tests/guides; `.expect("seed state")` uniformly, matching the guides' `.unwrap()` style — no Result-main wrappers exist there); FinalizeError Display generalized to "cannot build the parsing state: {message}" (old wording named finalize_transition, false for seed refusals) + type docs list both producers; minidefs' `minilatex_package` stays infallible — base-mode read falls back to `L::ModeId::default()` on a failing seed (documented in a comment; same value StateData::empty seeds); BrokenSeedLang test pins both constructors passing the Err through. (b) resolve_command (ParseDriver + CommandResolver in step), resolve_state_event, and both Compute arms return Result<_, ParseError>; DeriveError lift ruling: no in-crate `From` exists or is possible (ParseError needs a span DeriveError doesn't carry) — the documented lift is `HookFailed::new(e.to_string(), None).with_cause(e)` (DeriveError: Error+Send+Sync rides the cause field), stated on the Compute variants and exercised by test; micro-ruling: hook Errs get the live traceback attached at the call site when they carry none (`ParseContext::attach_hook_frames`, pub(crate)) — hooks have no session access, mirroring implementation_error's frame attachment; lower_state_events pops the lent stack entry on the abort path (break-then-pop, tested); Err-vs-CommandResolution::Failed channel split documented on resolve_command (Failed = diagnose+recover, Err = abort); each hook doc states "Ok(...) wrapping is the only change" for infallible impls; guides show no literal signatures of these hooks (links only — no guide edits needed beyond the seed examples in (a)); 3 abort-semantics tests (Compute with real derived() failure + cause-chain check, resolve_command under Tolerant with frames, resolve_state_event with zero stack residue) |
| 3.2 Tier B signatures (+ observe_transition sink) | **done** | five commits (stop hooks / factories / body_state_delta / make_node_ext / observe_transition). All seven PLAN rows landed with the 3.1 conventions (frames via `ParseContext::attach_hook_frames` at every consultation site — hooks have no session access; Err-means-abort under any policy; hook docs state the HookFailed/ImplementationError/document-condition split and "Ok(...) wrapping is the only change" for infallible impls). Factory docs keep "could not build the parser" distinct from depth refusal (descent guard's `DescentLimitExceeded`). Micro-rulings: (a) **make_node_ext errs with `NodeBuildError` + new variant `ExtMintFailed { detail: String }`** — message-only, NO Arc'd cause field: a `ResolveError`-style cause would cost the enum its derived PartialEq/Eq (matched by in-crate and downstream assertions), and the established parse lift stringifies anyway (stage_node threads the error; staging callers' `implementation_error` lift aborts with frames) — an impl renders its cause chain into `detail`; (b) **`SessionDeriveError` (new, `techy::core`)** carries the session-mediated derivations' two failure arms — `Derive(DeriveError)` (structured tolerant-continuation material) vs `Observe(ParseError)` (observe_transition's abort) — DeriveError could not absorb a ParseError (it promises a recovered state and is also produced by out-of-parse `derived()`); (c) observe_transition parameter order `(ext, diagnostics, prev, new, delta)` — the two mutable sinks first, then the observed facts; (d) session memo inserts moved after the observation so "on Err nothing is committed" stays uniformly true; (e) token_stop/test_node_stop stay session-blind helpers returning Result, frames attached by the loop (the 3.1 Compute pattern). Two guides carried literal signatures (construct-parsers.md takeover factory, custom-lang.md hidden make_node_ext) — updated, plus the builder.rs staged_children recipe snippet. Tests per hook: failing-hook (abort under Tolerant + frames where a parse is live: predicate, node condition, spec factory in a group, driver nodes factory at a group descent, body_state_delta through Language::parse, mint inside a parse as ImplementationError, observe Err on the framed inner descent) + builder-level ExtMintFailed value/Display + the sink test (error-severity diagnostic recorded, parse completes) + the pre-existing hook tests as the Ok-wrapping-only proofs. |
| 3.3 Tier C infallibility docs | **done** | all seven hooks (recovery / refine_diagnostic / make_paragraph_break_node / source_resolver / specials_trigger_chars / ComposePiece::append / LineColProvider::line_col) carry a "Deliberately infallible: …" paragraph — rationale per HOOK_FALLIBILITY Tier C table + the embedding course (report through the embedding's own channel; neutral answers: Recovery::Strict / pass `data` through / the default chars node / `None` resolver / TriggerChars::Any / leave `self` unchanged, surface via Recomposer::Error / `None`); docs-only, no signature or test changes |
| 3.4 stage_invocation Err not panic | **done** | one `content.get(start..end)` guard before `SourceSpan::new` covers all three invalidity causes (decreasing range / out of bounds / off a char boundary); error path = the existing `implementation_error` lift (ImplementationError, aborts under any policy), error located at the trigger's span (always valid); the `Span::new(start, end)` in the stage_node map_err is now provably panic-free (end >= start post-guard); doc gains an Err paragraph — there was NO pre-existing Panics entry to remove (the panic was implied via SourceSpan::new; lib.rs register never listed stage_invocation, verified) — and no guide claimed the panic; tests: end<start, end>len, mid-multibyte end, and Tolerant-does-not-swallow, all via a BadEndSpec takeover through Language::parse |
| 3.5 ParseDriver::diagnostics_limit() | **done** | defaulted `Option<usize>` hook beside recovery(); seeding lives in `Language::parse_source` (the only in-crate session-construction path with a driver in hand — `ParserSession::new()` stays driver-free; attached-source sub-parses reuse the enclosing session, verified), placed before observe_parse_start so the cap governs the whole sink; micro-ruling: hand-built sessions apply the cap themselves via the public `diagnostics` field — stated on the hook doc (the install_descent_guard precedent); `Diagnostics::with_limit` gained the reciprocal pointer sentence; test: CapDriver(Tolerant, Some(2)) parse of "}}}}"→ limit 2 / len 2 / suppressed 2 / has_errors, plus the None-default keeps DEFAULT_LIMIT; DocLang's seed block extracted into generic doc_state_data() so CapLang shares it |
| 3.6 ParseResult returns SessionExt | **done** | public field matching the struct's existing style (plain public fields, not #[non_exhaustive]; sole construction site = ParserSession::finish); micro-ruling on the name: `session_ext`, not the session's own `ext` — in ParseResult's scope the tree's node exts compete ([§dd-arch:naming] specificity), and the field mirrors `L::SessionExt` directly; field doc states the data-half/reporting-half split, observe_transition + finish docs gained the read-back pointer, both custom-lang guides too; Debug prints it; test: ObserveDriver counts 2 transitions over "{a}{b}" via Language::parse and reads them off result.session_ext (+ `()` read-back for a no-ext lang) |
| **Stage 3 gate** (build/test/docs + hot-path size check) | **green** | (a) hot-path sizes measured (pinned by `constructs::tests::hot_path_result_payloads_stay_within_one_cache_line`): `ParseError<Option<String>>` = 64 B; `Result<bool, ParseError>` (Predicate/StopSpec::node; pre-sweep `bool` = 1 B) = 64 B; `Result<Arc<ParsingState>, ParseError>` (both Compute arms; pre-sweep `Arc` = 8 B) = 64 B — the `Result` wrapper adds ZERO bytes over bare `ParseError` (niche-fit), stays within one cache line, and is smaller than the loops' pre-existing per-token plumbing (`ConstructParserResult<_, Option<Token>>` = 88 B, the probe_token return that already rode every content loop) → **no box** (the descent-precedent boxing threshold not met; boxing would shrink to 16 B but restructure the just-landed public hook signatures without need; the test doc says re-measure before restructuring); (b) cargo build clean, cargo test 0 failed (lib 817, integration 30+8+13+23, doc-tests 69 passed/3 ignored), cargo docs from fresh target/doc zero warnings; (c) stale-signature grep over src+docs clean — remaining bare `-> Box<dyn ConstructParser` forms are `EnvironmentBehavior::make_body_parser` (outside the ruled Tier A/B roster — deliberately untouched) and helpers/test fns; `resolve_command_in_scopes` stays a bare-value helper by design (hooks wrap in Ok); both guides carry only the fallible forms |
| 4.1+4.2 source identity; GroupRule identity | **done** | `pyext-4.1-2:` Language::parse + Source::new state fresh-identity + identity-based span equality + the hold-one-Arc/parse_source correlation route; GroupRule (ruling: KEEP structural `==`, document) gains an "Identity matters" section naming all four verified `Arc::ptr_eq` sites (derived()'s temporary scope check, PrefixTable reuse, group-interior memo, optional-argument parsers' own-rule match) and scoping `==` to TokenKind equality's question; TokenRules::groups cross-refs it. Note: the `==` is a manual impl, not a literal derive (the `L: Lang` generic) — ruling applied unchanged |
| 4.3+4.5 ParseDriver dispatch/wrapping; session field doc | **done** | `pyext-4.3+5:` trait-level "Wrapping a driver" section + explicit self-dispatch clauses on recover (refine_diagnostic + recovery through `self`), probe_token (recovery), make_invocation_parser (delegates to the spec, builds nothing itself); ParseContext::session field doc rewritten to the reachable surface (diagnostics, ext, snapshot_frames()) routing everything else through the context's methods; builder/memos/frame stack named internal |
| 4.4+4.6+4.7 guide chapters | **done** | `pyext-4.4+6-7:` stage_node prose in both chapters = Result (BuildId or NodeBuildError lifted via implementation_error, never swallowed); replay-granularity rewritten in custom-lang.md + ai-guide-custom-lang.md (collapse = the forwarding construct's own transition; sibling after-effects, never descents — a nested group descent is an ordinary derivation; worked \one/\two example jumping straight to the later value, built on MacroSpec::with_after_effect); construct-parsers.md gains the attached-source paragraph (attach_source_reference = resolve-and-diagnose vs parse_attached_source = source-in-hand; sub-parse shares the session so staged ids are the caller's; routed through parse_construct, descent guard included; persist-vs-transparent) and the group_interior_state paragraph (memoized per (base, rule); hand-deriving loses the memo AND the driver's delta — verified: post-3.2 it returns SessionDeriveError, memo on ParserSession) + the with_after_effect declarative-route sentence (4.15 overlap) |
| 4.8–4.12 rustdoc clarifications | **done** | `pyext-4.8-12:` make_node_ext + StagedChildren borrow warning (view borrows storage the pending staging call grows; stated for lifetime-erasing embeddings); SourceRecomposer names its only failure = the coherence check no parse output triggers (variant pointer); Package::get states specials keyed by trigger → always None here, read via iter_symbols/scan_specials; slot_content_parent + ContentNodes::InRegion name input_macro_spec's `attached` slot as the shipped wrapperless example (verified input.rs:325); carrying source = `node.span().source()` on TextContent::resolve + node module doc + the two invocation-syntax payload spots. 4.13's summary caveat physically rode this commit (same file staged) |
| 4.13+4.14 summary caveat; extract triple | **done** | `pyext-4.13-14:` summary(): ruled wording — may change between releases, exact within one, the crate's own tests pin it, agreeing re-implementations reproduce verbatim; extract module: the triple is one operation — suffixed forms = the general form with a canned callback, `Clone + Default` bound only on `_keep_annotations` |
| 4.15 guide coverage for Stage 2 additions | **done** | `pyext-4.15:` MacroSpec rows in specs.md + ai-guide-definitions.md mention the optional after-effect; NodeTree::slice → ai-guide-trees table + integration.md navigation paragraph; NodeTree::tree_tag → integration.md ids paragraph (pre-check before the panicking node(); get() answers None) + ai-guide-embedding row; TreeViolation::new → micro-ruling: NO guide discussed validate_tree at all, so integration.md gained a two-sentence "Checking a tree" paragraph (the embedder audience validate_tree's own docs name) + ai-guide-embedding row; DiagnosticInfo::identifier → parsing.md matching-conditions paragraph (const is the norm, method = binding-adapter exception) + new ai-guide-embedding row |
| **Stage 4 gate** (docs build, link check) | **green** | cargo test all suites 0 failed (lib 817, integration 30+8+13+23, doc-tests 69 passed/3 ignored); cargo docs from fresh target/doc zero warnings (= intra-doc link check clean); generated-HTML anchor spot-check for every newly linked item (slice, tree_tag, TreeViolation::new, with_after_effect, DiagnosticInfo::identifier, group_interior_state, parse_attached_source) all present; docs-only stage — no code behavior changed, no test added/removed |
| Stage 3 review fixes | **done** | review findings applied in one commit (`pyext-stage3-review:`): README quick-start seed now `.expect("seed state")` (H1); ExtMintFailed lifted as HookFailed at every parse-side staging lift — new pub(crate) `ParseContext::staging_error` applies the split, all 17 stage_node/stage_invocation lift sites switched, pinning test now expects `core.hooks.hook-failed` + group frame, stage_node/stage_invocation/make_node_ext/guide docs state the split (H2); `#[non_exhaustive]` on `SessionDeriveError` (M3); HookFailed identifier → `core.hooks.hook-failed`, all pins updated + 3.0 row annotated (M4); three "empty one-liner" spots → "the `Ok(())` one-liner" (M5); ExtractError::Build + NodeBuildError poisoned-builder docs qualified for ExtMintFailed (M6); size-pin test now asserts the niche-fit equalities + one <=64 bound, println! dropped (M7/L15); invocation frame pushed around the parser factory call via with_frame — factory tracebacks name the failing spec; test pins [callable ‘\\fail’, group ‘{’] (L9); weak !is_empty frame assertions strengthened to exact title/count pins (resolve_command, nodes factory, body_state_delta, Compute-arm now pins the empty snapshot) (L10); three guide condition paragraphs state the three-way split incl. HookFailed (L11); minilatex_package rustdoc states the failing-seed fallback (L12); ParseResult::session_ext completed-parse-only sentence (L13); ParseContext type doc gained the third-party hook-dispatch traceback paragraph (doc note in lieu of L8); PLAN.md gained "Post-execution follow-ups for the user" (L14 latent span panic in environment_parser ~:706; L8 attach_hook_frames pub flip) |
| 5 closure (api-baseline; rationale entries AFTER cleanup agent done; courtesy notes; delete folder) | pending | ARCHITECTURE/DESIGN_RATIONALE untouchable until cleanup agent finishes |

## Stage 5 — semver record (cargo-semver-checks against `api-baseline`)

**Baseline position (found at execution, differs from the plan's note):**
`api-baseline` already points at `1659057` (the descent-merge tip, whose
ancestry includes the lang-features merge `0027167`) — the reflog shows a
recent `branch: Reset to HEAD`. So the report below covers **exactly this
plan's Stage 2+3 changes**, NOT the lang-features/descent breaks the plan
expected to see mixed in. The branch was NOT moved by this stage — the
`api-baseline` move happens at release/merge time by the user
(`git branch -f api-baseline <release-commit>` per the script header).

**Run:** `bash scripts/check_semver.sh` (cargo-semver-checks v0.50.0 already
installed), on `worktree-py-ext-feedback-plan` at the Stage-5 tree.
Result: `196 checks: 190 pass, 6 fail, 0 warn, 58 skip` — "semver requires
new major version" (at 0.x: minor bump, 0.(x+1).0).

The 6 failing checks, mapped to plan items:

1. `constructible_struct_adds_field` — `ParseResult.session_ext` (item 3.6;
   `ParseResult` has plain public fields, so exhaustive literals break).
2. `constructible_struct_adds_private_field` — `MacroSpec.after_effect`
   (item 2.4; struct-literal construction of `MacroSpec` no longer possible —
   intended, recorded micro-ruling).
3. `enum_no_repr_variant_discriminant_changed` — `NodeBuildError`: 16 variants
   shifted one position by the inserted `ExtMintFailed` (item 3.2; breaks only
   numeric casts of discriminants, no `repr` involved).
4. `trait_added_supertrait` — `SpecsProvider`, `SourceResolver`,
   `EnvironmentBehavior` gained `Any` (item 2.1; the tool did not flag
   `ArgumentParser`, which gained `Any` in the same commit — record it
   manually).
5. `trait_method_parameter_count_changed` — `ParseDriver::observe_transition`
   4 → 5 parameters (item 3.2, the diagnostics sink).
6. `trait_method_return_value_added` — `ParseDriver::observe_transition`
   `()` → `Result` (item 3.2).

**The tool's list is NOT the full breaking record** — cargo-semver-checks has
no lint for a changed (as opposed to added) trait-method return type or for
`&dyn Fn` signature changes inside enum variants, so the fallibility sweep's
main body is invisible to it: `initial_state_data`, `resolve_command`
(+`CommandResolver`), `resolve_state_event`, the three driver factories,
`CallableSpec::make_invocation_parser`, `body_state_delta`, `make_node_ext`,
`TokenStopKind::Predicate`, the node-stop, both `Compute` arms, and the
fallible `lang_initial*` family. The complete hand-tracked list (one line per
signature) is `dev-docs/extra/PY_EXT_UPDATE_NOTES.md` §2, plus the two narrow
Stage 2 source breaks recorded in PLAN.md Stage 5 (the
`DiagnosticInfo::identifier` E0034 ambiguity; `Language::new`'s
`impl Into<Arc<…>>` no longer pinning `L` by structural unification).
