# Code Review Reports

AI agents were instructed to review various source files of this project.  The reports are collected here.

## Source file `src/constructs/arguments_parser.rs`

(Relevant parts extracted manually from Opus' prompt output.)

1. Duplicated comment-node reconstruction (maintainability).
Lines 124–127 are byte-for-byte identical (same code and same explanatory comment) to nodes_parser.rs:593–596:
let content_span = Span::new(post_space.start - content.len(), post_space.start);
let start_span = Span::new(token.span.start, content_span.start);
let kind = NodeKind::comment(start_span, content_span, *post_space);
Two independent sites reconstruct the three sub-spans of a Comment token. A shared constructor — e.g. NodeKind::comment_from_token(&token) or a helper on TokenKind::Comment — would DRY this and keep the "span = delimiter + content + post-space" invariant in one place. Low priority, but this is exactly the kind of parallel logic that drifts.

2. Eager display allocation in the expression command path (efficiency).
parse_expression_node, line 220:
let display = format!("{}{}", escape_char, name);
This String is built for every command-in-expression dispatch (\frac1\alpha, etc.) but is consumed only on the rare requires-arguments error branch inside dispatch_expression_invocation (lines 271–274). The Specials arm already passes a borrowed name with no allocation (line 251). Consider deferring the format into the error branch (or passing the pieces) so the common zero-arg dispatch doesn't allocate. Micro-optimization — flagging because it's a per-invocation cost on the hot path, and the fix is local.

3. GroupArgumentParser fallback consumes a group of any class (design note, not a bug).
When the delimited-form class check fails (line 415), control falls to parse_expression_node, whose GroupOpen arm accepts a group of any class in scope (lines 205–213). So \frac[x]{y} — with […] registered as a group class — would swallow the whole [x] bracket group as \frac's first argument rather than treating [ as a single char. This matches the documented "expression = a delimited group of any class in scope" semantics and isn't exercised by the test rules (only brace is registered), so it may be fully intended. Worth a one-line doc note on GroupArgumentParser if the intent is specifically "my class as a group, everything else as a single token," since a reader could reasonably expect the fallback to be char-only.

Regarding Test coverage. One small gap I'd suggest: there's no test for a mandatory group argument whose delta affects tokenization interacting with the noise scan — e.g. a comment appearing in the noise while comments are disabled by the argument's delta. per_argument_state_delta_scopes_the_arguments_extent covers the delta scoping inside the group, but not a noise-scan token classified under the argument delta. Likely fine given scan_argument_noise runs under cx.state (the argument state), but it's an untested corner.

## Source file `src/constructs/nodes_parser.rs`

(Phase 6.2 — content dispatch loop core. Reviewed by an Opus agent; no correctness bugs found. Findings lean toward maintainability, robustness, and test gaps.)

**1. Comment sub-span reconstruction is duplicated and fragile (maintainability / robustness) — `nodes_parser.rs:593-596`, duplicated at `argument_parsers.rs:120-127`**
The three comment sub-spans are reconstructed by byte arithmetic:
```rust
let content_span = Span::new(post_space.start - content.len(), post_space.start);
let start_span = Span::new(token.span.start, content_span.start);
```
This exact block (comment included) is copy-pasted in `argument_parsers.rs:124-127` — the existing report already flagged this, and it's confirmed identical. Two concerns:
- The reconstruction only works because `TokenKind::Comment.content` is currently a raw source slice (`&'s str`), so `content.len()` equals its byte span (`token/token.rs:92-94`). It carries the content *string* but not a content *span*. Any future normalization/decoding of comment content (or a reader that synthesizes content) silently breaks both the span math and the partition invariant.
- `post_space.start - content.len()` is unchecked `usize` subtraction (line 594). It cannot underflow with today's invariants (content sits after a ≥1-byte start delimiter), but CLAUDE.md forbids panic paths in lib code, and this one is one broken invariant away from a debug panic / release wraparound.

Suggested fix: have `TokenKind::Comment` carry the content span (or start-delimiter span) directly, or add one shared helper (e.g. `fn comment_subspans(token) -> (Span, Span, Span)`) used by both call sites. Either removes the duplication and the fragile subtraction at once.

**2. `GroupClose` stop matching is correct — the `]`-closes-`{` class-vs-delim bug is handled (design-note / not a bug) — `nodes_parser.rs:393-398`, `716-728`**
The docs warn about a close matching on class alone. The code correctly requires *both* `delim == close` **and** `group_close_type(state, delim) == Some(*group_type)`, re-resolving the class against the same `cx.state` the tokenizer used. The four adversarial cases (shared class + different delim, reclassifying delta, expected-close path, delimiter-table path) are each covered by a dedicated test (`nodes_parser.rs:1337-1408`). Calling this out explicitly so it is not re-flagged: working as designed.

**3. `.expect("the node was just staged")` is a library-code panic path (robustness, low) — `nodes_parser.rs:375`**
`test_node_stop` does `staged.get(id).expect(...)`. It is a genuine internal invariant (the id was just returned by `stage`), so it cannot realistically fire, but it is the one non-debug `expect` on the hot path and technically violates the "never panic in lib code" rule. Acceptable as-is; if you want strictness, treating a missing id as "condition did not fire" (`false`) would degrade gracefully instead of aborting. Low priority.

**4. Loop-progress depends on an unstated `resume_pos` advancement contract (robustness) — `nodes_parser.rs:496-510`, `530-707`**
Every non-terminal arm makes progress by consuming a token (`move_past`). Recovery placeholders (`recovered == true`) are deliberately *not* consumed here; progress then relies entirely on `resume_at(cx.tokens, recovery.resume_pos)` having moved the reader strictly past the tokenizer error. If a `TokenRecovery` ever yields a `resume_pos` at or before the error position, the next `peek` re-hits the same error and the loop spins forever. This is a token-layer contract, not a bug in this file, but it is load-bearing and unasserted here. Consider a `debug_assert!` that the reader position advanced after `resume_at`, or documenting the contract at the peek site (line 505).

**5. Deferred diagnostic when a node condition pre-empts an unresolvable command (design-note / test-coverage) — `nodes_parser.rs:423-428`**
In `recover_as_chars`, if `flush_through` reports the node condition fired on the preceding run, the function returns `Ok(true)` *before* `cx.recover(...)`, so the "cannot resolve command" diagnostic is not emitted and the command token is left unconsumed for re-processing. This is consistent (the diagnostic is emitted when the caller re-encounters the command), but it means diagnostic count depends on whether a node condition is active — a subtle behavior with no test. Worth a one-line test asserting the diagnostic is deferred (not lost), or a doc note on the intended ordering.

**6. Minor test-coverage gaps for consume/predicate/callable-node paths (test-coverage) — tests region `nodes_parser.rs:1259-1602`, `1717-2119`**
Coverage is broad, but a few decided behaviors lack a direct test:
- `consume = true` is exercised only for `Command` (line 1453) and `GroupClose` (1479); not for `ParagraphBreak` or `Predicate` stop kinds (the post-space handling differs — a `ParagraphBreak`/predicate-matched comment carries post-space that `move_past(..., true)` would swallow).
- The node stop condition firing on a `Callable` (invocation) node — groups are covered (`node_condition_counts_a_group_as_one_node`, 2208) but dispatched invocations are not, despite `dispatch_invocation` ending in `test_node_stop` (line 474).
None are correctness concerns; they're the untested corners of otherwise-tested machinery.

**7. State is correctly restored on the error path (invariant, confirmed OK) — `nodes_parser.rs:462-466`, `698-701`**
Both descent sites do `cx.state = outer_state;` *before* the `?` on the child parser's result, so an aborting sub-parse never leaks the swapped child state to the caller. The invocation after-effect delta is applied to the loop's own state (line 472) and later discarded by the caller's own structural revert. Verified correct; flagged so it isn't mistaken for a leak.

**Summary:** No correctness bugs found. Prioritize **#1** (unify + harden the comment sub-span reconstruction — duplicated across two files and the only fragile arithmetic in the module). **#4** and **#5** are cheap robustness/clarity hardening. The rest are low-priority notes.

## Source file `src/constructs/group_parser.rs`

(Phase 6.3 — groups. Reviewed by an Opus agent; no correctness bugs found. `cargo test group_parser` passes 2/2.)

**Summary:** A clean, correct implementation. Both priority-1 concerns check out: the GroupClose stop condition passes **both** `group_type` and `close` (`group_parser.rs:104-107`) and `NodesParser::token_stop` matches them conjunctively (`delim == close && group_close_type(...) == Some(*group_type)`, `nodes_parser.rs:393-397`), excluding the `]`-closes-`{` and reclassified-`}` bugs. Structural state is reverted before any `?`, so no state leaks on abort. Span arithmetic partitions correctly in all three exit paths.

**1. State revert is correctly abort-safe — verified, not a bug (correctness) — `group_parser.rs:111-114`.** `cx.state` is restored (`cx.state = outer_state`) *before* `result?` unwinds, so a strict-mode abort inside the interior parse leaves `cx.state` at the caller's value. Same pattern in the two `cx.recover(...)?` recovery branches. Flagged as a positive because it's the easiest thing to get wrong here.

**2. `unreachable!` on `StopCause::NodeCondition` panics in release (robustness / design-note) — `group_parser.rs:144-146`.** The branch is genuinely unreachable — `GroupParser` builds its `StopSpec` via `StopSpec::at_token(...)`, which sets `node: None` (`nodes_parser.rs:175-177`), so `NodesParser` can never return `NodeCondition`. But `unreachable!` is a real panic in release, and CLAUDE.md mandates "never panic in lib code." This mirrors an existing `unreachable!` in `nodes_parser.rs:282`, so it's consistent with codebase convention and defensible as an invariant assertion; still worth a conscious call on whether these should be `debug_assert!`-guarded or folded into the `EndOfInput` handling. Low priority.

**3. EOF end position relies on reader `pos()` semantics — verified correct (correctness) — `group_parser.rs:131`.** Uses `cx.tokens.pos()` as the group's `end` in the unclosed-at-EOF path. After the interior parse hits `EndOfStream`, `NodesParser` flushes the terminal token's pre-space (trailing whitespace) into the interior siblings and does `move_to(&token, false)`, leaving `pos() == EndOfStream.span.start`, which equals the end of the last flushed interior node. So the partition invariant holds for `"{"`, `"{ab"`, and `"{ab  "` alike, and `StdTokenReader`/`TokenListReader` agree. Correct — noted because it's non-obvious and depends on the NodesParser flush contract.

**4. `UnexpectedGroupClose` diagnostic wording (maintainability) — `group_parser.rs:138`.** Emits `"unclosed group: expected '{close}'"` for what is actually a *mismatched / stray* close (a `]` under a `{`, or a re-classed `}`), pointing at the stray token's span. Consider distinct wording (e.g. `"mismatched group close: expected '{close}'"`) to help users distinguish the two recovery situations from the EOF message. Cosmetic.

**5. Own-file test coverage is thin; substantive paths covered by integration tests elsewhere (test-coverage) — `group_parser.rs:253-282`.** Only two direct-drive tests (well-formed round-trip under `Strict`; unclosed-at-EOF under `Tolerant`). Not exercised via the direct-drive harness: the `UnexpectedGroupClose` unwind, strict-mode abort (EOF and mismatch), `with_child_states`, nested groups, ambiguous-`$` interior-state derivation. These *are* all covered by `nodes_parser.rs` integration tests (e.g. `unclosed_group_strict_aborts`, `mismatched_close_inside_a_group_unwinds_without_consuming`, `ambiguous_dollar_group_closes_via_the_derived_expected_close`, `sibling_group_interiors_share_one_memoized_state`). Coverage is adequate overall; optional to make the file self-covering.

**6. `self.child_states.clone()` on each parse (efficiency) — `group_parser.rs:110`.** The clone is shallow (Arcs + borrowed `&dyn Fn`), so cost is negligible. `core::mem::take(&mut self.child_states)` would avoid it entirely, but `&mut self` permits re-invocation in principle, so the defensive clone is reasonable. Trivial; leave as-is.

**7. Doc-reference precision (maintainability / doc-note) — `group_parser.rs:25`.** The module header cites "(DESIGN_RATIONALE.md §3.6, §3.8)" for matching-and-recovery, but the rule that an unclosed group records an **empty `close` TextContent** is documented in §3.5 (alongside the `GroupData` definition). Adding §3.5 to the header cross-reference would be accurate. Cosmetic.

**Checked and found correct:** GroupData construction (`group_type: Some`, span-backed `open`/`close`, `ext: Default`); the recorded close is the exact delimiter span (GroupClose tokens carry no post-space); interior-state derivation goes through the session memo (`group_interior_state`) which always sets `expecting_group_close = rule`, guaranteeing the close stays recognizable (the `$…$` case); the outer (restored) state is attached to the group node while children were staged under the interior state; naming conforms to NAMING_STRATEGY.

## Source file `src/constructs/invocation_parser.rs`

(Phase 6.4/6.5 — invocation dispatch. Reviewed by an Opus agent; no correctness bugs found. All 24 argument tests pass.)

**Overall:** A clean, well-documented file that correctly implements the Phase 6.4/6.5 contract. The critical invariants — whole-trigger consumption, non-over-claimed post-space, per-argument state scoping with pre-`?` revert, and coordinate consistency between the flat child list and region-relative content offsets — all hold. Findings are minor plus one genuine test-coverage gap.

**1. Post-space handling is correct — no over-claiming, no double-count (correctness, verified) — `invocation_parser.rs:164`.** `post_space: TextContent::Spanned(token.post_space())` records exactly the trigger token's own syntactic post-space (`Command`/`Comment` kinds only; empty span at `span.end` otherwise, `token.rs:149-156`), matching the July 2026 amendment. The dispatch arm consumes the trigger whole first (`nodes_parser.rs:459`, `move_past(invocation.token, true)`), so `cx.tokens.pos() == token.span.end` on entry and `scan_argument_noise` cannot re-scan the post-space bytes. For `\frac 1 2` the recorded post-space sits between name and first region (span `5..6`); for `\& b` it is empty and the following space falls through to the caller as sibling content. Flagged as highest-priority because it is the file's riskiest invariant and it holds.

**2. Span-end relies on "last staged child is the source-rightmost node" (maintainability) — `invocation_parser.rs:149-155`.** `end` is taken from `children.last()`'s span end — correct for every standard argument shape, but an unstated invariant rather than a computed maximum. Since `parse_declared_arguments` is shared with the Phase 6.6 environment-shaped compositions (`invocation_parser.rs:85`), a future custom `ArgumentParser` whose region ends on a non-rightmost node would silently produce a too-short node span. Suggest a one-line comment pinning the assumption at line 149 (computing a `max()` is a per-invocation cost you likely don't want).

**3. Zero-arg / single-char-command post-space cases untested directly (test-coverage) — recommended action.** The 24 tests exercise `StdInvocationParser` end-to-end well, but two gaps: (a) no direct test of a callable with **zero declared arguments** asserting the trigger-only span and `post_space` (`\alpha` is dispatched in `expression_fallback_dispatches_a_full_invocation` but its span/post_space are never asserted); (b) the module's headline amendment example — a **single-character command** like `\& b` where post-space is empty and the trailing space must become sibling content — is not tested anywhere. Suggest adding: a zero-arg macro `\x` in `\x y` asserting `span == 0..2`, `post_space == ""`, sibling `" y"`; plus a multi-char `\x y` variant asserting `post_space == " "` and span including it. This is the exact behavior the July 2026 amendment turns on, only tested indirectly.

**4. Per-argument state restore is not unwind-safe (robustness, low) — `invocation_parser.rs:96-98`.** `cx.state` is swapped to `argument_state` and restored by plain assignment after `parse_argument`. If the argument parser panics, `cx.state` is left as the argument state. Acceptable under the project's abort-not-panic policy (an unwind tears down the borrowed `cx`), and `result?` propagates *after* the restore so the ordinary `Err` path leaks nothing. A `scopeguard`/`Drop`-based restore would be over-engineering. No change recommended.

**5. Per-invocation `Vec` allocations lack capacity hints (efficiency, low) — `invocation_parser.rs:89-90`.** `arguments` grows to exactly `argument_specs.len()`; `Vec::with_capacity(argument_specs.len())` avoids reallocation on multi-argument callables. Combined with the per-invocation `Box<dyn ConstructParser>` (decision 2's noted cost) and the required owned name copy (`name.into()`, line 160 — necessary), the hot path does several small allocations; only the `arguments` capacity hint is a free win. Minor.

**6. `.expect` builder read-backs (robustness, design-consistent) — `invocation_parser.rs:152`.** `.expect("the child was just staged")` is a panic in lib code, but it is the established builder-readback idiom (same pattern in `argument_parsers.rs:461`, `builder.rs`) governed by the builder's documented "panic on caller bug" policy. Unreachable in practice. Consistent with design; no change.

**7. Coordinate handling between flat child list and region-relative content is correct (correctness, verified) — `invocation_parser.rs:100-108`.** `children.extend_from_slice(&region.nodes)` builds one flat list across all arguments; `ChildRegion::new(start..end, region.content)` stores region offsets in combined-list coordinates while `region.content`'s `InRegion(..)` stays region-relative. Traced against `builder.rs:306-311`, which resolves `InRegion(r)` off the *resolved* region start — so the two coordinate bases compose correctly (e.g. `\frac 1 2` arg1 resolves content to `"2"`, not the noise space). No off-by-one. Confirmed by `single_token_fallback_stages_whitespace_as_region_noise`.

**Net:** No blocking issues. Recommended action is **#3** (add the zero-arg / single-char-command post-space tests), plus optionally the **#2** comment and **#5** capacity hint.

## Source file `src/token/list_reader.rs`

(Phase 6.1 — `TokenListReader`, a `TokenReader` over a pre-built `Vec<Token>` for isolated construct-parser tests. Reviewed by an Opus agent; a faithful reimplementation, 8/8 tests pass. `move_past`/`move_to`/`pos` are byte-for-byte identical to `StdTokenReader` and compute purely from the passed token's spans, so all fidelity risk concentrates in `peek` + `current()`.)

**1. Synthesized EndOfStream is a second, undocumented divergence from `StdTokenReader` (fidelity / design-note) — `list_reader.rs:89-93`.** The module doc (lines 18-23) claims the *only* inherent difference is the fixed list's inability to re-scan under momentary state. But the `None` branch synthesizes `EndOfStream` as `Span::empty(self.pos)` with **empty pre_space**, which diverges independently of re-scanning. Scenario: build a list from `scan("x  ")` but omit the trailing EOS token → `[Char('x') 0..1]`. After `next()`, `pos = 1`, and `peek()` yields `EndOfStream { span: empty(1), pre_space: empty(1) }`, whereas `StdTokenReader` on the same content yields `EndOfStream { span: empty(3), pre_space: 1..3 }` — both the terminal byte position and the trailing-whitespace pre_space differ. The root cause is inherent (a list without a trailing EOS carries no content-length or trailing-whitespace info), so it is not fixable in code, but the doc overstates equivalence: lines 18-23 and line 41 should be amended to note a synthesized EOS cannot reproduce trailing-whitespace pre_space or a terminal position beyond the last token's `span.end`. In practice `scan()` always appends EOS, so tests don't hit it.

**2. The documented `\verb`/in-span divergence is asserted nowhere (test-coverage) — tests around `list_reader.rs:230-235`.** The headline fidelity limit — `move_past(tok, false)` landing *inside* a token's span, after which `peek` returns the *following* listed token rather than re-scanning the post-space bytes (lines 18-23) — is never pinned by a test. `move_past_and_move_to_flags_match_std_reader` sets `pos = 6` via `move_past(false)` and checks `pos`, but never `peek`s there. A future change to `current()` could silently alter this behavior with all tests green. Suggest a test that, at that in-span position, asserts `TokenListReader` yields the following token AND explicitly asserts it differs from `StdTokenReader` — turning the prose contract into an executable one.

**3. `current()` lookup is correct and efficient — confirms faithful positioning (correctness, positive) — `list_reader.rs:69-72`.** `partition_point(|t| t.span.start < self.pos)` returns the first token with `span.start >= pos`. Because `pre_space.end == span.start` by convention, for any position at or before a token's start the clip `pre_space.start.max(pos).min(pre_space.end)` reproduces `StdTokenReader`'s `skip_whitespace`-derived pre_space exactly (verified against `reader.rs:157-158`). O(log n), not a linear scan. The `.min(pre_space.end)` also defensively prevents an inverted span if a hand-built token violates adjacency. This is the core of the file and it is sound.

**4. `move_past(tok, false)` can panic on subtraction underflow for hand-built tokens (robustness) — `list_reader.rs:101`.** `self.pos = tok.span.end - tok.post_space().len()` underflows (panic) if a hand-constructed token has `post_space().len() > span.end`. `StdTokenReader` has the identical line (`reader.rs:390`), so it's parity, not a regression — but this reader's entire purpose is hand-built lists, where malformed tokens are far more likely. Consider a `debug_assert!(tok.post_space().end <= tok.span.end)` to fail loudly rather than underflow-panic.

**5. `move_to_pos` drops the boundary/bounds guards `StdTokenReader` has (robustness / maintainability) — `list_reader.rs:64-66`.** `StdTokenReader::move_to_pos` (`reader.rs:141-145`) debug-asserts `pos <= content.len()` and char-boundary; this version sets `self.pos = pos` with no checks. There's no content here so those exact checks don't apply, but an out-of-order position silently produces the "following token" behavior with no diagnostic. Low priority given the fidelity limit is documented; a debug-only sanity note would help hand-built-list authors.

**6. `new()`'s invariant check is weaker than what `peek` relies on (maintainability) — `list_reader.rs:50-53`.** The debug_assert verifies only ascending, non-overlapping spans (`w[0].span.end <= w[1].span.start`). But `peek`'s clip fidelity additionally relies on `pre_space.end == span.start` and each token's pre_space lying in the gap after the previous token (`w[1].pre_space.start >= w[0].span.end`). A list satisfying the span-order assert but violating pre_space adjacency would clip incorrectly with no early warning. Consider strengthening the assert to also check these.

**7. `move_past`/`move_to` accept any token without verifying list membership (design-note) — `list_reader.rs:97-111`.** Both compute the new position purely from the passed token's span fields and never confirm the token came from `self.tokens`; a foreign/stale token silently repositions to arbitrary bytes. This mirrors `StdTokenReader` (intentional — the trait contract is position-from-token), so not a bug; for an isolated hand-built-list harness a membership `debug_assert` would be a cheap safety net.

**Coverage present and good:** round-trip parity against a scanned sequence, lockstep movement-matrix parity vs `StdTokenReader`, idempotent/terminal EOS parity for trailing whitespace *with* the EOS token present, mid-whitespace pre_space clipping, empty list, rewind-to-earlier-token. Main gaps are items 1 and 2.

## Source file `src/node/invariants.rs`

(Phase 6.3 — `check_tree_invariants`, a public test utility validating parsed trees, applied retroactively to the whole suite. Reviewed by an Opus agent. For well-formed parser output, all four rule families are genuinely enforced with no constructible false-pass; the `NodeKind` match is compiler-exhaustive. Findings are mostly latent gaps, robustness, and one notable test-coverage hole.)

**1. No negative tests for the callable/region/post-space logic — the most complex and most-amended code has zero broken-tree tests (test-coverage) — `invariants.rs:402-541`.** The three `#[should_panic]` tests cover only Chars-content, List-sibling-gap, and Group-open-delimiter. There is **no** negative test feeding a deliberately-broken *Callable*: region tiling gap/overlap (`check_regions`, 324-399), children-block non-contiguity (248-271), the revised rule-3 post-space position (226-244), content escaping its region subtree (355-391), nor the reachability/multi-parent/unreachable-node asserts (70-85). Since a false pass is the most damaging bug class here, this means a future refactor could silently weaken exactly the hardest-to-reason-about code. There's also no *positive* callable test in this file (all builders use Chars/Group/List); the callable path is validated only indirectly via `environment_parser.rs`/`argument_parsers.rs`/`nodes_parser.rs`/`node/mod.rs` tests. Suggest `#[should_panic]` tests staging a callable with (a) a region-tiling gap, (b) a post-space whose `end` misses the first child, (c) a node in two children ranges, (d) an unreachable node.

**2. Callable post-space is not verified whitespace-only, and its `start` is loosely bounded (completeness / minor) — `invariants.rs:236-244`.** Rule 3 (DESIGN_RATIONALE §3.5) pins post-space as the trigger token's own syntactic post-space, whitespace-only by construction. The checker asserts `s.end == expected_end && s.start >= span.start` but never checks the payload is whitespace, nor that `s.start` is at/after the name. A malformed post-space swallowing non-whitespace name bytes (`s.start == span.start`, covering the whole `\foo`) would pass. Likely intentional (whitespace is `Lang`-defined and not cheaply checkable here), but worth an explicit note. Same unchecked-whitespace observation applies to Comment `post_space` (160-168).

**3. Comment interior byte-accounting is only checked when *all three* parts are `Spanned` (completeness) — `invariants.rs:169-181`.** The partition `start.end == content.start && content.end == post_space.start` runs only inside the all-`Spanned` `if let`. If `content` were `Owned` while `start`/`post_space` are `Spanned`, an internal gap/overlap between delimiter and post-space would go undetected. Not reachable from current parser output (rule 1 makes fresh comments all-`Spanned`; `materialize()` makes them all-`Owned`), so a latent gap for future synthesized/transformed comments, not a live false-pass.

**4. Group interior arithmetic trusts `Owned` delimiter lengths without verifying they physically occupy the span (design-note / latent false-failure) — `invariants.rs:193-221`.** `text_len` computes the interior as `span.start + open_len .. span.end - close_len` but skips the positional prefix/suffix checks for `Owned` delimiters. For a *synthesized* group whose span covers only its interior with `Owned` delimiters corresponding to no physical bytes (constructible via `GroupData::untyped`), the interior would be miscomputed and the partition check would wrongly fail (or wrongly succeed if lengths line up). Out of current scope (`materialize()` preserves lengths byte-for-byte, per the `accepts_a_materialized_tree` test); worth a one-line comment recording the "span physically contains the delimiters" assumption.

**5. The root skipped-byte exemption described in the module doc is not actually coded (design-note) — `invariants.rs:1-5` + `check_interior_partition:279-306`.** A `List`'s interior is its whole span (186) and the partition is required exact (`pos == interior.end`, 298). When the promised tolerant root recovery that skips a stray close lands, the root `List`'s children won't tile its full span and this checker will panic — there is no exemption path. Correct today (matches the doc's "amends a test, not the architecture" stance), but a reader expecting the exemption to already exist will be surprised. Consider a brief `// no exemption is coded yet` note at the List arm.

**6. `check_regions` can panic with an opaque index/`unwrap` on a malformed tree instead of a clear assertion (robustness) — `invariants.rs:356, 369`.** `tree.nodes[content_parent.index()]` and `parent[a as usize]` are indexed without validating `content_parent` / the `content_range` bounds against `n` first; a resolved region pointing out of range produces a bare slice-index panic rather than a described violation. For a test utility a panic is acceptable, but an `assert!(content_parent.index() < n, …)` (and `content.end <= n`) would turn an opaque panic into a named failure.

**7. Doc comment claims O(n); `check_regions` walks ancestors, making it O(n·depth) worst case (maintainability / minor) — `invariants.rs:19`.** The content-parent ancestor walk (367-382) is O(depth) per region, so a deeply-nested tree with many callables isn't strictly linear. Negligible for a test utility; either qualify the doc ("effectively linear") or note the ancestor walk.

**8. Confirmation (not a defect): the four rule families and all NodeKind variants are covered.** Partition (List/Group → `check_interior_partition`), Spanned residency (per-arm checks), region tiling + children-block contiguity (`check_regions` forces exact coverage — orphan children and callables-with-children-but-no-regions both rejected), and single-parent/reachability/root are all present. Absent arguments (`region: None`) are correctly skipped via `filter_map` (321). No variant is unvisited.

## Source file `src/engine/mod.rs`

(Phase 6.1/6.3/6.4 — `ParserSession<L>`, the root engine object: `NodeTreeBuilder` + `Diagnostics` + `Recovery`, plus the 6.3 state-derivation seam (`derived_state`, `group_interior_state` ptr-keyed memo) and the recovery helper. Reviewed by an Opus agent; builds clean, panic-free in lib code, 9/9 tests pass. No correctness bugs found; substantive findings are scalability/retention.)

**1. `group_interior_memo` is an unbounded, linearly-scanned `Vec` — O(n²) time and monotonic retention over a parse (efficiency / robustness) — `mod.rs:53-54, 107-111`.** The memo is a `Vec<(Arc<ParsingState>, Arc<GroupRule>, Arc<ParsingState>)>` scanned with `.iter().find(...)` on every group descent. Two consequences on large/deeply-nested input: (a) *time* — each descent is an O(n) scan over all prior distinct `(base, rule)` pairs, worst-case O(n²) across a parse, partly undercutting the memo's own stated purpose; (b) *memory* — the memo never evicts and holds strong `Arc`s to every keyed `base` **and** `interior` state (and rule) for the whole session, so interior states (which nesting makes into deeper groups' `base`) are pinned alive long after their frame unwinds. Since a session is one transient parse it won't leak across parses, but it's a real per-parse scaling cost. Suggested fix: a `HashMap` keyed on `(Arc::as_ptr(base) as usize, Arc::as_ptr(rule) as usize)` for O(1) lookup — but note the keys must stay held (finding 2), so the retention is somewhat intrinsic and worth a conscious decision.

**2. Ptr-key ABA soundness holds — but only because the memo pins its keys (memoization / design-note) — `mod.rs:45-50, 107-111`.** The doc-comment's claim ("entries hold their key `Arc`s alive, so pointer keys cannot be reused") is correct: because each entry keeps a strong ref to `base` and `rule`, those allocations can't be freed while in the memo, so an incoming live `Arc` whose pointer `ptr_eq`s a stored key is necessarily the same allocation — hence the same logical state; no stale/false hit is possible. The derivation is also a pure function of `(base, rule)` (delta depends only on `rule` via `expecting_group_close`; `finalize_transition` runs over `base` with empty events), so a hit always serves the correct interior. A genuine soundness guarantee, not a latent hazard — but it is exactly the key-retention that drives finding 1; the two are coupled.

**3. Tolerant `recover` discards the structured `ParseErrorKind`, keeping only its `Display` string (maintainability) — `mod.rs:142`.** Strict mode returns `ParseError::new(kind, span)` (kind preserved), but the tolerant path stores `Diagnostic::error(kind.to_string(), span)` — collapsing `Token(..)` vs `Syntax { .. }` into an opaque message. Downstream consumers of `Diagnostics` can't programmatically distinguish the category the way a strict-path caller can. Consistent with the current `Diagnostic` type (only holds a `String`, `error.rs:44-49`), so not a bug today; flag as a future limitation if diagnostics ever need filtering by kind (fix: let `Diagnostic` optionally carry the `ParseErrorKind`).

**4. `derived_state` / group-interior threading is correct (correctness — confirmation) — `mod.rs:79-87`, cross-check `nodes_parser.rs:462-472`.** `derived_state` freezes the new state (`Arc::new(base.derived(delta))`, running `finalize_transition` once) and *then* fires `observe_transition(prev=base, new, delta)` — correct order, observation-only. The 6.4 after-effect threading in `dispatch_invocation` is also correct: the structural revert (`cx.state = outer_state`) happens first, then the delta is applied to the loop's own reverted state. `group_interior_state` fires `observe_transition` on every call including memo hits, while `finalize_transition` runs only on a miss — matching the documented split in `lang.rs:140-162`.

**5. Minor: the interior `delta` is fully rebuilt on every call, including memo hits (efficiency) — `mod.rs:103-106`.** `group_interior_state` constructs a fresh `TokenRulesOverrides` + `Arc::clone(rule)` on each call even on a hit, solely to pass to `observe_transition` (which by default ignores it). Cheap (one refcount bump, a struct of `None`s) and unavoidable given the signature; noted only for completeness.

**6. Test coverage is good on behavior, thin on the ptr-key edge and observe arguments (test-coverage) — `mod.rs:400-451`.** `group_interior_state_memoizes_by_pointer_identity_and_observes_hits` covers hit-returns-same-`Arc`, equal-but-distinct-`rule` miss, distinct-`base` miss, and observe-on-hit; `derived_state_is_data_equivalent_and_observed_but_never_memoized` covers the non-memoized path. Gaps: (a) no test drives the nested case where a returned interior is subsequently passed back in as a `base` (the reuse path finding 2's soundness rests on); (b) tests assert only the `transitions` counter, never that `observe_transition` receives the correct `prev`/`new` states. Both minor.



## Source file `src/token/prefix_table.rs`

(Phase 2/3 — the per-state delimiter table derived from `TokenRules::groups`, consulted on every token by `StdTokenReader::detect_group_delimiter`. Reviewed by an Opus agent; **no correctness bugs found** — the longest-match, tie-break, merge and UTF-8 invariants all check out. `cargo test prefix_table` passes 6/6 (8/8 including the two `parsing_state` tests that exercise the table). Findings are one piece of dead machinery whose doc contradicts a *reversed* design decision, plus efficiency/robustness/test-coverage notes.)

**Summary:** The matching logic is correct and the file is small and clear. The one substantive issue is `first_chars`: it is a leftover of the **rejected** maximal-run-`Chars` design (DESIGN_RATIONALE.md §4, "Maximal-run `Chars` tokens — rejected"), it is referenced by *no* call site anywhere in the crate, and its rustdoc still describes the deleted "content-character run" machinery. Meanwhile the hot path it *could* guard (`match_at`, run on every content character) does a full linear scan of the table for every character in the document. Fix those two together and the file is in good shape.

---

**1. `first_chars` is dead code, and its doc describes machinery that was deleted (maintainability / documentation) — `prefix_table.rs:52`, `92-98`, `108-112`.**
`first_chars()` has **zero call sites** in the crate (`grep -rn first_chars src/ tests/ examples/ *.md` matches only `prefix_table.rs` itself and its own test at line 264). Its doc says it is "used to bound content-character runs: a run stops at any character that might start a delimiter" — but content-character runs no longer exist: the July 2026 token-design review reversed maximal-run `Chars` in favor of single-character `Char(char)` tokens, and its own words are "*deletes the conservative stop-set machinery*" (DESIGN_RATIONALE.md §3.2, single-character `Char` bullet; listed again under §4 rejected patterns). So the accessor documents a design that was explicitly abandoned, and the field is computed (and allocated, and cloned) at every state freeze for nothing.

Two coherent outcomes — this is a design call, so flagging rather than prescribing:
- **Wire it in** as the hot-path guard for `match_at` (see #2). That is the direct analogue of `TriggerChars::may_start` (`specials.rs:74-82`), which guards the specials hook the same way, and it is what DESIGN_RATIONALE.md §6 open question **1b** ("Precompiled-table merging (`PrefixTable`++)") anticipates. If kept, the doc must be rewritten and the API should mirror the sibling: a predicate (`may_start(c) -> bool`) rather than a raw `&str` getter.
- **Delete it** (field, accessor, test) as vestigial, and let §6.1b reintroduce a merged first-char table when the hot loop is profiled.

Leaving it as-is is the worst of the three: it is a public API promising a use that does not exist.

---

**2. `match_at` is an unguarded linear scan run on every content character (efficiency) — `prefix_table.rs:104-106`, called from `reader.rs:174`/`276`.**
`peek_impl` calls `detect_group_delimiter` *before* the char fallthrough, for every position (`reader.rs:174`), and that calls `state.prefix_table().match_at(rest)` (`reader.rs:276`), which is:
```rust
self.entries.iter().find(|e| rest.starts_with(e.delim.as_str()))
```
So every plain letter of the document pays one `str::starts_with` per table entry — for a latexlike preset (`{}`, `[]`, `$`, `$$`, `\(`, `\)`, `\[`, `\]`, …) that is ~8–14 string comparisons per character, for a guaranteed miss. A single first-character membership test (`first_chars`, #1) rejects the overwhelming majority in one short scan; the entry loop then runs only on the ≤1% of positions that can actually start a delimiter. This is the cheapest available win on the tokenizer's hot loop and it needs no new data structure — the data is already built and already cached per state. (The module header's own claim, `prefix_table.rs:3-4`, is that "the hot token-reading path scans a small pre-sorted table" — accurate, but the guard that would make the scan rare is sitting unused next to it.)

---

**3. Two `expect()` panic paths in library code (robustness, low) — `prefix_table.rs:74`, `prefix_table.rs:94`.**
`entries.last_mut().expect("just pushed")` and `entry.delim.chars().next().expect("empty delimiters were skipped")`. Both are genuinely unreachable (verified: `add` returns early on an empty delimiter at line 67-69, and `push` immediately precedes the `last_mut`), but CLAUDE.md rule 4 forbids panics in lib code and both are removable without cost — e.g. push then index by the known position, and `if let Some(c) = entry.delim.chars().next()` for the first-char loop. Same class as the `unreachable!`s already flagged in `nodes_parser.rs:282` / `group_parser.rs:144`; consistent with codebase convention, so this is a "decide once, apply everywhere" item rather than a bug.

---

**4. Empty delimiters are silently dropped, though `GroupRule` documents them as non-empty (robustness / design-note) — `prefix_table.rs:67-69`, `rules.rs:36-37`.**
`GroupRule::open`/`close` are `pub String` and `rules.rs:36` states the invariant ("Open and close delimiters are arbitrary **non-empty** strings"), but nothing enforces it: `for_rules` silently ignores an empty side, and the reader independently re-guards with `!expected.close.is_empty()` (`reader.rs:266`). A rule with an empty `close` therefore produces an open-only entry and, if the group parser enters it, `expecting_group_close` can never fire — the group runs to EOF with no diagnostic pointing at the malformed rule. The defensive behavior is right (no panic, no empty-match infinite loop); what is missing is any signal to the *rule author*. A `debug_assert!(!delim.is_empty())`-style check at build time (or a one-line note on `GroupRule` that empty sides are dropped) would surface the construction error where it happens. Low priority; it is a "garbage in" path, not a live bug.

---

**5. Verified correct — the invariants worth not re-litigating (positive confirmations).**
- **Longest-match is genuinely longest.** `sort_by(|a, b| b.delim.len().cmp(&a.delim.len()))` (line 90) + first-hit `find` (line 105) is correct because any two delimiters that are both prefixes of the same input are necessarily prefixes of one another, so byte-length order = match-length order among the candidates. Byte length (rather than `chars().count()`) is therefore safe even for mixed multi-byte/ASCII tables. Test: `longest_delimiter_matches_first` (line 236).
- **UTF-8 / char-boundary safety.** `str::starts_with(&str)` only matches on whole `str` content, so `pos + entry.delim().len()` in `reader.rs:277` always lands on a char boundary. No `is_char_boundary` juggling needed; no slicing panic is reachable from here.
- **Tie order matches the documented contract.** `slice::sort_by` is a *stable* sort, so equal-length entries keep insertion order, which is `TokenRules::groups` order (open before close within one rule) — exactly what lines 47-49 promise.
- **"Earlier rule wins" and the direction invariant.** `add` fills a slot only `if slot.is_none()` (lines 77-81), so a duplicate claim in the same direction is dropped; and because a *fresh* entry always gets one slot filled immediately, no entry can exist with `open == None && close == None`. That is what makes `unreachable!("prefix table entries always carry a direction")` (`reader.rs:282`) genuinely unreachable — and `PrefixEntry`'s fields are private with no public constructor, so external code cannot violate it either.
- **`Arc` pointer identity survives the table.** `add` stores `Arc::clone(rule)` (line 79) and the reader re-clones the same `Arc` onto the `GroupOpen` token (`reader.rs:280`). `ParserSession::group_interior_state`'s memo is keyed on **pointer identity** of `(base_state, rule)` (`engine/mod.rs:53-54`, 89-95), so the fact that `PrefixTable` shares rather than deep-copies `GroupRule` is load-bearing for the memo's hit rate. Worth a comment: a future refactor storing `GroupRule` by value in `PrefixEntry` would silently degrade that memo to 0% hits.
- **`match_at` doubles as an exact lookup, and that is sound.** `nodes_parser.rs:716-728` (`group_close_type`) calls `match_at(delim).filter(|e| e.delim() == delim)`. This is only correct because entries are deduplicated by delimiter string (one entry per string), so an entry equal to `delim` is necessarily the longest prefix of `delim` and is the one returned. Correct today, but the idiom leans on an invariant stated nowhere at the call site — a dedicated `PrefixTable::lookup(&str) -> Option<&PrefixEntry<L>>` would express the intent and be O(n) either way.
- **The `enable_groups` gate is baked in as documented** (lines 62-63: empty `entries` *and* empty `first_chars`), matching DESIGN_RATIONALE.md §3.2's "flags bake into the eager per-state caches" decision, and the eager-at-freeze build matches §3.3's implementation note.

---

**6. Build allocates a `String` per delimiter at every state transition (efficiency, low) — `prefix_table.rs:73`, `70` (O(n²) merge).**
Every freeze — i.e. every group descent, every argument with a delta, every `\makeatletter` — rebuilds the table, allocating one `String` per distinct delimiter plus the `Vec`. The merge scan (`entries.iter_mut().find(...)`, line 70) is O(n²), irrelevant at n≈10. The docs already decided this is "cheap relative to a transition" (DESIGN_RATIONALE.md §3.3 implementation note), and `group_interior_state`'s memo removes the dominant repeat. Recording it only because the delimiter strings are *already* owned immutably behind the rules' `Arc`s, so the copies are avoidable in principle (store the owning `Arc<GroupRule<L>>` + a side flag) at the cost of some clarity. Not worth doing before profiling — explicitly a note, not a recommendation.

---

**7. Test-coverage gaps (test-coverage) — tests region `prefix_table.rs:174-275`.**
The six tests cover the main contracts, but these *decided* behaviors have no test:
- **Cross-rule open/close merging.** `same_string_open_and_close_merges` (line 226) only covers one rule opening and closing with `$`. The doc's headline case — "of the same rule (`$…$`) **or of different ones**" (lines 17-19) — where string `X` opens rule A and closes rule B, is untested, and it is precisely the ambiguity the merged entry type exists for. It should also assert the merged entry count (`entries().len() == 1`), which no test currently does.
- **Earlier-rule-wins for the *close* slot.** `conflicting_claims_earlier_rule_wins` (lines 245-254) asserts only `entry.open()`; the symmetric close-slot conflict (line 77-81's other branch) is never checked.
- **`enable_groups: false` at unit level.** The early return (lines 62-63) is only covered indirectly via `parsing_state.rs:262-281` (which asserts `match_at` misses) and `reader.rs:1305`. No test asserts `entries().is_empty()` **and** `first_chars().is_empty()` for a disabled table.
- **Multi-byte UTF-8 delimiters.** No test uses e.g. `«…»` / `⟨…⟩`: neither the `first_chars` push of a multi-byte char nor byte-length sorting against same-byte-length ASCII (`«` is 2 bytes, `$$` is 2 bytes) is exercised. Given the reader does `pos + delim.len()` arithmetic on the result, one such test is cheap insurance.
- **Strict-prefix miss.** With only `$$` registered, `match_at("$x")` must return `None` (a shorter input that is a strict prefix of an entry). Not covered.
- **Documented `entries()` ordering.** Lines 47-49 promise "longest first, equal lengths keep declaration order"; no test reads `entries()` and asserts the order, so the public ordering contract is untested.

---

**8. Naming and doc-text notes (naming / documentation, cosmetic).**
- `PrefixTable` / `PrefixEntry` are the names blessed in NAMING_STRATEGY.md:83 ("Derived delimiter table") — no change warranted, despite the table covering *only* group delimiters (comment starts get their own ad-hoc longest-match at `reader.rs:359-364`, command escapes their own `find` at `reader.rs:181`). §6.1b is the place where that generalization is queued.
- `prefix_table.rs:19` leaks internal planning vocabulary into public rustdoc: "(the WIP's \"open-or-close\" ambiguity merging)". The WIP is not something a library user can consult; the sentence works fine without the parenthetical.
- If `first_chars` survives #1, name it after its sibling (`TriggerChars::may_start`) rather than exposing a raw `&str`.

---

**Net:** in priority order —
1. **Resolve `first_chars` (#1 + #2).** Either wire it in as the `match_at` first-character guard — the one meaningful hot-path win available here, and the shape §6.1b already sketches — or delete it as vestigial. Its current doc actively misdescribes a reversed decision, which is the most misleading thing in the file.
2. **Close the merge/conflict test gaps (#7)**, above all the cross-rule open/close merge (the entry type's raison d'être) and one multi-byte-UTF-8 delimiter test.
3. **Retire the two `expect()`s (#3)** — free, and squares the file with CLAUDE.md rule 4.
4. Optional: `debug_assert` or document the empty-delimiter drop (#4); consider a named `lookup()` for the exact-match use in `nodes_parser` (#5); drop the "WIP" phrasing from public docs (#8).

## Source file `src/token/token.rs`

(Phase 2/3 — `Token<'s, L>` / `TokenKind<'s, L>`, the token model; amended in Phase 6.4 so `Specials` carries `callable_type`. Reviewed by an Opus agent. No correctness bugs in the file's own logic; the span/post-space invariants all hold. `cargo test`: 280 passed, 0 failed. **The file contains no tests at all** — 299 lines, zero `#[cfg(test)]` — so every behavior it decides is pinned only indirectly, from `reader.rs`.)

**Summary:** The model is sound and its trickiest invariant — `post_space()` being total, trailing-inside-`span`, and *exactly* the token's own syntactic post-space — is respected by construction at every producer and every consumer I traced. The substantive findings are (a) the type's headline doc invariant now **contradicts its own code** after the 6.4 amendment, (b) `TokenKind::Comment` carrying content as a `&str` with no span is a real API gap that pushes underflow-prone byte arithmetic into two consumers *and* opens a panic path for third-party `TokenReader`s, (c) three separate sites compute `span.end - post_space().len()` when a one-line accessor on `Token` would be exact and subtraction-free, and (d) the documented invariants are asserted nowhere, even though `Token::new` is the crate's sole construction site.

**1. The type's headline invariant list is stale — it flatly denies the `callable_type` the code carries (documentation) — `token.rs:16-20` vs `token.rs:81-89`.**
The first design-invariant bullet reads *"**No invocation-form knowledge.** There is no macro/environment/specials taxonomy here and no `CallableTypeId`"* — while 65 lines below, `TokenKind::Specials` declares `callable_type: L::CallableTypeId` (line 84), correctly annotated with the July 2026 amendment. DESIGN_RATIONALE §3.2 (lines 246-253) resolves this properly: the no-`CallableTypeId` rule is **scoped to tokens whose resolution happens at parse time** (`Command`); `Specials`, where recognition *is* resolution, carries the full `ResolvedCallable` pair. The doc on this type never got that scoping. Two other places repeat the unqualified claim: `src/token/mod.rs:15-18` ("no `CallableTypeId`, no begin/end-environment tokens") and DESIGN_RATIONALE.md:1722-1724's §4 *Rejected patterns* quick list ("**Invocation-form ids (`CallableTypeId`) on tokens** … re-creates the 'token says MACRO, node says ENVIRONMENT' wart"), which per CLAUDE.md is a document future sessions are told to obey. A reader — human or agent — following §4 would "fix" the 6.4 amendment back out. Highest-priority item precisely because it is cheap and because the doc is load-bearing policy: scope the bullet in all three places ("no `CallableTypeId` on `Command` tokens; `Specials` carries its resolution").

**2. `TokenKind::Comment` carries content as `&'s str` but no content/delimiter span — the model gap that forces the duplicated, underflow-prone arithmetic in consumers (API design / robustness) — `token.rs:92-100`.**
This is the item the prior reports raised in the *consumers*; evaluated at the model level, the answer is yes — the token should carry the span.
- Both consumers reconstruct the sub-spans identically: `nodes_parser.rs:593-596` and `argument_parsers.rs:124-127` compute `Span::new(post_space.start - content.len(), post_space.start)`, then derive the start-delimiter span from `token.span.start`.
- This works *only* because `StdTokenReader::read_comment` slices the content verbatim out of the source (`reader.rs:366-374`), so `content.len()` happens to equal its byte extent. `TokenReader` is a **public extension point** (`reader.rs:40`): a third-party reader that normalizes comment content (strips a `\r`, decodes, or synthesizes it) makes `post_space.start - content.len()` underflow — a debug panic, or a wrapped `usize` in release that then trips `Span::new`'s `debug_assert!(start <= end)` (`span.rs:23`) or panics later when the span is sliced. That is a lib-code panic reachable from a legitimate implementation of a public trait, not merely from a hand-built test token.
- Notably, **no consumer uses `content` as a string** — grep shows both sites use only `content.len()`. The `&'s str` is currently a length carrier.
- Cleanest fix, and it makes the token→node mapping 1:1: add `start: Span` (the matched start-delimiter span) to `TokenKind::Comment`, mirroring `NodeKind::Comment { start, content, post_space }` (`node/kind.rs:53-64`, whose `start` exists for exactly the same "which delimiter fired is a per-instance fact" reason). The reader already has it (`content_start = pos + start.len()`, `reader.rs:366`). Then `content_span = Span::new(start.end, post_space.start)` — no subtraction, no dependence on `content.len()`, and both duplicated blocks collapse into one `NodeKind::comment(...)` call. (A `Token::comment_spans()` helper would DRY the duplication but would *not* close the third-party-reader hole.)

**3. `span.end - post_space().len()` is written out three times and is exactly `post_space().start` (robustness / maintainability) — `reader.rs:390`, `list_reader.rs:101`, `environment_parser.rs:651`.**
Verified invariant: for the two kinds that have post-space, the span is built as `Span::new(pos, post_space.end)` (`reader.rs:338` for `Command`, `reader.rs:375` for `Comment`), so `post_space.end == span.end` always; and for every other kind `post_space()` returns `Span::empty(self.span.end)` (`token.rs:154`), whose `.start` is also `span.end`. Therefore **`tok.post_space().start` is the end of the token proper for every kind, universally**, and the three subtraction sites are computing it the long way. A one-line accessor on `Token` (e.g. `pub fn end_before_post_space(&self) -> usize { self.post_space().start }` — name per NAMING_STRATEGY's clarity-over-brevity rule) would: delete three unchecked `usize` subtractions (one of which the `list_reader.rs` review already flagged as a panic path for hand-built tokens), state the trailing-subrange invariant in one place, and give the Phase 7 `\verb` idiom (`move_past(tok, false)`) a named concept. Pure win, contained to this file plus three one-line call-site edits.

**4. The documented span invariants are asserted nowhere — and `Token::new` is the crate's sole construction site (robustness) — `token.rs:139-143`.**
The doc pins three invariants (`token.rs:112-123`): `pre_space` ends exactly at `span.start`; post-space is a trailing sub-range *inside* `span`; post-space never crosses a paragraph break. None is checked, not even in debug. I grepped for struct-literal construction: the only two `Token { … }` literals in the whole crate are inside this file (`token.rs:142`, `191`), so **every** token in the crate — reader-produced, hand-built test lists, recovery placeholders, the `resume_at` marker — goes through `Token::new`. A `debug_assert!` there is therefore effective in practice and free in release:
```rust
debug_assert!(pre_space.end == span.start);
// and for Command/Comment: post_space.end == span.end && post_space.start >= span.start
```
This is worth more than it looks: `TokenListReader::peek` *relies* on `pre_space.end == span.start` to clip pre-space correctly (`list_reader.rs:69-81`), and the `list_reader` review asked for exactly this check to be added there (its finding 6) — putting it in `Token::new` covers hand-built lists, third-party readers, and finding 3's identity in one place instead of three.

**5. `Specials` span is taken from `SpecialsMatch::end` with no validation — an empty match hangs the parse (robustness; adjacent files, but a `Specials`-variant contract question) — `reader.rs:191-201`, `specials.rs:29-33`.**
`Lang::scan_specials` is a public hook; the reader does `Span::new(pos, m.end)` with no check that `m.end > pos` or that it lands on a char boundary (confirmed: `reader.rs:186-201` has no guard, and `Span::new`'s `debug_assert` only catches `end < pos`, in debug). A scan returning `end == pos` yields a zero-width `Specials` token; the dispatch loop's progress comes entirely from consuming the trigger (`nodes_parser.rs:459`, `move_past(invocation.token, true)` sets `pos = span.end`), so the position never advances → infinite loop with all tests green. A non-boundary `end` panics later when the span is sliced. Neither `TokenKind::Specials`'s doc (`token.rs:74-80`) nor `SpecialsMatch::end`'s (`specials.rs:31-32`, "byte offset one past the end of the matched trigger") states that the match must be **non-empty** and boundary-aligned. Recommend documenting it on both, plus a `debug_assert!(m.end > pos)` (or a `TokenError`, since the scan already participates in the recovery-token protocol).

**6. No tests in the file; two decided behaviors are untested anywhere (test coverage) — `token.rs` (299 lines, no `#[cfg(test)]`).**
Coverage is entirely indirect, via `reader.rs`. Two decided behaviors that this file owns are not pinned:
- **`post_space()`'s `_` arm returns the empty span *at `span.end`*** (`token.rs:154`). The accessor is asserted directly exactly once — `reader.rs:1341`, on a `Command`. Nothing pins the *position* of the empty span for `GroupOpen`/`GroupClose`/`Char`/`ParagraphBreak`/`EndOfStream`, yet `invocation_parser.rs:164` and `argument_parsers.rs:295` record that value verbatim as a callable node's `post_space`. A misplaced empty span still resolves to `""`, so it would slip past almost every existing assertion (`nodes_parser.rs:1812` asserts `Some("")` for a specials invocation); only `check_tree_invariants`' rule 3 (`invariants.rs:226-244`) checks the *end* position, and only for callables. A five-line unit test in this file would pin it for all kinds.
- **The `PartialEq` asymmetry documented at `token.rs:195-198`**: `Specials` specs compare by **`Arc` pointer identity** (`Arc::ptr_eq`, line 217) while `GroupOpen` rules compare **structurally** (`r1 == r2`, line 206). Zero tests. Two `Specials` tokens with equal names but distinct-but-equal spec `Arc`s must compare *unequal*; two `GroupOpen` tokens with distinct-but-equal `Arc<GroupRule>`s must compare *equal*. This is test-support machinery (token equality is used only by assertions and by `TokenError`/`TokenRecovery`'s `PartialEq`, `error.rs:111,119`), which is exactly why a silent flip would go unnoticed — while quietly changing what every `assert_eq!(token.kind, …)` in the suite means.

**7. Phase 7 (`\verb`/verbatim) sufficiency: the token *model* is adequate; the *reader trait* is the seam to revisit (design note) — `token.rs:21-23`, `149-156`.**
Verified against the Phase 7 requirements: single-character `Char` tokens plus an `enable_*: false` child state give a verbatim parser byte-by-byte reading with real spans (whitespace included — `reader.rs`'s `whitespace_disabled_gives_character_level_content` test), and `post_space()` + `move_past(tok, false)` implement the documented "`\verb`-style parsers reposition before swallowed post-space" idiom (`reader.rs:1346`; DESIGN_RATIONALE §3.2 line 373). No new token kind looks necessary. But the *other* two things a `\verb` parser needs are not on the `TokenReader` trait: raw content and absolute repositioning. `content()` and `move_to_pos()` are **inherent** methods of `StdTokenReader` (`reader.rs:126`, `141`) and `TokenListReader` (`list_reader.rs:64`), not trait methods — so the crate fabricates a **fake `EndOfStream` token as a positional marker** to reposition (`constructs/mod.rs:70-74`). It works (every reader honors `move_to(tok, false) → pos = tok.span.start`), and raw content is reachable through `cx.source` since the 6.4 amendment, so nothing is broken; but before Phase 7 commits to the verbatim escape hatch it is worth deciding whether `move_to_pos` belongs on the trait rather than being expressed by manufacturing tokens that mean "cursor".

**8. `Display for TokenKind` drops the escape char, so consumers re-spell commands themselves (maintainability, low) — `token.rs:292`.**
`Command` displays as `Command("foo")` — the `escape_char` (the 6.0 addition whose whole point is that several command syntaxes may coexist) is not shown, so `\foo` and `@foo` render identically in diagnostics. `argument_parsers.rs:220` consequently builds its own user-facing spelling with `format!("{}{}", escape_char, name)` (the allocation the existing report flags). A `Token`/`TokenKind` helper that yields the invocation's written form would centralize the spelling and let that call site defer the allocation into its error branch. Cosmetic, but the two findings interlock.

**Confirmed correct (verified, not defects):**
- **Post-space containment and the "trailing sub-range" rule hold by construction.** `Command`: `post_space = name_end..skip_whitespace(...)` and `span = pos..post_space.end` (`reader.rs:326-338`); single-character names get `Span::empty(name_end)` (line 329), so `\&` genuinely takes none. `Comment`: `post_space = content_end..skip_whitespace(...)`, `span = pos..post_space.end` (`reader.rs:371-375`). Hence `post_space.end == span.end && post_space.start >= span.start` at every producer, and `post_space()` is total and coherent across all kinds.
- **The July 2026 amendment ("a token's post-space is exactly its own syntactic post-space; nothing beyond it is ever claimed") is honored by both recorders.** `invocation_parser.rs:164` and `argument_parsers.rs:295` copy `token.post_space()` verbatim into `CallableData::post_space` and never extend it; the trigger is consumed whole before any argument parser runs, so the post-space bytes cannot be re-scanned. `\& b`'s space correctly falls through as sibling content.
- **`pre_space.end == span.start` holds at every construction site**, including the two recovery placeholders (`reader.rs:206`, `304`) and the `resume_at` marker (`constructs/mod.rs:72`).
- **The manual `Clone`/`PartialEq`/`Eq`/`Debug`/`Display` impls are complete and field-exact** (checked variant by variant, including the 6.4 `callable_type` in all five). The stated reason for hand-writing them — derives would demand `L: Clone/Debug/PartialEq` although no `L` value is stored — is right, and `L::CallableTypeId: Copy + Ord + Hash + Debug` (`state/lang.rs:97`) supplies exactly what the impls need.
- **Naming conforms to NAMING_STRATEGY**: no `Latex` prefixes, `TokenKind` (not a bare `Kind`), and `callable_type`/`escape_char`/`post_space` match the vocabulary used on the spec and node sides.

**Net (priority order):**
1. **#1** — fix the stale "no `CallableTypeId`" invariant in `token.rs:16-20`, `token/mod.rs:15-18`, and DESIGN_RATIONALE §4's rejected list; it currently instructs future sessions to undo Phase 6.4.
2. **#2** — give `TokenKind::Comment` a `start: Span` (mirroring `NodeKind::Comment`); this deletes the duplicated reconstruction in `nodes_parser.rs`/`argument_parsers.rs` *and* closes a panic path reachable from any third-party `TokenReader`.
3. **#3** — add `Token::end_before_post_space()` (= `post_space().start`) and use it at the three subtraction sites.
4. **#4** — `debug_assert!` the span invariants in `Token::new` (the crate's single construction funnel).
5. **#6** — add a small `#[cfg(test)] mod tests`: `post_space()`'s empty-at-`span.end` contract for every kind, and the `Specials`-by-pointer / `GroupOpen`-by-value equality asymmetry.
6. **#5**, **#7**, **#8** — document/guard the `SpecialsMatch::end` non-emptiness contract; decide the `move_to_pos`-on-trait question before Phase 7; optionally centralize the command display spelling.

## Source file `src/source/span.rs`

(Phase 1/2 — `Span`, the plain `Copy` byte range on which all span arithmetic in the crate rests: tokens, `TextContent::Spanned`, `TokenError`, every node span before it becomes a `SourceSpan`. 98 lines, 3 tests, all green; full suite 280/280 + 1 doc-test. Reviewed by an Opus agent. **No correctness bug in the file itself** — the type is small and does what it says. The findings are about what the type *doesn't* do: it is the natural home for arithmetic that is currently hand-rolled (and, in two known places, underflow-unsafe) in six other files, and its safety contract is advisory in a way the rest of the `source` module's is not.)

**Summary:** `Span` is correct, minimal, and honest. Its `new()` debug-asserts `start <= end`; `empty()`, `len()`, `is_empty()`, `range()`, `slice()`, `From<Range>` and the compact `Debug` (`3..7`) are all right. But: (a) the fields are `pub`, so the validating constructor is bypassable and *is* bypassed in four places, making the `start <= end` invariant advisory rather than enforced — an asymmetry with `SourceSpan`, whose fields are private; (b) `len()` is an unchecked subtraction, so an inverted span wraps silently in release instead of failing; (c) the three `tok.span.end - tok.post_space().len()` underflow sites flagged by earlier reviews are **not** a missing-`Span`-helper problem — they are algebraically identical to `tok.post_space().start` and can be deleted outright; (d) `Span` and `SourceSpan` do not interoperate — every bridge goes through an untyped `Range<usize>` (21 occurrences of `SourceSpan::new(&cx.source, span.range())`); (e) no `contains` / `overlaps` / `merge` / `get` exist at all, and consumers hand-roll each of them. Test coverage is thin in exactly the areas where the type's contract lives: no multi-byte / char-boundary test, no inverted-span test, no `slice`-panic test.

---

**1. The three `span.end - post_space().len()` underflow sites are removable, not fixable with a new helper (robustness / simplification) — `reader.rs:390`, `list_reader.rs:101`, `environment_parser.rs:651`.**
All three compute "the position just before the token's post-space" as an unchecked `usize` subtraction:
```rust
self.pos = tok.span.end - tok.post_space().len();   // reader.rs:390, list_reader.rs:101
resume_at(cx.tokens, trigger.span.end - trigger.post_space().len());  // environment_parser.rs:651
```
By the token's own documented invariant — post-space is a *trailing sub-range of `span`* (`token.rs:120-123`), enforced at construction (`reader.rs:338`, `reader.rs:375`: the span's `end` **is** `post_space.end`), and for post-space-less kinds `post_space()` returns `Span::empty(self.span.end)` (`token.rs:154`) — the identity

  `span.end - post_space().len()  ==  post_space().end - (post_space().end - post_space().start)  ==  post_space().start`

holds for **every** kind. So all three lines are exactly `tok.post_space().start`. That form cannot underflow, needs no new `Span` API, and degrades gracefully (to a garbage-but-in-range position) rather than panicking on a hand-built `TokenListReader` token whose post-space is malformed — which is precisely the residual risk flagged in the `list_reader.rs` review (item 4) and the `nodes_parser.rs` review (item 1). This is the highest-value action arising from this file's review even though the fix is not *in* this file. (The *other* subtraction, `post_space.start - content.len()` at `nodes_parser.rs:594` / `argument_parsers.rs:125`, is genuinely irreducible: `TokenKind::Comment` carries the content *string* but no content *span*. That one still needs the already-recommended token-level fix — carry the content span — and no `Span` helper rescues it.)

**2. `pub` fields make the `new()` invariant advisory, and it is bypassed today (design / correctness-adjacent) — `span.rs:14-17, 22-25`.**
`Span::new` debug-asserts `start <= end` (line 23), but `start`/`end` are public, so the assert guards one of several construction paths. Four lib-code sites mutate the fields in place, skipping it entirely:
- `nodes_parser.rs:280` `run.end = pre_space.end;`
- `nodes_parser.rs:297` `run.end = token.span.end;`
- `nodes_parser.rs:1956` `content.end = token.span.end;`
- `argument_parsers.rs:659` `span.end = token.span.end;`

All four extend `end` forward (monotone), so none can currently invert a span — but the debug-asserted contract is not what actually holds the line; the parsers' own `debug_assert!(run.end == pre_space.start, …)` contiguity checks (`nodes_parser.rs:274-279, 291-296`) are. This is also an inconsistency inside `source`: `SourceSpan` has **private** fields plus `start()`/`end()`/`range()` accessors and a validating `new()` (`source.rs:188-217`), while its `Copy` sibling is a bag of two public `usize`s. Two coherent positions — (i) accept that `Span` is a transparent value type and demote `new()`'s assert to documentation, or (ii) privatize the fields, add `start()`/`end()`, and let mutation go through an explicit `extend_to(end)` / `with_end(end)` — but the current middle ground gives the *appearance* of an enforced invariant without the substance. Worth a conscious call (user decision; do not change unilaterally).

**3. `len()` wraps silently on an inverted span in release; `is_empty()` disagrees with it (robustness) — `span.rs:33-40`.**
```rust
pub fn len(&self) -> usize { self.end - self.start }
pub fn is_empty(&self) -> bool { self.start == self.end }
```
For `start > end` (reachable via pub fields, or via the `nodes_parser.rs:594` underflow in release), `len()` wraps to a near-`usize::MAX` value while `is_empty()` returns `false` — and the corruption then surfaces far away as an opaque `&content[range]` slice panic ("slice index starts at N but ends at M") inside `slice()`/`TextContent::resolve`, or as a `SourceSpan::new` debug-assert in a completely different module. `self.end.saturating_sub(self.start)` (and `self.start >= self.end` for `is_empty`) costs nothing measurable and turns a wrap into a benign zero. This is a one-line hardening consistent with CLAUDE.md's no-panic rule; whether it is *desirable* to paper over a broken invariant instead of failing loudly is a judgment call — the alternative is a `debug_assert!(self.start <= self.end)` in `len()` itself, which at least names the violation at the point where it matters.

**4. `Span` and `SourceSpan` do not interoperate — the bridge is an untyped `Range<usize>` (API completeness) — `span.rs:42-45`, `source.rs:201, 240`.**
`SourceSpan::new` takes a `Range<usize>`; `Span` offers `range()`; so the crate is littered with `SourceSpan::new(&cx.source, span.range())` — **21 occurrences** (of 31 `SourceSpan::new(&cx.source, …)` calls total; the other 10 build a range by hand from two spans, see finding 5). In the other direction there is nothing at all: `SourceSpan` exposes `start()`/`end()`/`range()` but no `span() -> Span`, so `node/invariants.rs` — the checker for the very invariants this type underpins — works entirely in `Range<usize>` and re-derives everything (`invariants.rs:99`, `106`, `204`, `213`, `221`, `252`). The `Range<usize>` round-trip is lossless, so this is not a bug; it is a missing seam. Two cheap additions would remove most of it: `SourceSpan::span() -> Span` and either `Span::in_source(&Arc<Source<O>>) -> SourceSpan<O>` or `SourceSpan::new_spanned(&source, span)`. Naming: `in_source` reads correctly and carries no competing sibling vocabulary (NAMING_STRATEGY principles 3-4); flagging it as a design question rather than a recommendation because it puts a `SourceSpan`-returning method on the deliberately-`Arc`-free type (the module's cycle-prevention invariant is unaffected — both live in `source` — but the "`Span` is the cheap one" mental model is worth preserving).

**5. Missing operations that consumers hand-roll: `cover`/`merge`, `contains`, `get` (API completeness) — `span.rs:20-56`.**
`Span` has *no* relational or combining operators. Each is hand-rolled where needed:
- **cover / merge** (a span spanning two others): `Span::new(first.start, last.end)` at `nodes_parser.rs:999-1002`; `SourceSpan::new(&cx.source, open_span.start..close_span.end)` at `nodes_parser.rs:1971`; `…, trigger.span.start..body.end)` at `environment_parser.rs:605`; `…, end_token.span.start..name_group.end)` at `environment_parser.rs:260`; the "span-end from the last staged child" idiom at `invocation_parser.rs:149-155`. A `Span::cover(self, other) -> Span` (or `to(self, other)`) would DRY five sites and give the "node span = open .. close" idiom one name.
- **contains** (span-in-span): `span.start <= child_span.start && child_span.end <= span.end` at `invariants.rs:252`; the group-interior arithmetic at `invariants.rs:194-221`.
- **non-panicking slice**: `invariants.rs:106` hand-rolls `source_content.get(s.range()).is_some()` — a `Span::get(content) -> Option<&str>` is the natural companion to the panicking `slice()` and is the one addition that directly serves CLAUDE.md's no-panic rule (`slice()` is reachable from public node accessors via `TextContent::resolve`, and a transform that re-sources a node without materializing its content will hit it).
- **adjacency** (`a.end == b.start`): `nodes_parser.rs:275, 292`; `list_reader.rs:51`. Marginal; a named `abuts` is probably over-engineering.

**Important design note if these are added:** the empty-span semantics of `contains`/`overlaps` must be *decided*, not defaulted. Spans are half-open, and the crate is full of legitimately empty spans (`Span::empty(pos)` for absent pre/post-space, `EndOfStream`, unclosed-group close delimiters). Does `Span::new(2,5)` contain `Span::empty(5)`? Does `Span::empty(3)` overlap `Span::new(3,7)`? Both answers are defensible and both will be depended on silently once shipped. Pin them in the doc comment and in tests at the moment of writing, and record the call in DESIGN_RATIONALE §3.1.

**6. `Copy`/`Eq`/`Hash`/`Debug`/`From<Range>` are all right; `Ord` is absent and should stay absent (trait impls — confirmation) — `span.rs:12, 58-68`.**
- `Clone, Copy, PartialEq, Eq, Hash` derived (line 12) — correct and consistent with the "`Copy`, no `Arc`" mandate (ARCHITECTURE.md §source, NAMING_STRATEGY "Byte range | `Span` (in `source`)").
- `From<Range<usize>>` routes through `new()` (line 60), so it inherits the debug-assert. Good. The inverse `impl From<Span> for Range<usize>` does **not** exist; `range()` covers the need and the type is `Copy`, so this is a cosmetic gap at most (it would allow `SourceSpan::new(&src, span.into())`, marginally worse than the `in_source` helper of finding 4). An `impl Index<Span> for str` is orphan-rule-legal and would allow `&content[span]`, but is exactly the kind of cleverness CLAUDE.md's "keep it simple" rule warns against.
- Hand-written `Debug` printing `3..7` (lines 64-68) — right call: it keeps `Token`'s `Debug` (`token.rs:251-256`) readable. It correctly does not name the type, and `{:#?}` alternate formatting is irrelevant for a two-integer value.
- **No `PartialOrd`/`Ord`.** Nothing in the crate needs it (`list_reader.rs:70` binary-searches on `t.span.start`, an integer). A derive would give lexicographic `(start, end)` order, which happens to be the correct source order — so if a future diagnostic sorter wants it, the derive is safe. Do not add it speculatively.

**7. Char-boundary safety is entirely a downstream concern, and the doc is slightly misleading about it (documentation) — `span.rs:10-11, 47-55`.**
The struct doc says offsets "must fall on `char` boundaries" (line 11), but `Span` never validates this and *cannot* (it has no content). Actual enforcement is: `SourceSpan::new`'s debug-asserts (`source.rs:209-215`) on the node path; `StdTokenReader::move_to_pos`'s debug-assert (`reader.rs:143`) on the reader path; and the raw panic inside `slice()` on the resolve path. The tokenizer itself only ever advances by `c.len_utf8()` (`reader.rs:205, 214, 313, 320`) or by a matched `&str`'s `len()`, so parser-produced spans are boundary-correct by construction. Nothing is wrong here — but the sentence reads as a claim about the type when it is a contract *on the caller*, and it names no enforcer. One clause ("…and must fall on `char` boundaries — a caller contract, checked where the span meets its content: `slice()` panics, `SourceSpan::new` debug-asserts") would make it accurate. Same for `new()`, whose doc (line 21) does not mention the `start <= end` precondition it debug-asserts.

**8. No `#[inline]` on the accessors (efficiency, low) — `span.rs:22-55`.**
`new`, `empty`, `len`, `is_empty`, `range`, `slice` are non-generic, sub-statement-sized, and called once or more per token on the hot path. Within the crate LLVM inlines them freely, so this costs nothing today; but `Span` is a crate-root public type (`lib.rs:96`) and non-generic non-`#[inline]` functions are **not** inlinable by downstream crates without LTO. `#[inline]` on the six is free and idiomatic for a `Copy` newtype-ish value. Genuinely low priority — flagging only because "faster than Python" is an explicit design goal and this is the one file where the cost is per-token.

**9. Test coverage gaps — the contract is almost entirely untested (test-coverage) — `span.rs:70-98`.**
Three tests (`span_basics`, `span_empty`, `span_from_range`) cover the happy path for `len`/`is_empty`/`range`/`slice`/`Debug`/`From<Range>`. Everything that constitutes the type's *contract* is untested:
- **No multi-byte / UTF-8 test at all.** Every `slice` test uses ASCII (`"abcdefghij"`). The one multi-byte exercise of `Span::slice` in the crate is incidental (`reader.rs:554`, `token.span.slice(text) == "é"`). Add: `Span::new(0, 2).slice("éx") == "é"` and, as the documented negative, `#[should_panic] Span::new(0, 1).slice("é")` (a non-char-boundary slice) — this pins the panic contract documented at lines 49-52, which nothing currently asserts.
- **No inverted-span test.** `#[cfg(debug_assertions)] #[should_panic(expected = "span start")] fn new_rejects_start_after_end() { Span::new(7, 3); }` would pin the only guard the type has (line 23).
- **No out-of-bounds `slice` test** (the other half of the documented panic), and no `Span::empty(content.len()).slice(content) == ""` test — the boundary case that *must* work (`EndOfStream` spans sit exactly there, `reader.rs:168`).
- **No `Copy`/`Hash`/`Eq` exercise.** `Hash` is derived and currently unused anywhere in the crate; either use it or note why it's derived.

**Checked and found correct:** the half-open convention is applied consistently (`len == end - start`, `empty(p) == p..p`, `range()` yields `start..end`); `empty()` trivially satisfies `start <= end` so its bypass of `new()` is harmless; `From<Range>` correctly routes through `new()`; the `Debug` format is the right one for embedding in `Token`/`TextContent` debug output; the module doc's division of labor ("`Span` = transient/`Copy`; `SourceSpan` = outlives the parse") matches reality — `TokenError` carries `Span` (`token/error.rs:80`), `Diagnostic` carries `SourceSpan` (`error.rs`), exactly as DESIGN_RATIONALE §3.8 specifies; naming (`Span`, `empty`, `range`, `slice`) conforms to NAMING_STRATEGY (§"Byte range | `Span` (in `source`) | `Copy`, no `Arc`; transient parsing use").

---

**Net (priority order):**

1. **Delete the three `span.end - post_space().len()` subtractions** (`reader.rs:390`, `list_reader.rs:101`, `environment_parser.rs:651`) in favor of `tok.post_space().start` — algebraically identical for every token kind, underflow-free, no new API. This closes the robustness item raised in two previous reviews without adding a helper. (The comment-content subtraction at `nodes_parser.rs:594` / `argument_parsers.rs:125` still needs the separate token-level fix: give `TokenKind::Comment` a content *span*.)
2. **Add the missing tests** (finding 9): multi-byte `slice`, non-char-boundary panic, out-of-bounds panic, `empty(len)` at end of content, inverted-span debug-assert. Five short tests; they turn the entire prose contract into an executable one, and this is the file where that matters most.
3. **Decide the `pub`-fields question** (finding 2) — privatize + accessors + an explicit `extend_to`, or explicitly demote `new()`'s assert to a convenience check and document that `Span` is a transparent value type. Either is fine; the current middle ground is not.
4. **Harden `len()`** with `saturating_sub` (or a `debug_assert`), and fix the two doc sentences (findings 3 and 7) — 15 minutes, no behavior change on well-formed input.
5. **Optional, user's call:** `Span::get(content) -> Option<&str>` (used immediately by `invariants.rs:106`, and the no-panic-rule-conforming companion to `slice`); `Span::cover(other)` (DRYs five sites); a `Span` ⟷ `SourceSpan` bridge (DRYs 21 sites). If `contains`/`overlaps` are ever added, pin the empty-span semantics in the doc *and* in tests at the same commit.

## Source file `src/error.rs`

(Phase 1 — `Severity`/`Diagnostic`/`Diagnostics`, the `Recovery` policy, `ParseError`/`ParseErrorKind`, and the `format_position`/`format_traceback` rendering helpers. Reviewed by an Opus agent; **no correctness bugs and no panic paths found**. `cargo test`: 280/280 pass, of which 11 are `error::tests`. The file is clean, well-documented, genuinely zero-dependency and `no_std`-correct. The substantive findings are all about *what the types can carry*: the error/diagnostic model is a `String` model, and the tolerant path is where that bites.)

**Summary:** The mechanics are right — Arc-based spans (self-contained errors, no `'src` infection, per §3.1), hand-written `Display`/`core::error::Error` (Decision 5), lazy `LineIndex` rendering with a graceful `None` fallback, provenance-chain walking. The weaknesses are structural: (a) `Diagnostic` carries only a `String`, so the tolerant path throws away every bit of structure the strict path keeps (`engine/mod.rs:142` — confirmed); (b) `ParseErrorKind::Syntax { message: String }` is itself stringly, so there is very little structure to throw away in the first place — 13 of the 15 `ParseErrorKind` construction sites in the crate are `Syntax { message: format!(…) }`; (c) errors carry exactly one span, so the two-location conditions (unclosed group, environment terminator mismatch) can't point at both ends, and `format_traceback` — written precisely to render that context — has **no producer anywhere in the crate**. Plus one real efficiency defect in the rendering path.

---

**1. The error model is stringly where the design doc says it should not be (API design) — `error.rs:214-224`.**
`ParseErrorKind` has exactly two variants: `Token(TokenErrorKind)` (structured) and `Syntax { message: String }` (a formatted sentence). Every parse-level condition in the crate goes through `Syntax`:

- `group_parser.rs:123` unclosed-at-EOF (carries: expected close delimiter)
- `group_parser.rs:137` mismatched/stray close (carries: expected close delimiter)
- `environment_parser.rs:253` terminator name mismatch (carries: expected name, found name)
- `environment_parser.rs:272` malformed terminator (carries: environment name)
- `environment_parser.rs:323, 338, 512, 539, 661` missing-terminator variants
- `argument_parsers.rs:235, 276, 361, 452` missing/malformed argument
- `nodes_parser.rs:430` unresolvable command

Each one `format!`s the very fields a consumer would want (`"missing terminator of environment ‘{}’: found the terminator of ‘{}’ instead"`, `environment_parser.rs:255-258`) into prose and then discards them. This is the same pattern DESIGN_RATIONALE §3.8 explicitly congratulates itself for *eliminating* at the token level: "structured `TokenErrorKind` (closed enum: end-of-stream-after-escape, forbidden-char — **replaces pylatexenc's stringly `error_type_info`**)". The parse level reintroduced it. The `#[non_exhaustive]` attribute and the doc comment ("parse-level conditions are added as the construct parsers grow, Phase 6.2–6.6") say the intent was to grow real variants — but 6.2–6.6 have landed and none were added; `Syntax` absorbed them all.

Consequence for the eventual consumers named in the goals (FLM, linters, editors/LSP): no error codes, no programmatic filtering, no "which environment?" without re-parsing the message. Localization is impossible. And the messages are constructed eagerly at the detection site even in strict mode, where nobody may ever read them.

Recommendation (this is a design decision for the user, not a mechanical fix): promote the recurring conditions to variants while `#[non_exhaustive]` still makes it cheap — e.g. `UnclosedGroup { expected_close: String }`, `TerminatorMismatch { expected: String, found: String }`, `MissingArgument { … }`, `UnresolvableCommand { name: String }` — and keep `Syntax` as the escape hatch for third-party construct parsers. The `Display` impl then owns the wording (one place, not 13), which is also where localization would hook in.

**2. The tolerant path is lossy because `Diagnostic` holds only a `String` — confirms the existing report's `engine/mod.rs:142` note, and locates the cause here (API design) — `error.rs:44-49`.**
`Diagnostic { severity, message: String, span }`. Strict mode returns `ParseError::new(kind, span)` with the kind intact; tolerant mode does `Diagnostic::error(kind.to_string(), span)` (`engine/mod.rs:142`). Verified: **that line is the only diagnostic producer in the entire library** (`grep 'diagnostics.push' src/` → one non-test hit). So *every* diagnostic a real parse produces is a severity + a sentence + a span, and even the one genuinely structured kind — `Token(TokenErrorKind)`, which is `Copy` and would cost nothing to store — is flattened.

The existing report calls this "not a bug today, consistent with the current `Diagnostic` type." Evaluating the error type's role in it, as asked: the loss is *caused* here, not in the engine. `ParseError` and `Diagnostic` are the same information under two policies (this is literally admitted by `ParseError::render`, `error.rs:268`, which rebuilds a `Diagnostic` from itself), yet only one of them is allowed to be structured. The minimal fix is an optional kind on `Diagnostic`:

```rust
pub struct Diagnostic<O: SourceOrigin = Option<String>> {
    severity: Severity,
    kind: Option<ParseErrorKind>,   // present for parser-reported conditions
    message: String,
    span: SourceSpan<O>,
}
```
with `Diagnostic::from_error_kind(kind, span)` doing the `to_string()` once. `engine::recover` then loses nothing, and `Diagnostics` gains a `filter_by_kind`. Combined with finding 1, this is the difference between "diagnostics you can print" and "diagnostics a tool can act on".

**3. One span per error; no related locations — and `format_traceback` has no producer (span/location plumbing) — `error.rs:311`, `error.rs:244-247`.**
`ParseError` and `Diagnostic` each carry exactly one `SourceSpan`. But several conditions are inherently two-location, and the code visibly struggles with the choice:
- `group_parser.rs:129` (unclosed at EOF) points at the **open** delimiter — so the message says "before end of input" while the caret is on `{`, nowhere near the end of input.
- `group_parser.rs:139` (stray close) points at the **close** — so the open brace, the thing the user actually needs to find, is not reported at all.
- `environment_parser.rs:329` points at the `\begin` trigger; `environment_parser.rs:343` points at the stray group close. Same split.

The idiom for this is a secondary/related span list (`rustc`'s "unclosed delimiter here", LSP's `relatedInformation`). ARCHITECTURE.md:782-784 anticipates exactly this — "open-blocks traceback — the existing `format_traceback` work slots in here" — and `format_traceback` (`error.rs:311`) is faithfully ported and tested. But **nothing produces its `&[(SourceSpan, String)]` argument**: `grep format_traceback src tests examples` → only `lib.rs:91` (the re-export) and this file's own tests. Nothing in the engine tracks open blocks, and neither `ParseError` nor `Diagnostic` has a field to hold them, so no rendered message can ever include a traceback. The helper is a rendering function for a data structure the error types don't have.

Recommendation: add `related: Vec<(SourceSpan<O>, String)>` (or a small `Note` struct) to `Diagnostic`/`ParseError`, have `render()` append `format_traceback(&self.related)`, and have the two-location sites populate it (the group parser already holds `self.open_span`; the environment parser already holds `self.trigger_span`). That wires the ported helper to a producer and fixes the "caret in the wrong place" cases in one move. This is the highest-value structural change in the file.

**4. `format_position` rebuilds a `LineIndex` on every call — rendering k diagnostics is O(k·N) (efficiency) — `error.rs:284-291`.**
```rust
let source = span.source();
let mut line_index = source.line_index();      // <-- fresh index, every call
let pos_str = match line_index.line_col(span.start()) { … };
```
`Source::line_index()` (`source/source.rs:137-140`) *constructs* a `LineIndex` (`line_starts: vec![0]`, `computed_end: 0`); `Source` caches nothing. `LineIndex::line_col` then lazily scans `content[0..byte_offset]` with `char_indices()` (`line_index.rs:97-110`). The laziness only pays off if the index is *kept*, and `format_position` drops it at the end of the expression.

Errors are cold, so per-error cost is fine — but the natural consumer of `Diagnostics` is "render all of them", and that is not cold: a tolerant lint of a 100 kB document with 500 diagnostics rescans an average of 50 kB per diagnostic ≈ 25 M char steps, and it is quadratic in document size. `format_traceback` (`error.rs:319`) has the same shape: one fresh index per open block, all over the same source.

Fix, in keeping with "no premature optimization" but not paying O(k·N) for the crate's own main rendering path: a renderer that owns one `LineIndex` per distinct source. E.g. `Diagnostics::render_all(&self) -> String` (or a small `DiagnosticRenderer` holding a `Vec<(Arc<Source>, LineIndex)>` matched by `Arc::ptr_eq`, mirroring the session's group-interior memo idiom), with `format_position` kept as the one-shot convenience. Note there is currently *no* API to render a `Diagnostics` collection at all — callers must loop — so the renderer would fill a real API gap and fix the complexity at the same time.

**5. The provenance walk duplicates `Source::provenance_chain()` (maintainability) — `error.rs:95-116` vs `source/source.rs:150, 326-340`.**
`Diagnostic::render` hand-rolls a `loop { match provenance { Primary => break, Resolved{..} => { …; provenance = triggered_at.source().provenance(); } … } }`. `Source::provenance_chain()` is exactly that traversal, already implemented as an `Iterator` (`ProvenanceChain`, `source.rs:326-340`) and separately tested (`source.rs:459`). `render` should be `for p in self.span.source().provenance_chain() { match p { Primary => {}, Resolved{..} => …, Synthesized{..} => … } }`. Two independent copies of the chain walk is the classic drift setup — and if the provenance enum grows a variant, only one of them is compiler-forced to handle it correctly (the hand-rolled loop *is* exhaustively matched today, so this is a duplication finding, not a bug).

**6. `Diagnostics` accumulation is unbounded, with no cap and no dedup (robustness) — `error.rs:130-143`.**
`Diagnostics` is a bare `Vec` with `push`. In tolerant mode every recoverable condition appends one `Diagnostic` — each ~48–56 bytes plus a heap `String` message (~40+ bytes) plus an `Arc` bump. Adversarial or merely degenerate input (a file of forbidden chars → one `ForbiddenChar` diagnostic per character, `nodes_parser.rs:499`) produces a diagnostic per input byte. A 10 MB input can therefore allocate on the order of a gigabyte of identical messages, in the exact mode (tolerant) that editors and linters — the stated consumers — are expected to use. Strict mode is immune (first error aborts).

Nothing here panics or is incorrect, and I would not gold-plate this; but a cheap `Recovery`-adjacent policy (e.g. `Diagnostics::with_limit(n)` where `push` beyond `n` increments a `suppressed: usize` counter, surfaced as "… and N more") is a few lines and turns an unbounded-memory failure mode into a bounded one. Worth a conscious decision rather than an accident.

**7. std-trait hygiene: a small set of gaps (API design / maintainability).**
- **`core::error::Error::source()` is not overridden** (`error.rs:278`). Correct today — `ParseErrorKind::Syntax` has no cause, and `TokenErrorKind` does not implement `Error` (only the `'s`/`L`-generic `TokenError` does, `token/error.rs:167`, which could never be a `&(dyn Error + 'static)` anyway). But `TokenErrorKind` is `Copy + 'static + Display`; making it implement `core::error::Error` and returning `Some(kind)` from `ParseError::source()` for the `Token` arm would give the crate a real source chain for free. Low priority, but it is the one std convention the file leaves on the table.
- **The `TokenError` → `ParseError` lift is duplicated** at `constructs/mod.rs:89-91` and `nodes_parser.rs:499-500` (identical: build `ParseErrorKind::Token(error.kind())`, `SourceSpan::new(&cx.source, error.span().range())`). This byte-`Span` → `SourceSpan` lift is *the* boundary this module owns; it belongs here as `ParseError::from_token_error(&TokenError<L>, &Arc<Source<O>>)`. It cannot be a `From` impl (it needs the source), which is presumably why it never got written — but a named constructor in `error.rs` would DRY it and put the layering rule in one place.
- **`Diagnostic` and `ParseError` derive neither `PartialEq` nor `Eq`**, although `SourceSpan` implements both (`source.rs:266-272`) and `ParseErrorKind` derives both. Tests are consequently forced to compare field-by-field (`engine/mod.rs:255-256`, `error.rs:358-361`). Deriving them is free and makes `assert_eq!` on a whole diagnostic possible.
- **`Diagnostics` has no `Extend`/`FromIterator`** despite both `IntoIterator` impls. No merging need exists today (one session, one sink), so this is a note, not a request.
- **Consistency:** `ParseErrorKind` is `#[non_exhaustive]` "because it will grow" (`error.rs:213`), but `TokenErrorKind` — which its `Token` variant wraps, and whose own doc says it "grows as the tokenizer learns to detect more error conditions" (`token/error.rs:27-28`) — is **not** `#[non_exhaustive]`. Adding a token error kind is therefore a downstream-breaking change that the parse-level wrapper carefully avoided being. One of the two attributions is wrong; from this file's perspective, `TokenErrorKind` should carry the attribute too.

**8. Test-coverage gaps (specific).**
The 11 tests here are good on `Diagnostic::render` (line info, origin label, full provenance chain), on `format_traceback` (single/multiple/empty/with-origin), and on the `format_position` char-pos fallback. The gaps are concentrated on `ParseError` and on `Severity`:
- **`ParseError` has zero tests in this file.** Its `render()`, `span()`, `kind()`, and `Display` are exercised only incidentally by `engine/mod.rs:239-267`. In particular `ParseError::render()` (`error.rs:267-269`) — which forces `Severity::Error` and rebuilds a `Diagnostic` — is asserted only via `.contains("line 1")` (`engine/mod.rs:260`).
- **`ParseErrorKind::Token(..)`'s `Display` is never tested** (`error.rs:229`). Only the `Syntax` arm is (`engine/mod.rs:259`). The delegation to `TokenErrorKind`'s `Display` is untested at the `ParseErrorKind` level.
- **`Severity`'s `Display` impl (`error.rs:33-41`) and its `Ord` (`error.rs:23`) are both untested**, and neither is used anywhere in the crate: `Severity::Note` and `Severity::Warning` have **no producer at all** outside this file's own tests (verified by grep). If severity ordering is meant to support "show me warnings and above", that intent has no test and no consumer.
- **`Diagnostics`: `as_slice`, `Default`, and both `IntoIterator` impls are untested** (`error.rs:166-193`). `Diagnostic::note` is never constructed (`diagnostics_collection` uses `warning` + `error` only).
- **Custom line/column offsets are never exercised through `format_position`.** `Source::with_line_column_number_offsets` (`source.rs:100`) feeds `Source::line_index()` (`source.rs:137-139`), so a diagnostic on an embedded snippet renders shifted line numbers — the whole point of the feature — and no test in this file pins that the diagnostic path honors it.
- Nit: the fallback test's `DEFAULT_MAX_TEST_LEN = 60_000` (`error.rs:502`) is a *repeat count*, not a length, and it hardcodes the 100 000-byte threshold rather than referencing `source::line_index::DEFAULT_MAX_SCAN_LEN`. That constant is `pub` (`line_index.rs:40`) but the module is private and `source/mod.rs:59` re-exports only `LineIndex` — so it is **unreachable from outside the crate**, which also means an embedder cannot discover the threshold that `format_position`'s doc comment (`error.rs:281-282`) tells them about. Small export bug in `source/mod.rs`, surfaced by this file's doc.

**9. Confirmations (checked, correct — flagged so they are not re-raised).**
- **No panics in library code.** No indexing, no unwrap/expect, no arithmetic that can underflow; the only fallible operation (`LineIndex::line_col`) returns `Option` and is handled with a graceful fallback (`error.rs:288-291`). Fully compliant with CLAUDE.md rule 4.
- **The provenance walk cannot loop forever.** A `SourceProvenance` cycle would need a `Source` whose `triggered_at` points (transitively) at itself; `Source::resolved`/`synthesized` require an already-constructed `Arc<Source>`, and no interior mutability or `Arc::new_cyclic` is exposed — the acyclicity is structural (§3.11 rule 3, `source/mod.rs` "Cycle-prevention invariant"). The unbounded `loop` at `error.rs:97` is safe.
- **Zero-dependency / `no_std` discipline holds.** `alloc::{format, string, vec}` + `core::fmt` only; `core::error::Error` (MSRV 1.81, recorded in `Cargo.toml`); `Cargo.toml` has an empty `[dependencies]`. Hand-written `Display` impls throughout — exactly what Decision 5 / §3.9 mandates, with no `thiserror` residue.
- **Arc-span self-containment holds.** Both `Diagnostic` and `ParseError` carry `SourceSpan<O>`, no `'src` lifetime, both `Clone` — errors and diagnostics outlive the parse, per §3.8's "Errors carry Arc-based `SourceSpan`" decision.
- **`Recovery`'s documented semantics match the implementation.** The doc (`error.rs:197-199`) says tolerant mode "continues with the error's recovery token **where one is available**"; `nodes_parser.rs:499-505` does exactly that, and returns `Err(ParseError)` even in tolerant mode when `into_recovery()` is `None`. `constructs/mod.rs:81-95`'s `try_peek` deliberately *doesn't* diagnose in tolerant mode to avoid a double report — also consistent.
- **Naming conforms.** `Diagnostic`/`Diagnostics`/`Severity`/`Recovery` are the names pinned in NAMING_STRATEGY.md:106; `ParseErrorKind` follows the `…Kind` = closed-core-enum rule (principle 5). The `Recovery` (policy) / `TokenRecovery` (payload) pair reads as a near-collision, but the table entry explicitly defines `Recovery` = "tolerant-parsing policy (strict/tolerant)", so it is a decided name — not re-litigating.
- **Module doc accuracy** is high; two stale/imprecise spots only: `error.rs:198-199` says the recovery-token mechanism "arrives with the token layer, Phase 2" (it has arrived — Phases 2–3 are done per `lib.rs:27-28`), and the `@ char pos 42` fallback (`error.rs:290`) gives the user no hint *why* line info is missing (the source exceeded the scan limit) — the very information §3.9 said should reach the API rather than a `warn!`. A parenthetical in the rendered string would close that loop.

---

**Net (priority order):**
1. **#3 — give errors/diagnostics related spans and wire `format_traceback` to a producer.** It is the biggest user-visible gap (two-location conditions currently render a caret in the wrong place), the helper is already written and tested, and the producing sites already hold the spans they'd pass.
2. **#1 + #2 together — decide the structure question.** Promote the recurring `Syntax` conditions to real `ParseErrorKind` variants (cheap now, `#[non_exhaustive]` is already in place), and let `Diagnostic` carry the kind so the tolerant path stops being lossy. These two are one decision, not two; needs the user's sign-off per CLAUDE.md rule 1.
3. **#4 — a `Diagnostics`-level renderer that reuses one `LineIndex` per source.** Fixes an O(k·N) hot spot in the crate's only rendering path and fills an actual API gap (there is no way to render a collection today).
4. **#8 — the test gaps**, especially: `ParseError` has no test in its own file, `ParseErrorKind::Token`'s `Display` is untested, and custom line/column offsets are never exercised through `format_position`.
5. **#5, #6, #7 — cheap hardening:** reuse `Source::provenance_chain()`; consider a diagnostics cap; add `ParseError::from_token_error`, `PartialEq` derives, `#[non_exhaustive]` on `TokenErrorKind`.

## Source file `src/node/node_ref.rs`

(Phase 5 — the `Copy` read proxy over the flat `NodeTree` arena: id resolution, kind predicates, kind-specific accessors, and the region-based argument/slot read API. Reviewed against `tree.rs`, `kind.rs`, `arguments.rs`, `builder.rs`, `invariants.rs` and all real consumers in `src/constructs/`. Verdict: **structurally sound, no traversal or off-by-one bugs**; the defects are two silent-wrong-answer paths and a set of gaps that will bite Phase 7. `cargo test`: 280 passed, 0 failed.)

**Summary:** The core design holds up. `NodeRef` is 12 bytes (`&'t NodeTree` + `NodeId`), `Copy` is correct, accessors return `'t`-borrowed data (not `&self`-borrowed), traversal allocates nothing, and the region encoding is read back faithfully — `argument_nodes`/`argument_content_nodes`/`slot_content_nodes` all map directly onto builder-resolved global ranges with no re-derivation, so there is no place for an off-by-one to hide. What is wrong or missing: (1) `child(i)` truncates its index and can return the *wrong child* instead of `None`; (2) a `NodeId` from tree A used against tree B silently resolves to a different node — and the doc claims it panics; (3) `slot()`/`body()` can return the callable itself for a legal region shape, which would make a naive recursive walker loop; (4) as a foundation for Phase 7 the surface is missing the three primitives every post-processing pass needs — `tree()`, `parent()`, and a document-order walk (`tree.iter()` is **breadth-first**, not document order — a real trap).

---

**1. `child(i)` truncates the index: returns the wrong child instead of `None` (correctness) — `node_ref.rs:85-89`.**
```rust
pub fn child(&self, i: usize) -> Option<NodeRef<'t, L>> {
    let children = &self.data().children;
    let id = children.start.checked_add(i as u32)?;
    (id < children.end).then(|| NodeRef::new(self.tree, NodeId(id)))
}
```
The `checked_add` guards `u32` overflow, but `i as u32` has already silently truncated. On 64-bit, `node.child(1usize << 32)` computes `i as u32 == 0` and returns **child 0**; `child(usize::MAX)` yields `0xFFFF_FFFF`, which `checked_add` then catches — so the failure is index-dependent, not uniform. It is only reachable with an absurd index, but it is a silent wrong answer in an `Option`-returning accessor whose entire contract is "out of range ⇒ `None`", and the fix is one line: `let id = children.start.checked_add(u32::try_from(i).ok()?)?;`. `child_count()` (`node_ref.rs:80`) returns `usize`, so the `usize`-in/`u32`-inside mismatch is inherent to the signature and worth closing at the boundary.

**2. A `NodeId` from another tree silently resolves to a different node — and the documented panic does not fire (correctness / documentation) — `tree.rs:70-79`, `tree.rs:91-107`, `node_ref.rs:15-17`.**
```rust
/// Panics if `id` does not belong to this tree (out of range) — ids are only
/// meaningful for the tree that minted them.
pub fn node(&self, id: NodeId) -> NodeRef<'_, L> {
    assert!(id.index() < self.nodes.len(), "node id {:?} out of range", id);
```
The assert tests *range*, not *provenance*. `tree_b.node(id_from_tree_a)` where `id_from_tree_a.index() < tree_b.node_count()` returns a perfectly valid `NodeRef` to the wrong node, no panic. Same hole in `nodes_in` (`tree.rs:100-105`) — a `Range<u32>` lifted out of tree A's `ChildRegion` and applied to tree B silently yields foreign nodes. This is not hypothetical for Phase 7: transforms will hold *two* trees at once (source + rebuilt), and `NodeId`/`ChildRegion` ranges are `Copy`/`Clone`, public, and carry no lifetime — they are precisely the escape hatch that the module's "indices made safe by construction" claim (`node_ref.rs:15-17`, DESIGN_RATIONALE.md §3.5) does **not** cover. `NodeId(pub(crate) u32)` (`tree.rs:19`) at least prevents users from fabricating ids out of thin air, so the exposure is exactly "two trees in scope".
Two orthogonal fixes, both cheap: (a) correct the doc — it panics *only* on out-of-range, and an in-range foreign id is UB-of-the-logical-kind; (b) if you want it caught, give `NodeTree` a `tree_tag: u32/u64` (a monotonic counter or the `Vec`'s data pointer) stamped into `NodeId`/`ChildRegion` and `debug_assert`ed in `node()`/`nodes_in()`. That keeps release-mode cost at zero while making the "ids are tree-local" rule mechanically enforced in tests.

**3. `slot()` / `body()` can return the callable itself (correctness hazard / documentation) — `node_ref.rs:244-260`.**
```rust
pub fn slot(&self, i: usize) -> Option<NodeRef<'t, L>> {
    let region = &self.callable()?.slots.get(i)?.region;
    Some(NodeRef::new(self.tree, region.content_parent()))
}
```
`ChildRegion::content_parent()` is the callable's own index whenever the region's content was designated `ContentNodes::InRegion` (`builder.rs:307-310`: `ContentNodes::InRegion(r) => (…, callable)`), and `InRegion` is a legal, public, `ParsedSlot`-constructible shape (`ChildRegion::single()` at `arguments.rs:125` produces exactly it). Today both built-in environment paths use `InChildrenOf(body_list, …)` (`environment_parser.rs:586`, `:691`), so the body is always a `List` and the doc is true *for them* — but §3.5's own "the records are self-describing; a custom parser may produce a structure the spec didn't declare" (echoed at `environment_parser.rs:688-689`) means a `SlotsParser` that puts a slot's nodes directly among the callable's children will make `env.body()` return `env`. A walker written as `fn walk(n) { for c in n.body()… }` then recurses forever. Recommend either documenting the invariant loudly ("a slot's content must be designated `InChildrenOf`; `body()` is meaningless otherwise"), or making it structural: `slot()` returns `None` (or a distinct `SlotContent::InRegion(range)`) when `content_parent() == self.id()`. Note `argument_*` has no equivalent hazard because it never exposes the content parent (see finding 5).

**4. Missing `tree()` accessor — the read API dead-ends at `NodeId` (API design) — `node_ref.rs:25-28`, `node_ref.rs:213-220`.**
`NodeRef` holds `tree: &'t NodeTree<L>` but never exposes it. Consequence: `arguments()`/`slots()` hand back records whose payload is `NodeId` and `Range<u32>` (`ChildRegion::content_parent()`, `::children()`), and **from the `NodeRef` alone you cannot resolve them** — you must independently carry the `&NodeTree`. The real call sites already pay this tax:
```rust
// argument_parsers.rs:1179-1181
let region = item.arguments().unwrap().get(0).unwrap().region.as_ref().unwrap();
let parent = parsed.result.tree.node(region.content_parent());   // needs the tree, not the NodeRef
```
and identically at `node/mod.rs:305-307`, `:396-398`, `:640`. A `pub fn tree(&self) -> &'t NodeTree<L>` is one line, costs nothing (the field is already there), and makes every record self-resolvable. This is the single highest-value addition in the file.

**5. Argument/slot accessor asymmetry: no way to get an argument's group node (API design) — `node_ref.rs:222-260`.**
Slots get three accessors (`slot()` → content parent, `slot_content_nodes()`, `body()`); arguments get two (`argument_nodes()`, `argument_content_nodes()`) and **no** content-parent accessor — yet `content_parent()` is documented in `arguments.rs:158-161` as the answer to precisely the question consumers ask most ("give me the `Group` node of argument 0, so I can read its delimiters / know whether it was `{}` or `[]`", and "where would content go for `\m{}`"). Every consumer that wants it goes through the raw record (finding 4's snippet). Add `argument_content_parent(i) -> Option<NodeRef<'t, L>>` (mirroring `slot()`); while there, by-name access is also missing — `ParsedArguments::get_named()` exists (`arguments.rs:262`) but there is no `argument_nodes_named("title")` / `argument_content_nodes_named(…)`, so name-based access forces a `position()` scan by hand.

**6. No `parent()` — and the builder computes and then throws the parent table away (API design / efficiency) — `builder.rs:237-253`, `invariants.rs:54-79`, `tree.rs:44-50`.**
`NodeData` stores only `children: Range<u32>`. There is no upward navigation, and none is *derivable* in less than O(n) — `parent`, `next_sibling`, `prev_sibling`, "which argument am I part of", "am I inside a `\begin{align}`" are all unavailable. The evidence that this hurts is already in-tree: `check_tree_invariants` has to rebuild the whole parent vector itself (`invariants.rs:54-79`), and `finish()` **already builds exactly that vector** (`builder.rs:237`, `parent.push(pos as u32)`) and drops it on the floor. Storing `parent: u32` in `NodeData` costs 4 bytes/node (a ~7% growth on a struct that already carries a `SourceSpan` + `Arc` + `Range`), is free to compute, and unlocks `parent()`, `next_sibling()`, `prev_sibling()`, and `ancestors()`. ARCHITECTURE.md:601-608 explicitly defers the transform/visitor API, so this is the decision to *take* rather than a defect — but it is a storage-layout decision, and taking it after Phase 7 starts is a breaking change to `NodeData`. Worth deciding now.

**7. `tree.iter()` is breadth-first, not document order — an unflagged trap for the exact consumers Phase 7 targets (API design / documentation) — `tree.rs:86-89`, `builder.rs:243-254`.**
`finish()` lays the tree out breadth-first, so `NodeTree::iter()`'s "storage order" is BFS. For `a{b}c` the storage order is `root, "a", group, "c", "b"` — so the natural post-processing one-liner `tree.iter().filter(|n| n.is_chars())` yields **`a`, `c`, `b`**: scrambled text. The doc ("All nodes in storage order (root first; every node's children contiguous)") is technically accurate but does not warn, and there is no depth-first alternative anywhere in the module. A `descendants()` (pre-order, document order) on `NodeRef` — or at minimum an explicit "this is *not* document order; recurse via `children()`" note on `iter()` — is the missing primitive; every consumer in `src/constructs/` currently hand-rolls recursion or indexes children explicitly, which is why the trap has not fired yet.

**8. `NodeTree::node()` should have a non-panicking sibling (robustness) — `tree.rs:76-79`, `node_ref.rs:35-37`.**
CLAUDE.md forbids panics in lib code except unreachable paths. `NodeRef::data()` raw-indexes (`&self.tree.nodes[self.id.index()]`) and is unreachable-by-construction *only* because every public `NodeRef` constructor bounds-checks first (`root()`, `node()`, `nodes_in()`) — with one exception, `slot()` at `node_ref.rs:246`, which calls `NodeRef::new` directly with `region.content_parent()` and no check (safe today because `finish()` mints it, but it is the one unguarded path). I'd call the panic **sanctioned**: it matches the builder's documented panic-on-caller-bug policy and the `ChildRegion` resolved-only accessors (`arguments.rs:170-180`), and turning every accessor into `Option` would poison the whole read API. The proportionate fix is to add the standard arena escape hatch — `NodeTree::get(id) -> Option<NodeRef<'_, L>>` — for consumers (Phase 7 id-mapping, deserialization, tooling) that legitimately hold ids of unknown provenance, and keep `node()` as the assert-y convenience.

**9. Positive confirmations (verified, so they are not re-flagged).**
- **`Copy` is right and blocks nothing.** `NodeRef` is `(&'t NodeTree<L>, NodeId)`; the manual `Clone`/`Copy` impls (`node_ref.rs:263-271`) correctly avoid an `L: Clone` bound. Trees are immutable after `finish()` (which consumes the builder), so a shared borrow can never conflict with a mutation — there is no mutable path to conflict with. Holding a `NodeRef` costs nothing.
- **Accessor lifetimes are correct and deliberate.** `data()` returns `&'t NodeData<L>` (not `&'_`), so `chars()`, `kind()`, `span()`, `children()` etc. all return `'t`-borrowed data that outlives the transient proxy — `let it = node.children(); drop(node);` compiles. The `TextContent::resolve(self.source_content())` pattern (`node_ref.rs:127`, `:135`, `:144`, `:153`, `:178`, `:209`) unifies both borrows at `'t` correctly.
- **No allocation in traversal.** `children()` clones a `Range<u32>` and maps it (`node_ref.rs:92-95`); `nodes_in` maps a range (`tree.rs:106`). Nothing heap-allocates. `child()`/`child_count()` are O(1).
- **Region reads are faithful, and the callable-children partition is respected.** `argument_nodes` reads `region.children()` and `argument_content_nodes` reads `region.content_range()` verbatim — no arithmetic, no unwrap heuristic, so the "one region per provided argument, then one per slot" tiling (`builder.rs:135-203`, `invariants.rs:310-399`) cannot be mis-sliced here. The absent-vs-out-of-range conflation in `argument_nodes` (`None` for both) is documented at `node_ref.rs:224-225` and recoverable via `arguments()`. Correct.
- **Kind coverage is complete.** All five `NodeKind` variants have a predicate; every kind-specific accessor returns `None` on the wrong kind rather than panicking.
- **`Debug` does not recurse** (`node_ref.rs:273-282`): it prints `children` as the `Range<u32>`, not as nodes.

**10. Test coverage gaps (specific).** `node_ref.rs` has no `#[cfg(test)]` module of its own — its tests live in `node/mod.rs:237-534` (consistent with `tree.rs`/`kind.rs`/`builder.rs`; fine). Coverage of the happy paths is genuinely good (`tree_structure_and_accessors`, `argument_access`, `slots_and_body`, `argument_regions_hold_noise_nodes`, `content_designation_reaches_into_nested_groups`, `empty_content_is_anchored_by_its_parent`, plus heavy real-parser use in `constructs/`). Missing:
- **`child()` index overflow** — `child(1usize << 32)` / `child(usize::MAX)` (finding 1). `root.child(4).is_none()` (`mod.rs:274`) tests only the small-index boundary.
- **A slot whose region content is `InRegion`** — `slot(i)`/`body()` returning the callable itself (finding 3) is completely untested and currently unreachable from the built-in parsers; a hand-built tree in `node/mod.rs` would pin whatever behavior you decide on.
- **`None` paths of `group()` and `callable()`** — only `chars()`, `comment()`, `comment_start()`, `comment_post_space()`, `name()`, `arguments()`, `argument_nodes()` have wrong-kind assertions (`mod.rs:270-272`, `:316-319`). `body()`/`slot()` on a non-callable, and `body()` on a macro with no slots, are also untested (`slot(1).is_none()` at `mod.rs:442` only covers out-of-range on an environment).
- **Cross-tree id misuse** (finding 2) — not testable today because it fails *silently*; it becomes testable the moment a debug tree-tag exists, which is half the argument for adding one.
- **Whole-tree verbatim recomposition through the read API.** `node_ref.rs:59-64` advertises `span_content()` as "level-1 verbatim recomposition", but the only round-trip assertion is over one callable's direct children (`mod.rs:530-533`). A recursive leaf-walk asserting `concat(leaf.span_content() for leaf in preorder(root)) + callable post-spaces == source` — run over the parser-produced trees in `nodes_parser`/`environment_parser` — would be the single strongest test of this file's central claim, and it is exactly the traversal Phase 7 will need anyway (finding 7).
- **`NodeRef` `Copy`/`Debug`** are asserted only indirectly (`mod.rs:1104`); `StagedNodeView` has an explicit one (`mod.rs:1084`). Trivial, low value.

**Naming (per NAMING_STRATEGY.md).** No violations: no `Latex*` prefixes, `NodeRef`/`NodeId`/`NodeTree` keep the sanctioned simple forms, and the kind-specific accessors (`chars()`, `comment_post_space()`, `group_delimiters()`, `callable_type()`) read cleanly. One wobble: **`slot(i)` returns a *node*, while `slots()` returns the *records*** (`ParsedSlots`) — the singular/plural pair implies "one of the things the plural yields", which it isn't. `slot_content_parent(i)` (mirroring `ChildRegion::content_parent`) or `slot_node(i)` would be honest, and `body()` stays as the sugar. Same vocabulary would then apply to the argument accessor proposed in finding 5.

---

**Net (priority order):**
1. **Fix `child()`'s `i as u32` truncation** (`node_ref.rs:87`) — one line, silent-wrong-answer bug.
2. **Add `NodeRef::tree()`** (`node_ref.rs`) — one line, unblocks resolving every `NodeId`/range the records hand out; today's consumers all work around its absence.
3. **Decide the `slot()`-returns-the-callable question** (`node_ref.rs:244-260`) — either document the `InChildrenOf`-only invariant for slots, or make `slot()` return `None`/a distinct type on region-level content. Latent infinite-recursion footgun for the walkers Phase 7 will write.
4. **Correct `NodeTree::node()`/`nodes_in()` docs** (`tree.rs:72-75`, `:96-99`) — they do *not* panic on a foreign in-range id; and consider a debug-only tree tag on `NodeId`/`ChildRegion`, plus a non-panicking `NodeTree::get(id) -> Option<NodeRef>`.
5. **Decide `parent: u32` in `NodeData` now** (`tree.rs:44-50`) — `finish()` already computes the table (`builder.rs:237`) and discards it; adding it later breaks the storage layout. Unlocks `parent()`/siblings/`ancestors()`.
6. **Add `argument_content_parent(i)`** and by-name argument node access (finding 5), and a document-order `descendants()` — plus a warning on `NodeTree::iter()` that it is breadth-first (finding 7).
7. **Close the test gaps** in finding 10, starting with the whole-tree recomposition walk (highest value: it validates the file's central claim *and* prototypes the Phase 7 traversal).

## Source file `src/spec/mod.rs`

(Phase 4 + 6.4/6.5 — the `spec` module root: module doc, the re-export surface (`CallableSpec`, `StdCallableSpec`, `ArgumentParser`, `ArgumentSpec`, `ParsedArgumentNodes`, `SlotSpec`), and three structure tests. Reviewed together with its siblings `spec/callable.rs` and `spec/structure.rs` and all consumers. No correctness bugs; the code and its naming match the decided July 2026 design. `cargo test`: 280/280 pass, 3/3 in `spec::tests`; `cargo doc --no-deps` produces no warning in this module — every intra-doc link resolves.)

**Summary:** The extension surface itself is sound: `ArgumentParser` and `CallableSpec` are object-safe and used as `Arc<dyn …>` / `&dyn …` in real call sites; `Send + Sync` supertraits are in place; the parser-side/spec-side naming split holds exactly as NAMING_STRATEGY.md decided (and it is the *docs* that drifted, not the code); there is no per-invocation churn in the argument path beyond the one accepted `Box`. The substantive findings are: two stale doc paragraphs in `structure.rs` that describe an API Phase 6 explicitly *rejected*; an incomplete crate-root export surface (`ArgumentParser` is exported but its return type is not); an under-specified `parse_argument` contract whose violations are enforced by *panics* in the builder; and concrete test gaps around the module's own contracts (thread-safety, flyweight sharing, the factory default).

**1. `structure.rs` promises slot separator/terminator machinery that Phase 6 explicitly decided against (documentation accuracy) — `spec/structure.rs:14-16`, `spec/structure.rs:148-150`.**
The module header says the "slot separator/terminator machinery (including the invocation-name back-reference…) grows in Phase 6, when `ArgumentsParser`/`SlotsParser` make the requirements concrete", and `SlotSpec`'s doc repeats "Separators and terminators … arrive with `SlotsParser` (Phase 6)". Phase 6 decided the opposite: **slot terminators are parser business**, `SlotSpec` stays `{ name, parsing_state_delta }`, and the terminator data parameterizes the core `EnvironmentBodyParser` (Phase6Execution.md:53-55, decision 5; ARCHITECTURE.md:409-413). That is what shipped — `EnvironmentBodyParser::new(trigger_span, name, "end", GT_BRACE)` (`constructs/environment_parser.rs:568`), and `constructs/environment_parser.rs:8` even states the rule correctly. `ArgumentsParser`/`SlotsParser` never existed. These are the only lines in the spec module that advertise a *future* spec-side API that was rejected; a reader planning a custom slot-bearing callable would wait for machinery that is never coming. Highest-priority fix.

**2. `structure.rs:9-12` attributes the standard argument parsers to "the preset"; they shipped in the core (doc accuracy, internal contradiction) — `spec/structure.rs:9-12` vs `spec/structure.rs:63-68` and `spec/mod.rs:16-17`.**
The header says "the standard parsers (delimited group, optional group, literal marker, chars-only, comma-lists, verbatim, …) are provided by the preset as `ArgumentParser` implementations", but 30 lines later the same file correctly says "The standard implementations live in [`constructs`]" and links `GroupArgumentParser`/`OptionalGroupArgumentParser`/`MarkerArgumentParser`/`ExpressionParser` — which is where they are (`constructs/argument_parsers.rs:341`, `402`, and the re-exports at `constructs/mod.rs:42-45`). `spec/mod.rs:16-17` gets it right ("provided as core construct parsers, parameterized by group types and rules"). Per Phase6Execution.md:361-366 the core ships the parameterized parsers and the *preset one-liners* are Phase 7 — so the stale phrase is the header's. The same stale framing appears in `NAMING_STRATEGY.md:89` ("standard parsers are preset-provided") and in this file's own test comments (`spec/mod.rs:53-54`, `spec/mod.rs:86-87`: "the preset's marker / optional-group / group parsers").

**3. The crate root exports `ArgumentParser` but not `ParsedArgumentNodes` — the primary extension trait cannot be implemented from the root prelude (API design) — `lib.rs:104` (cf. `spec/mod.rs:31`), and `lib.rs:84-88` (cf. `constructs/mod.rs:42-45`).**
`pub use spec::{ArgumentParser, ArgumentSpec, CallableSpec, SlotSpec, StdCallableSpec};` omits `ParsedArgumentNodes`, which is the *return type* of `ArgumentParser::parse_argument`. `BuildId` and `ContentNodes` (its two fields) *are* root-exported (`lib.rs:100`), so the omission is clearly an oversight rather than a policy. Separately, the constructs root re-export (`lib.rs:84-88`) omits everything a custom spec needs to reuse: `StdInvocationParser`, `GroupArgumentParser`, `OptionalGroupArgumentParser`, `MarkerArgumentParser`, `ExpressionParser`, `scan_argument_noise`/`stage_pre_space`/`ArgumentNoise` (the very helper `structure.rs:75-76` tells implementors to use), `EnvironmentBodyParser`. All are reachable via `techy::constructs::…`/`techy::spec::…`, so this is ergonomic, not blocking — but this module *is* the extension surface, and the root re-export list is the advertised one.

**4. The `parse_argument` contract omits the obligations whose violation panics inside the builder (robustness / extension surface) — `spec/structure.rs:96-109` + `node/builder.rs:150-194`.**
The doc pins two obligations well (noise ownership; absent ⇒ nothing consumed, rewind). It does not state the obligations a third-party parser must also satisfy, each of which is enforced by a hard `assert!`/`panic!` in `NodeTreeBuilder::add`:
- `content` must be in range — `InRegion(r)` needs `r.end <= nodes.len()` (builder.rs:172-179); `InChildrenOf(b, r)` needs `b` staged and `r.end <= b.children.len()` (builder.rs:181-193, with `panic!("content parent {:?} has not been staged")`), and at flatten time `b` must lie in the region's own subtree (builder.rs:311-329).
- The returned `nodes` must be staged, unclaimed, in source order, and span-contiguous with the preceding argument's region: `StdInvocationParser` takes the node's span end from `children.last()` (`constructs/invocation_parser.rs:149-155`) and the regions must tile the child list exactly (builder.rs:163-169, 196-202).
So an extension author who returns, say, a `content` range relative to the *child list* instead of the region, or stages nodes out of order, gets an opaque panic rather than a diagnostic. Nothing to fix in code (the asserts are the builder's documented caller-bug policy), but the trait doc should enumerate these four obligations — they are currently discoverable only by crashing.

**5. Test coverage gaps in `spec/mod.rs:33-137` (specific).** What is covered is good: dyn object-safety of `CallableSpec` (`mod.rs:95`), the neutral-callable defaults (`mod.rs:78-79`), the two structure lists incl. by-name access (`mod.rs:96-99`), and `Debug` surviving through the dyn parser (`mod.rs:134-135`). Missing, in priority order:
- **Thread safety is never asserted in the module that declares it a core contract** (`callable.rs:35-39`, `structure.rs:82-86`; DESIGN_RATIONALE.md:703-741). `node/mod.rs:154-157` has `assert_send_sync::<NodeTree>/<Arc<dyn CallableSpec>>`, but nothing pins `Arc<dyn ArgumentParser<L>>`, `ArgumentSpec<L>`, `SlotSpec<L>`, `StdCallableSpec<L>`. A three-line `fn assert_send_sync<T: Send + Sync>()` test here would fail loudly the day a field regresses.
- **The flyweight/`Clone` claim is untested.** `mod.rs:6-9` and `callable.rs:18-27` rest on specs being de-keyed and `Arc`-shared (one spec behind several names; `ArgumentSpec::clone` shares the parser `Arc`, `structure.rs:190-198`). No test does `Arc::ptr_eq` after a clone, nor registers one spec `Arc` under two names.
- **`make_invocation_parser` — the trait's entire behavioral surface — has no coverage in the spec module.** The default and the override are exercised only indirectly, in `constructs` (`nodes_parser.rs:1838`, `1921`; `argument_parsers.rs:1414`; `environment_parser.rs:484`, `622`). That is adequate *integration* coverage, so this is about locality, not risk; a spec-module test that a `dyn CallableSpec` honors an override would pin the crate's headline escape hatch where it is documented. (Note `ConstructParser` has no `Debug` supertrait, so the returned `Box` cannot be identified without running it — testing the *default* requires a `ParseContext`.)
- `StdCallableSpec::default()` (`callable.rs:112-116`), `SlotSpec::default()` / `SlotSpec::with_state_delta` (`structure.rs:174-184`) are untested here; the slot delta is exercised only in `environment_parser.rs:791`.

**6. Test name `argument_parsers_and_state_deltas_have_a_slot` collides with the module's own vocabulary (naming) — `spec/mod.rs:103`.** "Slot" is domain vocabulary in exactly this file (`SlotSpec` = a content region), and the test declares `vec![]` slots — the name reads as if it were about `SlotSpec`s while it is about the mid-granularity extension point (a bespoke `ArgumentParser` + per-argument delta without a custom invocation parser). NAMING_STRATEGY.md principle 4 ("context determines names — but only when no sibling vocabulary competes in the same scope") is precisely against this. Suggest e.g. `custom_argument_parser_and_delta_need_no_invocation_parser`.

**7. Positive confirmation — the code's naming is current; three *docs* are stale (no code change).** The shipped API has no `ArgumentParserSpec` and no `ArgumentKind`: `ArgumentSpec.parser` is `Arc<dyn ArgumentParser<L>>` (`structure.rs:115`), exactly the twice-revised July 2026 decision (NAMING_STRATEGY.md:150; DESIGN_RATIONALE.md:658-667). But `ARCHITECTURE.md:387`, `NAMING_STRATEGY.md:16` and `:42`, and `CLAUDE.md:31` still name `ArgumentParserSpec` as a current type. The parser-side/spec-side split itself is clean and consistently applied: spec side `ArgumentSpec`/`SlotSpec`/`ArgumentParser`, node side `ParsedArguments`/`ParsedArgument`/`ParsedSlots` — no collision in scope. Also confirmed against the shipped code: `ArgumentSpec.name` really does feed diagnostics as the doc claims (`argument_parsers.rs:159-162`, `356`, `445`), and the "caller stacks the delta / reverts structurally" clause of `structure.rs:90-93` is exactly what `parse_declared_arguments` does (`invocation_parser.rs:92-98`, revert before `?`).

**8. Design note (efficiency): per-invocation allocation is one `Box` + one `Vec` per provided argument.** The good half: argument parsers are tier-1 `Arc`-shared `&self` objects — resolving `\frac`'s two arguments allocates no parser (`structure.rs:115`, `invocation_parser.rs:97`). The residual churn is (a) the `Box<dyn ConstructParser>` per invocation (accepted, with a benchmark obligation before phase close — Phase6Execution.md decision 2 / §6.7), and (b) a `Vec<BuildId>` per provided argument (`structure.rs:46`), immediately `extend_from_slice`d into the invocation's child list and dropped (`invocation_parser.rs:102`). If the 6.7 benchmark flags argument-heavy input, the model-preserving fix is passing `&mut Vec<BuildId>` and returning `(Range<u32>, ContentNodes)` — but the current shape is the decided one (Phase6Execution.md:349-356), so this is a flag, not a recommendation.

**9. Slot-bearing specs still have no declarative path — and `StdCallableSpec` never got the "optional parser override" ARCHITECTURE promises (API design, forward-looking) — `spec/callable.rs:70-78`, `callable.rs:81-97`; cf. `ARCHITECTURE.md:420`.**
ARCHITECTURE.md:420 describes `StdCallableSpec` as "the two structure specs + optional parser override"; what shipped is `{ arguments, slots }` with the override living on the *trait default* instead. That substitution is fine (arguably better — a factory field would need an HRTB over `'a`/`'s` tied to `&'a self`), and the docs in this module never claim the field — but ARCHITECTURE.md still does. The consequence worth acting on: an environment-shaped callable must implement `CallableSpec` from scratch and hand-roll the whole composition — argument parsing, slot-delta stacking, `ParsedSlots`/`ChildRegion` assembly, content designation, span end (~60 lines, `environment_parser.rs:507-609`, currently test code). That is exactly the region bookkeeping finding #4 says the builder enforces by panic. Phase 6.6 reserves the right to "promote the composition to a core helper only if it proves nontrivial" (Phase6Execution.md:392-395); the shipped test composition shows it *is* nontrivial. Recommend shipping it as a core helper (a slots-aware `StdInvocationParser` sibling) in 6.6, and correcting ARCHITECTURE.md:420.

**Net:** recommended actions in priority order —
1. Fix the two stale doc paragraphs in `spec/structure.rs:14-16` and `:148-150` (they advertise a rejected API), and the "preset-provided" phrase at `:9-12` (+ `NAMING_STRATEGY.md:89`, the test comments at `mod.rs:53,86`). (#1, #2)
2. Add `ParsedArgumentNodes` to `lib.rs:104`; consider adding the standard argument parsers + `scan_argument_noise`/`stage_pre_space` + `StdInvocationParser` to the root re-export. (#3)
3. Enumerate the four builder-enforced obligations in the `parse_argument` doc (`structure.rs:96-109`). (#4)
4. Add the missing spec-module tests: `Send + Sync` assertions, an `Arc::ptr_eq` flyweight/clone test, `StdCallableSpec::default`/`SlotSpec` delta. (#5)
5. Decide the 6.6 core helper for slot-bearing compositions, and sync `ARCHITECTURE.md:420` / the `ArgumentParserSpec` mentions in ARCHITECTURE/NAMING_STRATEGY/CLAUDE. (#9, #7)
6. Cosmetic: rename the `…have_a_slot` test. (#6)

## Source file `src/token/reader.rs`

(Phase 2/3 — the `TokenReader` trait, the `skip_whitespace` primitive, and `StdTokenReader`, the rules-driven scanner. Reviewed by an Opus agent. The scanning core is correct: I found **no span-arithmetic or off-by-one bug**, and the tricky whitespace/paragraph interplay is exactly right. `cargo test` passes 280/280. Findings are one API/contract defect, two robustness gaps, one decided-design consequence worth re-confirming, plus efficiency and test-coverage items.)

**Summary:** The detection priority, the pre-space/post-space partition, and the multi-newline rule all match DESIGN_RATIONALE §3.2 line for line, and several invariants that *look* fragile (`end + 1` after a newline, the `newlines < 2` branch, the O(run) whitespace scan) check out on inspection. The highest-signal item is **#1**: the trait's peek-idempotence contract explicitly invites implementations to memoize on `Arc` pointer identity, but `peek` hands them a bare `&ParsingState<L>` they cannot retain — so the documented cache key is ABA-unsound as specified, even though every call site already holds an `Arc`. **#2** (unvalidated `SpecialsMatch::end`) is the one place a well-behaved-looking `Lang` can hang the parser. **#3** flags that the dangling-escape recovery drops a source byte from the tree.

**1. The peek-memoization contract cannot be honored as documented: `peek` receives `&ParsingState<L>`, not `&Arc<…>` (design / robustness) — `reader.rs:31-36`, `reader.rs:42`.**
The trait doc states: *"implementations may memoize on that key (states are immutable, so `Arc` pointer identity is a sound cache key)"*. But the signature is `fn peek(&mut self, state: &ParsingState<L>)`. A memoizing reader can only derive `state as *const ParsingState<L> as usize` from that reference — it cannot hold a strong reference to keep the allocation alive. The moment the engine drops that state (structural revert after a group/argument — `nodes_parser.rs:462-466`, `invocation_parser.rs:96-98` do exactly this), the allocator may hand the same address to the *next* derived state, and the reader serves a stale token for a different rule set. This is precisely the ABA hazard that `ParserSession::group_interior_memo` avoids **by pinning its key `Arc`s** (engine review, finding 2) — so the codebase already knows the invariant; this trait is the one place the API doesn't provide it.
Every call site already has an `Arc`: `ParseContext::state` is `Arc<ParsingState<L>>` (`constructs/mod.rs:111`) and `cx.tokens.peek(&cx.state)` (`constructs/mod.rs:84`) only compiles today via deref coercion — so changing the parameter to `&Arc<ParsingState<L>>` would be source-compatible at every existing call site and would make the documented key actually attainable. Alternatively, amend the doc to say memoization requires the implementation to retain a state clone/`Arc` obtained by other means. Either way, the contract and the signature should agree, since the doc is what a third-party `TokenReader` author will build on.

**2. `SpecialsMatch::end` is used unvalidated — a zero-width match hangs the parse loop (robustness) — `reader.rs:191-200`.**
```rust
if let Some(m) = L::scan_specials(state, s, pos)? {
    return Ok(Token::new(TokenKind::Specials { … }, Span::new(pos, m.end), pre_space));
}
```
`m.end` is taken on faith. Three failure modes, all from a *safe* `Lang` impl:
- `m.end == pos` (an empty trigger, e.g. a scanner with an accidentally-empty entry in its trigger list — cf. the guards the codebase *does* have against empty delimiters at `reader.rs:266` and `prefix_table.rs:67-69`): the token has zero width, `NodesParser` dispatches it and `move_past(token, true)` leaves `pos` unchanged → **infinite loop** in the content dispatch loop. This is the same unasserted advancement contract the `nodes_parser` review flagged as finding #4, and this is its most reachable violation.
- `m.end > s.len()` or `m.end` off a char boundary: the token is built fine, and the panic surfaces much later when the node builder slices the source (`Span::slice`, `source/span.rs:53-55`) — an opaque failure far from the buggy scanner.
- `m.end < pos`: only a `debug_assert` in `Span::new` catches it; release builds get an inverted span.

A three-condition `debug_assert!(m.end > pos && m.end <= s.len() && s.is_char_boundary(m.end), …)` right at line 197 would pin the `SpecialsMatch` contract (`specials.rs:31-32` documents `end` but nothing enforces it) at the boundary where it is cheap to diagnose. Highest-value one-liner in the file.

**3. The dangling-escape recovery leaves the escape byte covered by no token and no node (design-note / span partition) — `reader.rs:302-310`.**
```rust
let placeholder = Token::new(TokenKind::EndOfStream, Span::empty(pos), pre_space);
… TokenRecovery { token: placeholder, resume_pos: s.len() }
```
The placeholder is an `EndOfStream` at `pos` (the escape's position) while reading resumes at `s.len()`, so the bytes `pos..s.len()` — the escape character itself — are claimed by nothing. Verified downstream: `nodes_parser.rs:1644-1656` (`escape_at_end_of_input_tolerant_recovers_and_repositions`) on `"ab \\"` produces `["chars 0..3 \"ab \""]` with `parsed.pos == 4`, and — tellingly — is the **only** recovery test in that region that does *not* call `assert_partition` (its siblings at `nodes_parser.rs:1626` and `:1689` do). So the byte is genuinely dropped, and a tolerant parse of a document ending in `\` yields a tree whose root children do not tile the content.
Contrast the `ForbiddenChar` recovery two lines below (`reader.rs:204-212`), which keeps the byte by using a `Char(c)` placeholder with the real span and `resume_pos = span.end` — content-preserving, partition-preserving. DESIGN_RATIONALE §3.2 *does* decide `EndOfStream` here ("Also serves as the recovery placeholder for a dangling escape at EOF"), but the recorded rationale is only that the Phase-2 empty-`Chars` sentinel became impossible — a `Char(escape_char)` placeholder (resume at `name_start`) was never on the table and would preserve both invariants. Flagging as a decision to re-confirm, not a bug to fix unilaterally: if the byte-drop is intended, it deserves an explicit note (and the `\`-at-EOF case should be excluded from any future whole-document partition check), since the recomposition/round-trip property silently fails on it.

**4. Membership tests on the hot path are linear string scans (efficiency) — `reader.rs:79`, `reader.rs:204`, `reader.rs:314`, `reader.rs:317`.**
`ws.chars.contains(c)`, `rules.forbidden_chars.contains(c)` and `rule.name_chars.contains(c)` are all `str::contains(char)` — a substring search over the `String`, executed *per character*. The command-name scan is the worst: `\somemacro` runs `name_chars.contains(c)` (a 52-char string in the latexlike-flavored rules) for each of its 9 name characters plus the terminator, and `skip_whitespace` pays a scan per whitespace character. This is textbook per-state derived data: the file already has the pattern for it — `PrefixTable` and `TriggerChars` are computed once at `ParsingState::freeze` (`parsing_state.rs:93-104`) precisely so the hot path doesn't re-derive. A cached char-set (ASCII bitmap + fallback set) for `whitespace.chars`, `forbidden_chars`, and each `CommandRule::name_chars` would slot into the same seam with no API change. Not urgent (correctness is unaffected, and CLAUDE.md says no premature optimization), but it is the one place where the "cache at freeze" discipline was not applied and the cost is per-character rather than per-token.
Minor companion: `rules.commands.iter().find(|r| c == r.escape_char)` (`reader.rs:181`) is a per-token linear scan — negligible for 1-2 rules.

**5. Panic surfaces: unchecked subtraction, `unreachable!`, and an unguarded public `skip_whitespace` (robustness, low) — `reader.rs:390`, `reader.rs:282`, `reader.rs:72`.**
- `reader.rs:390`: `self.pos = tok.span.end - tok.post_space().len()` — unchecked `usize` subtraction. Unreachable with reader-produced tokens; identical line exists in `list_reader.rs:101` where it was already flagged. A `debug_assert!(tok.post_space().end <= tok.span.end)` would cover both.
- `reader.rs:282`: `(None, None) => unreachable!("prefix table entries always carry a direction")`. **Verified genuinely unreachable**: `PrefixTable::for_rules`'s `add` closure (`prefix_table.rs:66-82`) is the only entry constructor, and it always fills one direction on a freshly pushed entry. Same class as the already-flagged `group_parser.rs:144` and `nodes_parser.rs:282` — consistent with codebase convention, but it is a release panic under the "never panic in lib code" rule. Worth one deliberate crate-wide call rather than three separate ones.
- `reader.rs:72`: `skip_whitespace` is `pub` (re-exported at `lib.rs:110`) and does `content[pos..]` with no documented precondition; an out-of-range or mid-char `pos` panics with a bare slice-index message. Related: `move_to_pos` (`reader.rs:141-145`) *does* `debug_assert` bounds and char-boundary, but `resume_at` (`constructs/mod.rs:70-74`) repositions through a synthesized marker token and `move_to`, bypassing those checks entirely — so a bad `TokenRecovery::resume_pos` from a custom scanner would panic inside `content[pos..]` at `reader.rs:78`/`:157` rather than at the boundary. (Today's two `resume_pos` values, `span.end` and `s.len()`, are both sound.)

**6. Test-coverage gaps — decided behaviors with no test (test-coverage).**
The suite here is strong (paragraph-break corners, `$`/`$$` disambiguation ported from pylatexenc, every `enable_*` gate, the two recovery paths, an end-to-end walk). The specific holes:
- **Forbidden-char priority is untested.** The forbidden check is deliberately *last* (`reader.rs:204`, after groups/commands/comments/specials), so a forbidden `$` must still tokenize as `GroupOpen`. `forbidden_char_error_with_recovery` (`reader.rs:1213-1231`) sets `forbidden_chars: "%$"` but only ever reads at the `%` (with comments disabled) — the `$` is never reached. The interesting half of the decided priority is unexercised.
- **`scan_specials` returning `Err` is untested anywhere in the crate** (grepped: the only `scan_specials` impls are at `reader.rs:1104`, `nodes_parser.rs:860`, `environment_parser.rs:452`, none of which can fail). DESIGN_RATIONALE §3.2 explicitly promises "the scan returns `TokenResult`, so scanner errors participate in the recovery-token protocol" — the `?` at `reader.rs:191` is the sole implementation of that promise and nothing drives it, including the recovery-token round trip through `NodesParser`.
- **`TriggerChars::Any` never reaches `peek`** (only the unit test at `specials.rs:105`). The conservative-scanner path — hook consulted at every content character — is unexercised end-to-end.
- **Two `CommandRule`s sharing an escape character** ("earlier entries win on conflict", `rules.rs:64`) — `command_records_the_fired_rules_escape_char` uses two *distinct* escape chars; the documented tie-break at `reader.rs:181` is untested.
- **`enable_whitespace: false` + comments** — `rules.rs:87-90` decides that the comment terminator is `'\n'` "independent of `WhitespaceRules`, so comments work even with whitespace handling disabled". The consequence (comment gets an *empty* post-space, and the terminating `\n` surfaces as a `Char` token) is a real decided behavior with no test.
- **`expecting_group_close` with an empty `close`** — the `!expected.close.is_empty()` guard (`reader.rs:266`) is load-bearing: without it a rule with an empty close would emit a zero-width `GroupClose` and hang the loop (same failure as #2). Untested.
- **`move_past(tok, false)` on a `Comment` token** — the rewind-before-post-space flag is only tested for a `Command` (`reader.rs:1345`), though `Comment` is the other post-space-carrying kind.
- Smaller: a multi-byte forbidden char / multi-byte escape char (the `pos + c.len_utf8()` arithmetic at `reader.rs:205`/`:300` is only exercised for ASCII); a paragraph break at byte 0 through `peek` (only through the `skip_whitespace` unit test).

**7. Confirmed correct — do not re-flag (positive verifications).**
- **The whitespace/paragraph interplay is exact.** `skip_whitespace` breaks at the *first* newline of a `\n\s*\n` run, so a token's `pre_space` can never contain a newline whenever a `ParagraphBreak` follows — which is what makes `detect_paragraph_break`'s "spans from the first through the last newline of the run" (`reader.rs:217-220`) literally true rather than approximately true. Consequently the `newlines < 2` early return (`reader.rs:247`) is unreachable from `peek_impl` and is purely defensive; correct to keep, but it is not a live branch.
- **`paragraph_continues(content, end + 1, ws)` (`reader.rs:84`) is char-boundary and bounds safe**: `end` is the byte offset of the just-inspected `'\n'` (1 byte), so `end + 1 <= content.len()` and lands on a boundary.
- **`skip_whitespace` is O(run), not O(run²)**: a run containing ≥2 newlines breaks at the first one, so `paragraph_continues` (itself O(run)) runs at most once per call.
- **Detection priority matches the spec exactly** — paragraph break → expected group close → longest delimiter → command escape → comment start → specials scan → forbidden check → `Char` (`reader.rs:162-214`), identical to `rules.rs:105-108` and DESIGN_RATIONALE §3.2.
- **The `pre_space.end == span.start` adjacency invariant holds for every kind** (chars, commands, comments, group delimiters, `ParagraphBreak` at `reader.rs:250`, `EndOfStream` at `:168`). `TokenListReader::peek`'s `partition_point` + pre-space clipping (`list_reader.rs:69-72`) depends on exactly this, and the two readers agree.
- **The `enable_*` gates are correctly *conjunctive* in `detect_paragraph_break`** (`reader.rs:227-231`): whitespace off, multi-newline off, *or* `'\n'` not in `ws.chars` each independently suppress break detection — so a language that doesn't call newline whitespace gets `Char('\n')`, not a spurious break.
- **`GroupClose` under `enable_groups: false` + `expecting_group_close` stays resolvable**: the reader's expected-close-first-then-table priority (`reader.rs:265-276`) is mirrored exactly by `group_close_type` (`nodes_parser.rs:716-728`), so the class of a close emitted through the ungated expected-close path is re-derived consistently. The decided interaction (DESIGN_RATIONALE §3.2, interaction 1) holds end to end.
- The two `.expect("… checked above")` calls (`reader.rs:178`, `:312`) are genuinely unreachable (guarded by `pos < s.len()` / `name_start < s.len()` immediately above).
- Naming conforms to NAMING_STRATEGY (`TokenReader`/`StdTokenReader`, `CommandRule`, the command-not-macro terminology in all doc comments).

**8. Minor notes (maintainability / docs).**
- `pos()` exists twice — inherent (`reader.rs:131-133`) and trait (`reader.rs:402-404`). Identical today; the inherent one shadows in direct-typed code, the trait one in generic code. Harmless, but if they ever diverge the bug is invisible. Same for `content()`/`is_at_end()`, which are inherent-only and therefore unavailable to generic `TokenReader` consumers — fine if intended, worth a word if `is_at_end` is meant to be part of the protocol.
- CRLF: `read_comment` sets `content_end` at the `'\n'` (`reader.rs:367-370`), so on `\r\n` input the `'\r'` stays inside the comment's `content` rather than in its post-space, even though `'\r'` is declared whitespace. This is byte-for-byte pylatexenc parity (`_tokenreader.py:658` does `s.find('\n', pos_inner_start)`), so it is a *quirk inherited*, not a regression — but CLAUDE.md invites improving on pylatexenc quirks, and this is a cheap one.

**Net:**
1. Decide **#1** (widen `peek` to `&Arc<ParsingState<L>>` — source-compatible at all call sites — or amend the contract). It is the only finding that affects the public extension point's soundness.
2. Add the `SpecialsMatch::end` `debug_assert` (**#2**) — one line, closes the only reachable non-termination hole in the tokenizer.
3. Re-confirm the dangling-escape placeholder (**#3**): keep `EndOfStream` and document the uncovered byte, or switch to a `Char(escape_char)` placeholder and restore the partition.
4. Fill the two contract-level test gaps that pin decided behavior: forbidden-char priority (a forbidden `$` still opening a group) and a failing `scan_specials` driven through `peek` + tolerant recovery.
5. Optional: the per-state char-set caches (**#4**) and the panic-surface hardening (**#5**).

## Source file `src/token/error.rs`

(Phase 2/3 — `TokenError` + the recovery-token machinery (`TokenErrorKind`, `TokenRecovery`, `TokenResult`). Reviewed by an Opus agent; no *live* correctness bug in the shipped code — both recoveries `StdTokenReader` can construct do satisfy the load-bearing advancement contract, verified below. The findings are: an unstated/unasserted contract that a public extension point can violate (hang or panic), one lossy recovery, and an expressiveness gap. `cargo test`: 280/280 pass; this file itself has **zero** tests.)

**Summary:** The type design is right and matches the decided architecture (policy-free reader, transient byte `Span`s, structured kind, explicit `resume_pos`, manual impls to avoid spurious `L:` bounds). What is missing is the *contract*: the whole content loop's termination — and the reader's memory safety — depend on properties of `TokenRecovery` that this file neither documents nor checks, even though `TokenError`/`TokenRecovery` are public with public fields and `Lang::scan_specials` (a documented extension point, `state/lang.rs:170-171`) returns `TokenResult` and can therefore mint arbitrary recoveries.

---

**1. The `resume_pos` advancement contract is real, satisfied today, and completely unwritten (robustness / correctness-hazard) — `token/error.rs:57-62`, `reader.rs:204-212`, `reader.rs:302-310`, consumed at `nodes_parser.rs:496-510`.**

Verification of the two recoveries the tokenizer can build (this is what the nodes_parser review asked to be checked here):

- `ForbiddenChar` (`reader.rs:204-211`): `span = (pos, pos + c.len_utf8())`, `resume_pos = span.end`. Since `pos = ws_end >= self.pos` (whitespace skipping only moves forward) and `c.len_utf8() >= 1`, `resume_pos > self.pos` **strictly**. On a char boundary, `<= content.len()`. ✔
- `EndOfStreamAfterEscape` (`reader.rs:302-309`): the branch is entered only when `name_start >= s.len()`; since `pos < s.len()` and `pos` is a char boundary, `name_start == pos + escape.len_utf8() == s.len()`, so `resume_pos = s.len() = name_start > pos >= self.pos`. ✔

So the shipped tokenizer is safe. But the contract is nowhere expressed. `TokenRecovery::resume_pos`'s doc is exactly one line — *"Byte position at which to resume reading"* (`error.rs:60-61`) — and there is no `debug_assert`. The failure modes are severe and both reachable from the public API:

- **Hang.** `NodesParser` deliberately does *not* consume a recovery placeholder (`recovered == true` suppresses every `move_past`/`move_to`); the sole source of loop progress is `resume_at(cx.tokens, recovery.resume_pos)` (`nodes_parser.rs:505`). A recovery with `resume_pos <= ` the reader's position at the failed `peek` re-peeks the same error forever. A `scan_specials` author who thinks "resume at the trigger, the loop will skip it" writes exactly that.
- **Panic (violates CLAUDE.md rule 4).** `resume_at` (`constructs/mod.rs:70-74`) repositions via `TokenReader::move_to`, and `StdTokenReader::move_to` (`reader.rs:394-400`) assigns `self.pos = tok.span.start` **without** the bounds/char-boundary `debug_assert`s that `move_to_pos` has (`reader.rs:141-145`). An out-of-range or mid-codepoint `resume_pos` therefore reaches `content[pos..]` in `skip_whitespace` and panics in *release*.

Recommended (cheap, all in this file): document on `resume_pos` that it must be **strictly greater than the error's `span.start`** (which is itself `>=` the reader position at the failed read), must be `<= content.len()`, and must lie on a char boundary; state the consequence (the content loop never consumes a placeholder, so a non-advancing recovery hangs it). Then add `debug_assert!(recovery.resume_pos > span.start)` in `TokenError::new` (`error.rs:66-72`) — the constructor has both values in hand, so the invariant can be checked at the *producing* site rather than only at the consuming one. The `debug_assert` in `nodes_parser` that the earlier review suggested is then a belt-and-braces second line.

**2. The dangling-escape recovery silently drops a source byte from the tree (design question, user call) — `error.rs:53-56` (the documented example), `reader.rs:304-308`, evidenced by `nodes_parser.rs:1645-1656`.**

The placeholder is `EndOfStream` with `Span::empty(pos)` while `resume_pos = s.len()`, i.e. the recovery deliberately jumps over the escape character. `NodesParser`'s `EndOfStream` arm flushes only the placeholder's `pre_space` and returns, so bytes `pos..s.len()` (the `\`) are consumed by the reader but represented by **no node**. The existing test confirms it: for `"ab \\"`, `shapes == ["chars 0..3 \"ab \""]` and `parsed.pos == 4` — and, tellingly, that test is the one tokenizer-recovery test that does *not* call `assert_partition` (compare `forbidden_char_tolerant_adopts_the_recovery_token`, `nodes_parser.rs:1626`). The loss is invisible to `check_tree_invariants` only because the harness derives the root `List` span from the first/last child (`nodes_parser.rs:996-1004`).

This is inconsistent with the project's own §3.8 recovery principle — "markup text inside a `Chars` node is an accepted tolerant-recovery artifact, always accompanied by a diagnostic" — which the *unresolvable-command* recovery follows by staging a chars node over the whole token span. DESIGN_RATIONALE §3.2 (line 321-322) justifies `EndOfStream` here on the grounds that Phase 2's *empty* `Chars` token became impossible when `Chars` became `Char(char)` — but a **non-empty** `Char(escape_char)` placeholder spanning `pos..s.len()` with `resume_pos = s.len()` is perfectly expressible, keeps the byte in the tree (it joins the pending chars run), and would make the tolerant escape test partition-clean. Worth a conscious decision; if the loss is intended (pylatexenc parity — its recovery token is an empty char token too), say so in the `TokenRecovery` doc, because the general rule for consumers should be *"bytes between `token.span.end` and `resume_pos` are dropped from the AST"*, and nothing states that today.

**3. `TokenErrorKind` gives custom scanners no way to report their own condition (expressiveness gap) — `error.rs:29-41` vs `state/lang.rs:164-183`.**

`Lang::scan_specials` returns `TokenResult` and its doc says it "May fail recoverably (the error participates in the recovery-token protocol like any tokenization step)" — but the only kinds it can construct are `EndOfStreamAfterEscape { escape_char }` and `ForbiddenChar { ch }`, both tokenizer-internal. A scanner that wants to report, say, an unterminated `~~`-style trigger has to lie with one of those two. Compare `ParseErrorKind::Syntax { message: String }` (`error.rs:220-223` in `src/error.rs`), which is exactly the free-form escape hatch the parse level has. Suggest a `TokenErrorKind::Syntax { message: String }` (or `Custom { message }`) variant for the hook, mirroring the parse-level precedent. Note the cost: `TokenErrorKind` would lose `Copy` (`error.rs:29`), and `TokenError::kind()` (`error.rs:75-77`) would have to return `&TokenErrorKind` — a small, contained API change worth doing before downstreams appear.

**4. `TokenErrorKind` is not `#[non_exhaustive]` although its own doc promises growth, and it is embedded in a `#[non_exhaustive]` enum (API / maintainability) — `error.rs:26-30`.**

The doc says it "grows as the tokenizer learns to detect more error conditions", and `ParseErrorKind` — which wraps it as `ParseErrorKind::Token(TokenErrorKind)` (`src/error.rs:213-217`) — *is* marked `#[non_exhaustive]` for precisely that reason. Adding any variant here is a breaking change for downstream `match`es that the outer enum has already protected itself against. Inconsistency; mark it `#[non_exhaustive]` too (this becomes mandatory if #3 lands).

**5. The recovery-placeholder token model is safe today, but the guard is out-of-band, not in the type (design note; answers the "are placeholders distinguishable?" question) — `error.rs:57-62`, `nodes_parser.rs:496-510`, `605-612`, `641-651`, `667-678`.**

A placeholder token is *not* distinguishable from a real token — `Token` carries no marker. The only thing that prevents a placeholder `Command`/`Specials` from being dispatched is `NodesParser`'s local `recovered: bool`, threaded by hand through every arm. That works, and is correct: the `Command` arm skips resolution when `recovered` (`nodes_parser.rs:611-612`), the `Specials` and `GroupOpen` arms route straight to `recover_as_chars`. And in practice `StdTokenReader` only ever mints `Char` and `EndOfStream` placeholders, so nothing dispatchable can arise from it (the comment at `nodes_parser.rs:668` — "no current `TokenRecovery` emits one" — is accurate). But `scan_specials` *can* mint a placeholder `Specials` carrying a live spec, and any other consumer that adopts a recovery (a custom `TokenReader`-driven parser, a future takeover parser) must re-derive the same discipline from scratch. Two cheap fixes, either or both: (a) document in `TokenRecovery` which kinds a placeholder may carry (recommend: only non-dispatchable kinds — `Char`, `EndOfStream`, `Comment`, `ParagraphBreak`) and that consumers must never treat one as consumable; (b) note that a placeholder's span and `resume_pos` should agree unless byte loss is intended (see #2), since divergence silently punches a hole in the node-span partition.

**6. `TokenError` inflates the hot-path `Result` by 45% (efficiency, measured) — `error.rs:44-48`.**

Measured against the crate (`SimpleLang`): `Token = 72`, `TokenRecovery = 80`, `TokenError = 104`, and therefore `Result<Token, TokenError> = 104` bytes — the value every `peek`/`next` returns, on every token, for a payload that is 72 bytes and an error case that is essentially never taken. Making the field `Option<Box<TokenRecovery<'s, L>>>` drops `TokenError` to 32 bytes and the `Result` to 80 (i.e. `Token` + tag). The error path is cold by construction (DESIGN_RATIONALE: "errors are rare and cold"), so the boxing cost is paid exactly where it doesn't matter. `recovery()`/`into_recovery()` can keep their current signatures (`Option<&TokenRecovery>` / `Option<TokenRecovery>` via `map(|b| *b)`). Modest but free, and this is the crate's single hottest return value.

**7. Test coverage: the file has *no* tests, and the contract of #1 is pinned nowhere (test-coverage) — `error.rs` (whole file); nearest coverage `reader.rs:1212-1253`, `nodes_parser.rs:1606-1668`.**

`reader.rs`'s two recovery tests assert *literal* resume positions (`resume_pos == 1`, `resume_pos == 3`) for two single-byte cases; they incidentally imply advancement but pin no contract, and nothing anywhere asserts that *every* recovery advances. Specific gaps, in priority order:

- **No "a recovery always advances" test.** Suggested: iterate the reader over a set of failing inputs and assert, for each `TokenError`, `recovery.resume_pos > err.span().start`, `resume_pos <= content.len()`, and `content.is_char_boundary(resume_pos)`. This is the executable form of the contract in #1 and the one test that would catch a regression into an infinite loop.
- **Multi-byte cases untested.** A forbidden char that is multi-byte (e.g. `→`) — the existing test uses `%`, so a bug writing `resume_pos: pos + 1` instead of `span.end` would still pass. Likewise a multi-byte `escape_char` (e.g. `§`) dangling at EOF (`name_start == s.len()` with a 2-byte escape).
- **The `recovery: None` path is never exercised.** `TokenError::new` accepts `None`, and `NodesParser` treats it as a hard abort *even under `Recovery::Tolerant`*, returning `Err` **without** recording a diagnostic (`nodes_parser.rs:501-502` — it bypasses `cx.recover`). That is defensible ("genuinely unrecoverable"), but it is a policy decision with zero test coverage and zero producers today; a custom `scan_specials` can produce one. Suggest one test driving a `None`-recovery token error through `NodesParser` in both modes.
- **No unit tests for this file's own surface**: `kind()`/`span()`/`recovery()`/`into_recovery()`, the two `Display` strings (the user-visible message text is currently unpinned), and `PartialEq`/`Clone` of `TokenError`/`TokenRecovery` (hand-written impls, `error.rs:97-123`).

**8. Confirmed correct / positive (not defects).**

- **Naming** matches NAMING_STRATEGY.md line 84 exactly (`TokenError<'s, L>`, `TokenErrorKind`, `TokenRecovery<'s, L>`, `TokenResult<'s, L, T>`; "clarity over brevity" — not `TokResult`), and `TokenResult`'s lang-first parameter order is the convention `ConstructParserResult<L, T>` was aligned to in 6.1 (Phase6Execution.md:26, `constructs/mod.rs:143-146`).
- **Policy-free by construction**: the file holds no strict/tolerant knowledge; the reader always returns `Err` with the recovery attached and `ParserSession::recover` (`engine/mod.rs:135-147`) decides. Exactly the decided §3.8 shape; the WIP's per-reader `tolerant_parsing` flag is genuinely gone.
- **No panics in this file**; the manual `Clone`/`PartialEq`/`Debug` impls (`error.rs:95-142`) correctly avoid spurious `L: Clone/Debug/PartialEq` bounds, matching `token.rs:159-161`.
- **Byte-`Span`-only errors** (no `SourceSpan`, no `Arc`) per §3.8; both consumers (`constructs/mod.rs:88-92`, `nodes_parser.rs:499-500`) do the `Span → SourceSpan` lift at the construct layer, which is where `cx.source` lives. (Minor: those two sites duplicate the same three-line lift; a `TokenError::into_parse_error(&Arc<Source<..>>)` helper would DRY it, but `From` can't be used since the source Arc is needed.)
- **`ForbiddenChar`'s doc is precise**: "appeared as content" — the check sits after group/command/comment/specials recognition (`reader.rs:204`), so a forbidden char that is also a delimiter or escape char is never reported. The doc does not overclaim.
- **Doc drift (outside this file, worth one edit):** DESIGN_RATIONALE §3.8 (line 1538-1539) still says the dangling-escape placeholder is "an empty chars token", which §3.2 (line 321-322) and the code (`reader.rs:304`) superseded with `EndOfStream`. `error.rs:53-56` has it right.

---

**Net (priority order):**
1. **Write and assert the `resume_pos` contract** (#1): doc on `TokenRecovery::resume_pos` (strictly past `span.start`, in-range, char boundary; hang is the failure mode) + `debug_assert!` in `TokenError::new`. This is the finding the nodes_parser review deferred to this file, and the contract is currently violable from a public extension point with a release-mode panic or an infinite loop as the payoff.
2. **Decide the dangling-escape byte loss** (#2) — `Char(escape_char)` placeholder spanning `pos..len` would preserve the byte and align with §3.8's chars-fallback principle; if the loss is intended, document it as "bytes between `token.span.end` and `resume_pos` are dropped".
3. **Add the recovery-always-advances test** plus the multi-byte and `recovery: None` cases (#7) — the file currently has no tests at all.
4. `#[non_exhaustive]` on `TokenErrorKind` (#4), and consider the scanner escape-hatch variant (#3) before downstreams exist.
5. `Box` the recovery (#6): `Result<Token, TokenError>` 104 → 80 bytes on the hottest return path.

## Source file `src/state/parsing_state.rs`

(Phase 3/4 — `ParsingState<L>` / `StateData<L>`: the immutable, `Arc`-shared parsing context, its private data, and the two eagerly-built derived caches (`PrefixTable`, `TriggerChars`). Reviewed by an Opus agent; **no correctness bug in the frozen/derived machinery** — the freeze-after-finalize ordering, cache invalidation, and non-mutation contracts all hold and were verified by experiment. The substantive findings are one **airtightness hole at the seed state**, the **per-transition cost/retention** of a full deep clone + full cache rebuild, and a dead `Clone` impl that is an identity hazard. `cargo test`: 280/280 pass + 1 doc-test.)

**Summary:** The choke-point design is implemented faithfully and the hot-path story is right: `data` is private, there is no `&mut` accessor, no `Arc::make_mut` anywhere in the crate, and every path that changes `data` (`apply_overrides` → `finalize_transition`) is followed by `freeze()`, which recomputes **both** caches from the *final* data — so a delta structurally cannot mutate an input without invalidating a derived cache. What is *not* airtight is the entry point: `ParsingState::new` is a public constructor of an arbitrary state that **never runs `Lang::finalize_transition`** (#1), so a normalizing customizer's invariants do not hold on the seed. Efficiency is the other real theme: a derivation deep-clones every `String`/`Vec`/`BTreeMap` in the state and rebuilds the `PrefixTable` from scratch even for the *dominant* transition (group interior), which touches nothing the table depends on (#2).

---

**1. `ParsingState::new` bypasses `finalize_transition` — the seed state is un-normalized, and the "airtightness is structural" claim has a hole (correctness / design) — `parsing_state.rs:49-51`, `93-104`.**
`new()` calls `freeze(data)` directly; `freeze` builds the caches but never calls `L::finalize_transition` (only `derived()` does, line 61). Consequences, all verified in a scratch copy of the crate:

- For the *pure-normalization* idiom that ARCHITECTURE.md §state recommends (and that this file's own `MathLang` test at line 312 demonstrates), a seed state built with `ext` already in the non-default mode keeps rules the customizer would have rewritten. Concretely, with a `finalize_transition` that adds a `$…$` `GroupRule` whenever `ext.math` is set: `ParsingState::new(StateData { ext: Ext { math: true }, .. })` yields a state whose `prefix_table().match_at("$x")` is `None`, and `state.derived(&ParsingStateDelta::new())` — an **empty** delta — "repairs" it to `Some`. So `state` and `state.derived(&empty)` are *not* data-equivalent for a normalizing `Lang`, contradicting the spirit of `empty_delta_is_a_clean_copy` (line 232, which only passes because `PlainLang`'s finalize is a no-op).
- The state is internally inconsistent in the customizer's own terms: `trigger_chars` *does* see `ext` (it is computed from the whole `StateData` at freeze, line 99), while the rules do *not* (finalize skipped). Same input, two different treatments.
- `new()` is `pub`, `StateData`'s fields are `pub`, and `rules()`/`libraries()`/`ext()` all hand out cloneable values — so *any* code (including a construct parser mid-parse) can assemble a modified `StateData` and mint a state that never passed the choke point. The compiler prevents *mutating* an existing state; it does not prevent *constructing a sibling*. ARCHITECTURE.md §state's "Airtightness is structural. Private fields + `derived()` as sole constructor mean the compiler guarantees finalize sees every change" therefore overstates the guarantee, as does `state/mod.rs:14-17`.

The structural reason is real (`finalize_transition` requires a `prev: &ParsingState`, which the seed has no candidate for), so this is a design gap, not an oversight. Options, in increasing order of change: (a) document explicitly on `new()` that finalize does **not** run and the seed's coherence is the language author's responsibility, plus a test pinning it; (b) add a `Lang::finalize_initial(&mut StateData<Self>)` hook run by `freeze` from `new()`; (c) make `prev: Option<&ParsingState<Self>>` and run the single hook in both paths. Whatever the decision, it should be recorded — the current docs read as if the invariant is airtight, and Phase 7's latexlike/FLM presets are precisely the normalizing customizers that will trip on it. (Note the deferral of `Language<L>` makes this live *now*: with no seeding facade, every caller in-tree constructs states through `new()` by hand.)

**2. Every derivation deep-clones the whole `StateData` and rebuilds both caches — including for the transition that dominates the parse (efficiency / memory retention) — `parsing_state.rs:58-63`, `93-104`.**
`derived()` does `self.data.clone()` → `TokenRules::clone` (`rules.rs:203-220`) clones `whitespace.chars: String`, `groups: Vec<Arc<_>>`, `commands: Vec<CommandRule>` (each with its own `name_chars: String` — 52 bytes for a latexlike alphabet), `comments: Vec<CommentRule>` (a `String` each), `forbidden_chars: String`; `LibraryStack::clone` (`library/mod.rs:308-312`) clones the `Vec<Arc<dyn SpecLookup>>` *and* the `fallbacks: BTreeMap<CallableTypeId, Arc<dyn CallableSpec>>` (a real allocating map clone, for data that is set once at seeding and never changes). Then `freeze` rebuilds the `PrefixTable` from scratch — a `String` allocation per delimiter entry plus `first_chars` (`prefix_table.rs:60-101`) — and re-runs `L::specials_trigger_chars`. Order ~15–20 allocations per transition.

This is not a rare cost: derivation happens on **every group descent** (`group_parser.rs:98`, `environment_parser.rs:108` — memoized per `(base, rule)`, so siblings share, but every distinct base pays), on **every optional-argument probe** — `argument_parsers.rs:537` derives a fresh state to peek, *even when the optional argument turns out to be absent*, and that path is **not** memoized — and on every argument/slot with a `parsing_state_delta`. And because nodes record their parse-time state, each distinct state is retained for the lifetime of the tree: a large document holds N near-identical copies of the rules data (this is the same root cause as the engine review's memo-retention finding).

Worth noting that the doc's justification — "both derivations are cheap relative to a transition" (`parsing_state.rs:36-38`, echoed in DESIGN_RATIONALE §3.3) — is close to circular: the cache rebuild *is* a large fraction of the transition's cost. Two cheap, safe fixes:
- **Skip the `PrefixTable` rebuild when its inputs are unchanged.** It depends only on `(enable_groups, groups)`. The dominant transition — the group interior — overrides *only* `expecting_group_close` (`engine/mod.rs:103-106`), which is deliberately not an input (`rules.rs:164-167`). Holding it as `Arc<PrefixTable<L>>` and, after finalize, reusing the parent's `Arc` when `enable_groups` matches and the `groups` vecs are elementwise `Arc::ptr_eq`, is a pure win with no new trait bounds and no semantic change. Same applies to library-push deltas and ext-only deltas.
- **Make the rules data cheap to clone**: `Arc<str>` for `name_chars`/`whitespace.chars`/`forbidden_chars`, and `Arc` the `LibraryStack::fallbacks` map. That turns the `StateData` clone into a handful of refcount bumps and collapses the retention of N states to refcounts.

**3. `Lang::specials_trigger_chars` is called far more often than its doc implies (API cost / doc) — `parsing_state.rs:98-102`, doc at `lang.rs:186-194`.**
The hook doc says the filter is "computed when a state is frozen and cached on the state instance (rebuilt only at transitions, like the `PrefixTable`)", which reads as *rare*. In practice it runs once per group descent, per optional-argument probe, per argument delta. The hook's whole purpose is to let a preset derive trigger characters from the *library stack* (`specials.rs:5-16`, DESIGN_RATIONALE §3.3) — i.e. the natural implementation walks every library in scope. That is an O(#libraries × #specials) walk on every group descent. Unlike the prefix table (#2), there is **no** safe generic reuse rule here: the inputs include `L::StateExt`, which carries no `Eq` bound, so the state cannot tell whether they changed. Recommend documenting the per-transition cost expectation on the hook ("keep it O(small); cache the answer in an `Arc` inside `StateExt` if it is expensive to compute"), since a preset author reading the current wording will reasonably assume it runs a handful of times per parse.

**4. Interior mutability in `L::StateExt` would silently break the frozen-state contract — the one hole in an otherwise structural immutability guarantee (robustness / doc) — `lang.rs:101` + `parsing_state.rs:99`.**
`StateExt: Clone + Debug + Default + Send + Sync` does not exclude a `Mutex`/atomic. Because `ext()` hands out `&L::StateExt` from behind a shared `Arc<ParsingState>`, an interior-mutable ext can be mutated *after* the state is frozen, at which point (a) `trigger_chars` — computed from the **whole** `StateData`, ext included (line 99) — is stale, (b) the `TokenReader` contract "peek is idempotent per (position, state *instance*), implementations may memoize on `Arc` pointer identity" (`reader.rs:31-36`) is violated, and (c) the engine's `(base, rule)` ptr-keyed memo can serve an interior state whose ext has since changed. This is not a live bug (no in-tree `Lang` does it), but ARCHITECTURE.md §nodes explicitly *sanctions* "interior-mutable set-once types" as the idiom for `NodeExt`, so a preset author could plausibly carry the idiom over to `StateExt`. One prohibiting sentence in the `StateExt` doc (and/or on `ParsingState`) closes it.

**5. `impl Clone for ParsingState` is dead code and the only identity hazard in the file (API design) — `parsing_state.rs:130-138`.**
Verified unused: I removed the impl in a scratch copy and `cargo check --all-targets` (lib + every test + benches) still compiles. Two reasons to drop it:
- It is the *only* construct in this file that can produce two distinct `Arc`s that ought to be one. Everything else is clean — `derived()`/`freeze()` return a value and never wrap in `Arc` (the engine owns identity), there is no interning, and `ChildStateSpec`'s "states pass as-is, `Arc` in / `Arc` out" rule (`child_state.rs:23-25`) depends on that.
- `Arc<T>::make_mut` requires `T: Clone`, so this impl makes `Arc::make_mut(&mut cx.state)` *compile* today. From outside the crate it can only fork the `Arc` (identity change → memo misses, silent divergence between the state a node records and the one its siblings use); from inside `state/`, it would additionally hand out `&mut ParsingState` with write access to the private data — an in-place mutation of a "frozen" state. Nothing does this now; removing `Clone` makes it un-writable.
If `Clone` is wanted for future transform APIs, say so in a doc line; otherwise delete it.

**6. Two stale/overstated doc references (doc accuracy) — `parsing_state.rs:46-48`, `36-38`.**
- Line 47-48: "at the engine level, `Language<L>` seeds the initial state from its defaults, Phase 6" — `Language<L>` is **deferred past** Phase 6 (`engine/mod.rs:8-12`; ARCHITECTURE §engine amendment: "Phase 7 at the earliest"). Today, every seed in-tree goes through `ParsingState::new` by hand. Suggest "…a future `Language<L>` will seed…".
- Line 36-38: "both derivations are cheap relative to a transition" — see #2; the derivations are a large share of the transition.

**7. Test-coverage gaps (test-coverage) — tests region `parsing_state.rs:152-375`.**
The existing 7 tests cover the override/keep-the-rest contract, prefix-table rebuild on a `groups` override, the `enable_*` scoped-disable/re-enable pair, the prefix-table gate re-bake, the finalize customizer (both idioms), and ext replacement. The specific holes:

- **(a) Nothing pins that the caches are built from *post*-finalize data.** The `finalize` → `freeze` ordering (lines 61-62) is load-bearing: a customizer that adds a `GroupRule` or flips an ext flag feeding `specials_trigger_chars` must have it reflected in `prefix_table()`/`trigger_chars()`. I confirmed by experiment that it does — but **swapping lines 61 and 62 passes every existing test**, because `finalize_centralizes_cross_cutting_rules` (330) asserts only `rules()`, never the caches. Highest-value addition: a `Lang` whose finalize pushes a group rule, asserting `derived.prefix_table().match_at("$x").is_some()`.
- **(b) `TriggerChars` has no delta-driven test anywhere in this file.** `enable_groups_flag_rebakes_the_prefix_table` (262) has no `trigger_chars` counterpart; the only `enable_specials` coverage is a *hand-built* state in `reader.rs:1169`. Missing: a delta toggling `enable_specials` off **and back on** re-bakes the filter; and — the intended preset shape — a `Lang` whose `specials_trigger_chars` reads `data.libraries`/`data.ext`, showing that a `push_library` / `ext` delta rebuilds the filter. Nothing in the crate currently feeds libraries or ext into trigger chars, so the cache-invalidation property for the `TriggerChars` half of the design is entirely untested.
- **(c) "exactly one `finalize_transition` run per derivation" is asserted nowhere.** The mergeability claim (ARCHITECTURE §state: "several changes → one delta → one `finalize_transition` run → one `Arc`") deserves a counting `Lang` (counter reachable through `ext` or a `static AtomicUsize`) asserting a delta carrying two events *plus* rules overrides runs finalize exactly once.
- **(d) No state-level `push_libraries` test.** It is covered in `library/mod.rs:584` (`push_library_via_delta_scopes_structurally`) — flagged so it is not double-counted — but the *ordering* guarantee documented at `delta.rs:106-108` ("the last one pushed ends up innermost") for a delta carrying **two** libraries is untested.
- **(e) No test of `new()`'s (non-)finalize behavior** — see #1; whichever way it is decided, it should be pinned.

**8. Confirmed correct (positive) — flagged so these are not re-litigated.**
- **Cache invalidation is airtight.** `data` is private, there is no `&mut` accessor, `derived()` is clone-then-mutate-then-freeze, and `freeze()` unconditionally recomputes *both* caches from the final data. There is no re-freeze path and no way to mutate a frozen state's inputs from outside `state/` — the `Arc<GroupRule>`s in `rules.groups` are also un-mutable in practice (`freeze` clones them into the prefix table, so their refcount is always ≥ 2 and `Arc::get_mut` returns `None`). The only escape is #4.
- **`finalize_transition` runs exactly once per new state**, after overrides and before freeze — matching `lang.rs:126-131` and the engine's split (a memo hit skips finalize but still fires `observe_transition`, `engine/mod.rs:112-125`).
- **The functional contract holds**: `derived()` never observably mutates `self` (tests at 209 and 344-345 pin it), and the `&mut` lives only on a clone nothing else can observe.
- **Both `enable_*` gates are correctly baked in at freeze** (`enable_groups` in the table, `enable_specials` in the filter), keeping the reader's hot path branch-free, while `expecting_group_close` is deliberately **not** gated — cross-checked against `reader.rs:265-276` and the `enable_groups_off_does_not_gate_the_expected_close` test (`reader.rs:1312`).
- **No panics in lib code**: no `unwrap`/`expect`/indexing in this file; `freeze`'s only `expect`s live inside `PrefixTable::for_rules` and are genuine internal invariants.
- **`ParsingState<L>: Send + Sync` is statically asserted** (`node/mod.rs:154-156`) — the `Lang` thread-safety contract is machine-checked, just from another topic's tests.
- **Naming conforms to NAMING_STRATEGY.md**: `ParsingState<L>` over a private `StateData<L>`, `ParsingStateDelta`, `derived()` as the transition verb — exactly the table entries at NAMING_STRATEGY.md:43, 71-74, 159. (`StateData` is mildly in tension with principle 2's specificity rule, but it is explicitly sanctioned in the strategy doc, so not a finding.)

---

**Net (priority order):**
1. **Decide and document the seed-state / `finalize_transition` hole (#1)** — the only item that can produce a semantically wrong state, and it will bite the Phase 7 normalizing presets. Add the pinning test either way.
2. **Cut the per-transition cost (#2)** — reuse an `Arc<PrefixTable>` when `(enable_groups, groups)` are unchanged (covers the dominant group-interior and library-push transitions), and `Arc`-ify the rules' strings + `LibraryStack::fallbacks`. Cheap, safe, and it also relieves the retention pressure the engine review flagged.
3. **Remove `impl Clone for ParsingState` (#5)** — verified dead, and it is the one thing here that can fork a state's identity (and, in-crate, enable `Arc::make_mut`).
4. **Add the two doc lines**: `StateExt` must be a plain value, no interior mutability (#4); `specials_trigger_chars` runs per transition, keep it cheap (#3).
5. **Tests 7(a) and 7(b)** — the freeze-after-finalize ordering and the `TriggerChars` delta path are the two decided behaviors currently protected by nothing.
6. Fix the stale `Language<L>`/Phase-6 doc reference (#6).

## Source file `src/constructs/mod.rs`

(Phase 6.1 — the `constructs` module root: the `ConstructParser` trait, `ParseContext`, `ConstructParserResult`, `Invocation`, and the two shared helpers `resume_at`/`try_peek`. Reviewed by an Opus agent. The abstractions are sound and faithful to §3.6; the file itself has **no tests** (203 lines, no `#[cfg(test)]` module) and its two helpers are exercised only transitively. `cargo test`: 280 passed, 0 failed.)

**Summary:** The trait/context design holds up: `ConstructParser` is dyn-compatible where it needs to be, `parse(&mut self, cx)` is uniform across all five real implementors, and the tier-2 temporaries story is implemented exactly as decided. Two genuine defects concentrate in the smallest function in the file, `resume_at`: (1) the content loop's termination depends on a `resume_pos` monotonicity contract that is stated nowhere and checked nowhere — I reproduced an **infinite loop in a release build** through a public extension point; and (2) `resume_at` deliberately routes *around* the readers' own positional guards because `TokenReader` has no positional move. Beyond that: the `cx.state` swap/restore protocol is hand-rolled at seven sites and belongs on the context type; the two helpers are `pub(crate)` although the protocols they encode are documented obligations of *public* extension points; and the module header claims a reach for `ConstructParser` that the code does not have (arguments go through the sibling `ArgumentParser` trait).

---

**1. A non-advancing `resume_pos` spins the content loop forever — the contract is unstated and unchecked (robustness / correctness hazard) — `constructs/mod.rs:70-74`, call site `nodes_parser.rs:496-509`.**
`resume_at` is the only repositioning primitive after a tokenizer error, and `NodesParser`'s loop makes **no other progress** on that path (`recovered == true` suppresses every `move_past`, lines 515/533/542/554/…): termination rests entirely on `recovery.resume_pos` having moved the reader strictly past the error. Nothing enforces it — not `resume_at`, not `TokenRecovery` (`token/error.rs:50-62` documents *why* `resume_pos` is explicit, never that it must advance), not the loop.

The two core producers do satisfy it (`reader.rs:210` `resume_pos = span.end`; `reader.rs:308` `resume_pos = s.len()`), so the crate is correct today. But both **public** extension points can violate it: a custom `TokenReader::peek` and `Lang::scan_specials` (which returns `TokenResult`, `reader.rs:191`) may each mint a `TokenRecovery` with any `resume_pos`. Verified empirically in a scratch copy: a reader whose `peek` errors with `resume_pos == pos` and a recoverable placeholder makes `NodesParser::parse` **hang in release** (`cargo test --release` → killed at timeout) while pushing one `Diagnostic` per iteration (unbounded memory growth as well). In debug it happens to trip an *unrelated* assert (`nodes_parser.rs:291`, "char token 0..1 is not contiguous with the pending run") — an accident of the chars-run invariant that would not fire for a `Comment`/`Char`-free placeholder kind, so it is not a guard.

Note the asymmetry that makes this a *design* question for this file: `resume_at` is deliberately **bidirectional** — it is also the absent-argument *rewind* (`argument_parsers.rs:85-86`) and the name-group rewind (`environment_parser.rs:115`) — so the monotonicity assertion cannot live inside it. Recommended:
- state the contract on `TokenRecovery::resume_pos` ("must strictly advance the reader past the error position; a reader violating this makes the content loop non-terminating") and reference it from `resume_at`'s doc, which today implies only "go to `pos`";
- guard it where it is load-bearing (`nodes_parser.rs:505`): capture `cx.tokens.pos()` before, and after `resume_at` either `debug_assert!` advancement or — better, since this is a *contract violation by third-party code*, not an internal invariant — return `Err(ParseError::new(kind, span))` when the position did not advance. That degrades a hang into the abort the project's error doctrine already blesses, with no panic (CLAUDE.md rule 4).

**2. `resume_at` routes around the readers' own positional guards; `TokenReader` has no positional move (API design / robustness) — `constructs/mod.rs:70-74`.**
The doc is candid that "go to `pos`" is *phrased* as a zero-width marker token, but two consequences deserve to be visible:
- `StdTokenReader::move_to_pos` (`reader.rs:141-145`) `debug_assert!`s `pos <= content.len()` **and** `content.is_char_boundary(pos)`. `resume_at` never reaches it: it calls the *trait* method `move_to`, which is a bare `self.pos = tok.span.start` (`reader.rs:394-399`). So a bogus `resume_pos` from a third-party reader lands the position mid-`char` or past the end with no diagnosis, and the next `peek` slices `content[pos..]` in `skip_whitespace` (`reader.rs:79`) — a lib-code panic, exactly what CLAUDE.md forbids.
- The marker trick silently adds an **unstated implementor contract**: `move_to` must be computed from the token's spans, never from token identity or list membership (a reader that looked the token up in its own list would be handed a synthetic token it has never seen). `StdTokenReader` and `TokenListReader` both comply — and both already carry an inherent `move_to_pos` (`reader.rs:141`, `list_reader.rs:64`) that the trait cannot reach.

Recommended: add `fn move_to_pos(&mut self, pos: usize)` to `TokenReader` (default body = today's marker trick, so no implementor breaks), let the two readers keep their guards, and reduce `resume_at` to a forwarding call — or delete it in favor of the trait method.

**3. The helpers are `pub(crate)`, but the protocols they encode are obligations of *public* extension points (API gap) — `constructs/mod.rs:70`, `constructs/mod.rs:81`.**
`ArgumentParser` is a public trait (`spec/structure.rs:87`) whose docs *require* implementors to follow two protocols this file owns: "absent means nothing was consumed — the reader is rewound past any probed noise" (`structure.rs:96-104`) and, per the 6.5 ruling, "token error while probing → absent without diagnosing (tolerant) / abort (strict)". An out-of-crate implementor gets `scan_argument_noise`, `stage_pre_space` and `ArgumentNoise::rewind` (all `pub`), but **not** `try_peek` and **not** `resume_at` — so a custom argument parser (or a `make_invocation_parser` takeover parser doing `\verb`-style repositioning) must re-derive both the recovery-policy peek and the marker-token trick from scratch, and any divergence produces exactly the double-report the design set out to avoid. Both helpers are already documented as protocol rather than as implementation detail; make them `pub` (and re-export them alongside `scan_argument_noise`), or fold them into the public surface some other way.

**4. The `cx.state` swap/restore protocol is hand-rolled at seven sites and belongs on `ParseContext` (API design / maintainability) — `constructs/mod.rs:100-115`.**
Every parser that scopes a child state repeats the same three-step dance, and each one must remember to restore **before** the `?`:

| site | what is scoped |
|---|---|
| `nodes_parser.rs:462-466` | invocation descent |
| `nodes_parser.rs:697-701` | group descent |
| `group_parser.rs:110-114` | group interior |
| `invocation_parser.rs:96-99` | per-argument delta |
| `argument_parsers.rs:538-541` | the optional-argument **probe peek** |
| `argument_parsers.rs:579-582` | optional-argument contents |
| `environment_parser.rs:109-112` | rigid name group interior |

All seven are correct today (each restores before propagating), but the correctness is per-site discipline, and `argument_parsers.rs:539-541` shows how unnatural it gets: `let open = try_peek(cx); cx.state = Arc::clone(&argument_state); match open? {…}` — a `Result` deliberately held un-`?`-ed across a restore. A scoping accessor on the context makes the ordering unrepresentable:

```rust
pub fn with_state<R>(&mut self, state: Arc<ParsingState<L>>, f: impl FnOnce(&mut Self) -> R) -> R
```

which type-checks for all seven (the closure reborrows `&mut self`, and `f` may capture the child parser mutably). I would frame the motivation as **ordering enforcement, not unwind safety**: an existing report flags the swap as "not unwind-safe", but the crate is `no_std`, `&mut ParseContext` is not `UnwindSafe`, and an unwind tears down the borrowed context anyway — a `Drop` guard would be over-engineering for a hazard nobody can observe. The `?`-ordering hazard, by contrast, is real, is repeated seven times, and grows with every new construct parser.

*Secondary, same type:* the file's stated reason for existing — "giving the API one place to grow (depth limits, cancellation)" (`mod.rs:97-99`) — is in tension with the all-public-fields struct-literal construction (7 literal sites today, and external drivers must construct it themselves until `Language::parse` lands in Phase 7): every new field is a breaking change at every construction site. A `ParseContext::new(tokens, source, state, session)` constructor (leaving the fields public for `state` mutation) would make the promised growth non-breaking.

**5. The module header overstates `ConstructParser`'s reach — arguments are *not* `ConstructParser` implementations (documentation accuracy) — `constructs/mod.rs:3-9`.**
"every construct — … arguments (the standard `ArgumentParser` implementations) … — is parsed by an implementation of it" is not true of the code. `ExpressionParser`, `GroupArgumentParser`, `OptionalGroupArgumentParser` and `MarkerArgumentParser` implement the **separate** `ArgumentParser` trait (`spec/structure.rs:87-110`): `&self`, `Send + Sync`, `Arc`-stored (tier 1), returning `Option<ParsedArgumentNodes>` with *no* delta channel. The only `ConstructParser` implementors in the crate are `NodesParser`, `GroupParser`, `StdInvocationParser`, `EnvironmentBodyParser` (plus test parsers). This is the two-tier model working exactly as §3.6 decided — but the header reads as if one trait covers both tiers, which is precisely the confusion the two-tier entry exists to prevent. Suggested rewording: name the two sibling parse traits, say that they share `ParseContext` and `ConstructParserResult` but sit in different tiers, and that arguments deliberately have no after-effect delta.

**6. `try_peek`'s tolerant arm ignores recoverability, unlike the content loop (design note / latent) — `constructs/mod.rs:86-93`.**
`NodesParser` distinguishes recoverable from unrecoverable token errors even in tolerant mode (`nodes_parser.rs:501-502`: `into_recovery() == None` → abort). `try_peek` does not: under `Tolerant` it returns `Ok(None)` for *any* `TokenError`. For an unrecoverable error inside an argument that means the parser reports the argument absent (recording its own "missing mandatory argument" diagnostic first), rewinds, and the enclosing loop then aborts anyway — same outcome, but preceded by a misleading diagnostic. No core code path produces `recovery: None` today (both producers pass `Some(..)`), so this is only reachable through a custom reader / `scan_specials`; still, mirroring the loop (`if error.recovery().is_none() { return Err(..) }`) would be one line and would remove an inconsistency between the crate's two token-error mapping sites.

**7. Test-coverage gaps — the file has no tests, and its documented recovery contract is unpinned (test-coverage).**
`src/constructs/mod.rs` contains no `#[cfg(test)]` module; nothing anywhere targets its helpers directly. The specific holes, in priority order:
- **`try_peek` is completely untested — both arms.** Every construct test module hard-codes `forbidden_chars: String::new()` (`argument_parsers.rs:767`, `group_parser.rs:211`, `environment_parser.rs:733`) and no test produces a tokenizer error at an argument-probe or name-group position, so the file's headline claim — tolerant → `Ok(None)` with *no diagnostic and no consumption*, the enclosing loop reporting the error **exactly once** — is asserted nowhere, across two files. I verified it holds by adding scratch tests: with `forbidden_chars = "#"` and a `\x{arg}` macro, `\x#{a}` under `Tolerant` yields exactly two diagnostics, `["missing mandatory argument", "character is forbidden here: ‘#’ (U+0023)"]` (no double report, no lost report; tree invariants pass, reader ends at 6), and under `Strict` aborts with `ParseErrorKind::Token(ForbiddenChar { ch: '#' })`. Those two assertions are the tests to add — they are cheap, and they are the only executable statement of the 6.5 "token error while probing" ruling.
- **The `resume_pos` advancement contract has no test** (finding 1): a reader returning a non-advancing recovery should abort, not spin. Worth a test once the guard exists — it is the only way to pin a non-termination bug.
- **`resume_at` is never tested directly**: a two-line unit test asserting `pos()` lands exactly on `pos` for both `StdTokenReader` and `TokenListReader` (and that a subsequent `peek` re-reads from there) would pin the marker-token trick, which currently rides entirely on `move_to`'s span convention.
- Also untested: an argument-probe token error inside an *optional* argument (the probe runs under a **derived** state at `argument_parsers.rs:538-541`, the one place where `try_peek` is called with a swapped state).

**8. Positive confirmations (checked, not defects).**
- **Trait shape.** `ConstructParser<L>` with `type Output` is dyn-compatible once `Output` is pinned, which is exactly how `make_invocation_parser` uses it (`Box<dyn ConstructParser<L, Output = BuildId> + 'a>`, `spec/callable.rs:72`); `&mut self` (rather than `self`) is *required* for that boxed call and is what lets working state live in fields (`NodesParser.run`/`.nodes`). Matches the §3.6 decision precisely.
- **`ConstructParserResult<L, T>`** follows the `TokenResult` lang-first convention (the 6.1 amendment) and correctly derives the error's origin parameter from `L`.
- **`Invocation`'s lifetimes are right.** `spec: &'a Arc<dyn CallableSpec<L>>` (rather than an owned `Arc`) is what lets `nodes_parser.rs:460-461` hold the spec borrow across `make_invocation_parser(invocation)` (which moves the `Invocation`) while `StdInvocationParser` still clones the `Arc` into the node (`invocation_parser.rs:160`) — no per-invocation refcount traffic for specs that don't store it.
- **Per-invocation `Box` allocation** is a known, accepted cost with a *scheduled* action (`Phase6Execution.md:47`, §6.7 "benchmark before phase close"). No `benches/` directory and no `#[ignore]`d timing test exists yet, so the flagged item is still open — flagging that it is tracked, not re-litigating it.
- **`ParseContext` field-disjoint borrows** work as intended: `try_peek`'s `cx.tokens.peek(&cx.state)` compiles only because it is a free function over `&mut ParseContext` (a `&mut self` method could not split the fields) — worth keeping in mind if the helpers ever migrate onto the type.
- Naming conforms to NAMING_STRATEGY (`ConstructParser`/`ParseContext`/`ConstructParserResult`, no `Latex`/`get_` prefixes, no vague `Context`/`Result`).

*Two cosmetics:* `ParseContext`'s `Debug` impl (`mod.rs:131-139`) omits the `source` field added in 6.4; and the crate-root convenience re-export list (`lib.rs:84-88`) was never updated for the 6.4/6.5 public items (`StdInvocationParser`, `EnvironmentBody`/`EnvironmentBodyParser`, the four argument parsers, `scan_argument_noise`, `stage_pre_space`, `ArgumentNoise`) — they are reachable only via `techy::constructs::…`.

**Net:**
1. **#1** — document + guard the `resume_pos` advancement contract (a verified release-build hang reachable from two public extension points; abort instead of spin).
2. **#7** — add the two `try_peek` tests (tolerant: one report, not two; strict: `Token(ForbiddenChar)` abort). Cheapest way to pin the file's own documented contract.
3. **#2** — put `move_to_pos` on `TokenReader` so `resume_at` stops bypassing the readers' boundary guards.
4. **#4** — move the state swap/restore into `ParseContext::with_state` (seven sites; ordering enforcement, not unwind safety) and add a constructor before external drivers appear.
5. **#3** and **#5** — make `try_peek`/`resume_at` public, and correct the module header's claim about arguments.
6. **#6** and the cosmetics — low priority.
